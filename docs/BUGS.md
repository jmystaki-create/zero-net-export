# Zero Net Export Bug Tracker

This document is the single source of truth for active bugs, regressions, validation status, and bug closure in this project.

If another project document appears to disagree about whether a bug is still open, whether it was validated, or whether it is actually closed, follow this file.

## How to use this file

Use this file for all of the following:
- logging a newly discovered bug or regression
- recording how the bug shows up in the real product
- tracking whether the bug is only in the repo, only in the live install, or both
- recording the fix commit or release candidate that should contain the fix
- tracking validation status
- closing the bug only after the right evidence exists

This file is for real bug state, not vague ideas.

## Bug workflow

### 1. Post a bug
When a new bug is discovered, add an entry with:
- bug id
- title
- status
- severity
- area
- where seen
- current observed behavior
- expected behavior
- evidence
- suspected cause if known
- next action

### 2. Work a bug
While fixing a bug, keep the entry updated with:
- current owner/workstream if helpful
- relevant branch/commit
- what changed
- whether the bug is believed fixed only in repo or also validated live

### 3. Validate a bug
A bug is not considered truly fixed just because code changed.
Validation should state:
- where validation happened
- what exact evidence was checked
- what version/build/commit was involved
- whether the bug is resolved in live Home Assistant or only in repo state

### 4. Close a bug
Only close a bug when the fix has the right evidence for that bug.
If the bug affects live Home Assistant behavior, closure should normally require live validation, not just repo confidence.

## Status values

Use these values:
- `open` — bug is confirmed and not yet fixed
- `in_progress` — active fix work is underway
- `fixed_pending_validation` — a fix exists in repo or candidate build, but closure evidence is still missing
- `validated` — fix has been validated with the right evidence
- `closed` — bug is complete and can be treated as done
- `deferred` — known but intentionally not the current priority

## Severity values

Use these values:
- `critical` — blocks load, setup, or the core product path
- `high` — major user-visible breakage or strong regression
- `medium` — important but not fully blocking
- `low` — minor issue, cleanup, or polish-level defect

## Bug areas

Suggested area labels:
- `runtime`
- `config_flow`
- `managed_devices`
- `source_mapping`
- `controls`
- `sensors`
- `diagnostics`
- `release`
- `docs`
- `process`

## Current active bugs

## ZNE-001 — Repairs platform contract breaks integration recovery
- **status:** `validated`
- **severity:** `critical`
- **area:** `runtime`
- **where seen:** live Home Assistant after `0.1.82` deploy/restart
- **current observed behavior:** Zero Net Export entities restore as unavailable, and logs show `HomeAssistantError: Invalid repairs platform <module 'custom_components.zero_net_export.repairs' ...>`
- **expected behavior:** Repairs platform loads cleanly and the integration recovers with live entities instead of staying restored/unavailable
- **evidence:** live HA logs after the `0.1.82` restart; affected entities included `sensor.zero_net_export_status`, `sensor.zero_net_export_command_center_status`, `sensor.zero_net_export_stale_source_count`, `binary_sensor.zero_net_export_safe_mode`, and `sensor.zero_net_export_reason`
- **suspected cause:** missing Repairs flow contract in `custom_components/zero_net_export/repairs.py`
- **repo fix:** `ad44fc1` — `fix: add repairs platform entry flow`
- **validation status:** validated against the live `0.1.83` deploy on 2026-04-15. Fresh post-restart log review no longer showed `Invalid repairs platform`, and the earlier `build_release_info()` setup-retry traceback also did not reappear. This clears the original hard-load failure even though broader entity recovery is still unresolved.
- **next action:** keep ZNE-002 open as the follow-on runtime recovery bug, because the hard repairs-platform failure is gone but the integration/entities still are not fully present live

## ZNE-002 — Main Zero Net Export entities stay restored/unavailable after restart
- **status:** `fixed_pending_validation`
- **severity:** `critical`
- **area:** `runtime`
- **where seen:** live Home Assistant after the `0.1.82` deploy/restart, and still unresolved after the exact `0.1.83` repo build was deployed to Home Assistant
- **current observed behavior:** earlier live inspection showed no active `zero_net_export` config entry and no controller device/entities after restart. That exact condition is still not present in the current live install: the config entry exists again and core Zero Net Export entities are live through the API, but another explicit restart/reload proof is still missing, so restart persistence is not yet re-validated.
- **expected behavior:** the live Home Assistant install should retain or recreate an active `zero_net_export` config entry so the controller device and core runtime entities load normally after restart
- **evidence:** earlier live SSH/API inspection showed `0` `zero_net_export` config entries, `0` Zero Net Export devices, and only `update.zero_net_export_update` visible. In this run, documented HA SSH access on `root@192.168.86.200:2222` succeeded again and direct inspection of `/config/.storage/core.config_entries` still shows one `zero_net_export` entry (`01KP8MW539MQ724BBFZX2EF6S2`, title `Zero Net Export`, created `2026-04-15T13:21:46.601855+00:00`). Direct Home Assistant API inspection in this run also returns 145 live `zero_net_export` entities, including `sensor.zero_net_export_status`, `sensor.zero_net_export_command_center_status`, `sensor.zero_net_export_unmanaged_candidate_overview`, and `button.zero_net_export_show_fleet_console`.
- **suspected cause:** the earlier missing-config-entry state was real, but the live system has since recovered past that blocker, likely via successful Add Integration. The remaining runtime/configure failures are now narrower follow-on bugs rather than absence of the entry itself.
- **validation status:** improved materially but not yet closed. The exact missing-config-entry condition is no longer reproducible live and the entity surface is back, but restart persistence still needs re-validation before this bug can be closed confidently.
- **next action:** keep this bug at `fixed_pending_validation`, then re-test restart/reload persistence after the current exact-build drift is cleared so the entry/device/entity set is proven stable across another live restart

## ZNE-003 — Requested native UI outcome still not visibly delivered
- **status:** `fixed_pending_validation`
- **severity:** `high`
- **area:** `managed_devices`
- **where seen:** product-level review against live/native UI expectations
- **current observed behavior:** some UI scaffolding exists, but the requested visible native UI outcome still does not feel complete to James. Repo-side native device-page review surfaces are stronger again after this run, the main fleet surfaces now also call out how much of the unmanaged backlog still needs explicit review, and the shared managed-fleet snapshots now carry fixed-versus-variable mix plus nominal power so Configure and device-page review read more like an operational fleet workspace. Repo HEAD advanced that continuity again in this run with `85e6ef9`, which keeps the unmanaged backlog snapshot visible across the existing-fleet enablement/edit/remove flows. Follow-on repo work in `3f91c5d` then realigned the opening command-center `Fleet activity` block so it leads with the managed/unmanaged split before the lower-priority enabled/usable composition detail, and this run applies the same blocker-first ordering to `managed_fleet_overview` so blocked or actively planned managed devices stay visible before the healthy fleet-composition counts. This run closes one more smaller console drift in repo state: empty-fleet command-center `Fleet activity` summaries now surface `repair sources first` before the unmanaged candidate mix, so the opening console matches the Managed Devices workspace when source blockers still outrank promotion work. This run also makes the unmanaged backlog summaries name the first review-first candidate when it differs from the top promotion target, and now carries that review-first candidate's own fit hint through the shared fleet snapshots too, so Configure, Managed Devices, and the opening Fleet activity block no longer imply the highest-ranked candidate is automatically the item that most needs operator attention. A follow-on repo fix in `fe5b0df` keeps that same review-first hint visible inside the opening command-center Fleet activity block itself, so the top console no longer drops the first review target's own usefulness cue whenever the highest-ranked promotion candidate and the first item needing operator review are different devices. This run closes the next handoff gap in that same workflow: empty-fleet command-center guidance and the native Managed Devices next-step sensor now point operators at the first review-first unmanaged candidate when the backlog still needs vetting, instead of skipping straight to whichever candidate ranked highest. This watchdog run found the follow-on continuity work was still incomplete on the native device-page Managed Devices handoffs, though: `custom_components/zero_net_export/button.py` was still building `promotion_handoff` from the top-ranked unmanaged candidate, so the `Review managed devices workspace` and `Review managed devices` buttons could contradict the review-first guidance already shown in Configure. Repo HEAD now closes that repo-side drift too, so Configure and the native device-page handoffs stay aligned on the first unmanaged candidate that actually needs operator review when it differs from the top promotion target. A follow-on repo pass in this run found one more smaller continuity leak on those same native device-page review surfaces: the workspace and review notifications still attached usefulness and warning detail only to the top-ranked unmanaged candidate, so the actual first review-first device could lose its own fit guidance whenever it differed from the promotion target. Repo HEAD now closes that follow-on drift too by surfacing the first review-first candidate plus its own usefulness and warning summary directly inside both native device-page Managed Devices notifications. The same repo pass then found the top fleet surfaces were still surfacing idle `hold` noise as if it were an active plan in Configure -> Managed Devices and the primary device-page managed summary sensor, which weakened the operator-console scan the `0.1.87` map expects. Repo HEAD now closes that smaller top-surface drift too by hiding idle-hold wording unless a real planned action exists. This run closes the next promotion-flow continuity gap too: post-save Managed Devices success feedback now reopens Configure -> Managed Devices with a candidate-specific next step, preferring the first review-first unmanaged device when vetting still needs to happen instead of dropping back to a generic promote-the-next-candidate handoff. This run closes the next review-first backlog legibility gap too: when the first unmanaged device needing review differs from the top promotion target, the shared unmanaged overview, `managed_fleet_overview`, and the opening command-center `Fleet activity` block now keep that review-first device's own warning hint visible as well, so the native fleet surfaces stop downgrading the actual operator-review target to a bare name plus confidence label. This run also makes the device-page managed review rows more audit-friendly by appending a compact why-fragment for active planned actions or current blockers, so deeper per-device review keeps the operator-facing reason visible without reopening the full detail body. Live HA validation is still needed before the bug can move past `fixed_pending_validation`.
- **expected behavior:** the product visibly delivers:
  1. clear managed vs unmanaged device experience
  2. clear promote / vet / review flow
  3. clear separation of Controls / Sensors / Managed Devices / Diagnostics
- **evidence:** direct user feedback plus repo/design audit captured in `docs/UI_DESIGN.md` and `docs/UI_IMPLEMENTATION_MAP.md`; repo inspection on 2026-04-15 also confirmed the managed-fleet workspace sensors and fleet-console helper were still marked diagnostic, which buried Managed Devices review under diagnostic-style entity presentation instead of treating it as a first-class fleet workspace; later same-day repo inspection also found the new managed/unmanaged and promotion-flow copy had landed in `custom_components/zero_net_export/strings.json` but not in `custom_components/zero_net_export/translations/en.json`, leaving the shipped English UI text behind the intended native flow wording; additional repo inspection in this run confirmed the Configure -> Managed Devices snapshot was still deriving `usable` counts from parse-only config data, so fleets with healthy runtime devices still displayed `usable: 0` and the edit/remove selectors hid real runtime readiness behind config-only labels; further repo inspection in this run confirmed the native integration device page still lacked a dedicated first-class managed-device deep-review action, leaving per-device runtime review implicit across scattered entities rather than a single obvious native handoff; repo inspection in this run also confirmed install-provenance uncertainty was still only exposed as secondary Diagnostics detail instead of a top-level command-center alert/recommended-path blocker, which weakened the intended global-plus-local problem-signal model; repo inspection after `a67acdc` also confirmed the device-page deep-review path has now advanced again with per-device managed review buttons for each configured load, so the source-of-truth bug state needed to be refreshed to match the shipped repo candidate before the next release-approval handoff; repo plus live-API corpus inspection in this run also confirmed the unmanaged promotion shortlist still needed stronger filtering because replaying the current Home Assistant entity set through repo candidate discovery was still surfacing obvious media-feature toggles such as speech enhancement/subwoofer controls and even `switch.zero_net_export_enabled` as promotable Managed Devices candidates; this run's documented live API corpus replay confirmed the same shortlist noise still included clearly non-load variable settings such as `number.bathroom_room_temperature_limit`, `number.switchbot_lock_pro_auto_relock_time`, `number.system_rome_dyn_price_fee`, `number.system_rome_dyn_price_vat`, `number.x1_p6k_us_s_battery_capacity`, and `number.living_room_sub_gain_2`; repo inspection in this watchdog run then confirmed the bug tracker had drifted ahead of repo reality: `custom_components/zero_net_export/config_flow.py` already preferred the first review-first unmanaged candidate in `_device_next_step(...)`, but `custom_components/zero_net_export/button.py` was still passing only the top-ranked candidate into `_managed_devices_workspace_handoff(...)`, so the native device-page Managed Devices handoffs could still contradict the Configure-side review-first guidance. Targeted repo validation in this run passes with `python3 -m unittest -q tests.test_config_flow_device_runtime_overlay tests.test_button_entity_categories tests.test_command_center_summary`, including new coverage for both the Configure and device-page handoff paths when the first review-first candidate differs from the top-ranked promotion target; follow-on repo inspection in this run then confirmed the same device-page notifications still limited usefulness and warning detail to the top-ranked candidate even after the review-first handoff fix, so the first review-first device could still lose its own fit context on the native review path. Targeted repo validation in this run also passes with `python3 -m unittest -q tests.test_button_entity_categories tests.test_config_flow_device_runtime_overlay` and `python3 -m unittest discover -q`, including new coverage that locks the first review-first candidate guidance into both native device-page Managed Devices notifications and the related exported button attributes. Repo inspection in this run also found that some command-center and Repairs fallback text still dropped back to vague `Open Configure` / `Configure flow` wording instead of the exact native Home Assistant path required by the current UI source of truth; this run also found the add-integration bootstrap copy still telling operators to use Home Assistant's "normal Configure flow" and the integration's "Configure screens" instead of the exact native Configure path defined by the UI source of truth; documented live Home Assistant API inspection in this run confirmed `sensor.zero_net_export_unmanaged_candidate_overview` and `sensor.zero_net_export_candidate_shortlist` were still publishing the exact same `AC Outlet 2; Lounge Room - Heated Floor; Air Purifier; +175 more` state with `candidate_count=178`, so the supposed overview was not actually giving operators a distinct managed-vs-unmanaged snapshot; repo inspection in this run also confirmed `sensor.zero_net_export_managed_fleet_overview` was still much thinner than the richer Configure fleet summary, exposing only managed/enabled/usable counts and leaving blocked devices or active fleet plans invisible at a glance on the native fleet surface; this run's documented live Home Assistant API inspection also confirmed the empty-fleet native `managed_fleet_overview` state was still only `No managed devices yet` even while 178 unmanaged promotion candidates were present, so the main native fleet surface was still hiding the unmanaged backlog exactly where the UI design expects the managed-vs-unmanaged split to stay obvious; documented live Home Assistant API inspection in this run also confirmed the newer live `managed_fleet_overview` still reads `0 managed | 163 unmanaged | top AC Outlet 2` while `sensor.zero_net_export_command_center_status` reports missing required source roles, so the local fleet surface still is not echoing the blocking source-repair urgency that the UI design expects to appear globally and locally; this run's repo inspection also confirmed `build_native_command_center_summary(...)` was still leaving `device_next_step` pointed at Managed Devices even when missing required source roles or install-provenance blockers already made Sensors or Diagnostics the true next path, which meant the command-center attributes and managed-device workspace handoffs could still contradict the top-level native alert model even in the repo candidate; repo inspection in this run then found the same contradiction still existed inside the native Configure -> Managed Devices forms themselves, where `device_next_step` kept pushing candidate promotion even when the command center already said Sensors or Diagnostics owned the real next action; this run's repo inspection also confirmed the command-center Fleet activity block still was not echoing the unmanaged backlog or top promotion target, which weakened the managed-versus-unmanaged legibility the UI design expects in the opening console; repo inspection in this run also confirmed the main native fleet summaries still reduced blocked devices and active plan state to anonymous counts instead of naming the first affected managed device, which kept the device-page fleet surface and opening command-center board less actionable than the UI design intends; repo inspection in this run then found the opening command-center Fleet activity block still dropped blocked managed devices entirely whenever a device was unusable but no blocked plan count was active yet, which meant the top operator console could look healthier than the actual managed fleet state until the next plan cycle; repo inspection in this run also confirmed the Managed Devices Configure, enablement-review, edit, and remove forms still buried global Sensors/Controls/Diagnostics ownership in the trailing `Recommended fleet action` line instead of giving operators an explicit blocker-first summary at the top of each fleet-management form; a follow-on repo inspection in this run then confirmed those same Configure Managed Devices lists were still sorting healthy ready devices ahead of blocked ones, which let the native fleet workspace bury the first managed-device exception even after the blocker-first wording landed; this run's repo inspection also found the Configure -> Managed Devices fleet labels were still missing live `current_power_w`, variable `current_target_power_w`, and `last_action_status` data even though the runtime coordinator already had those fields, which left the native fleet workspace less operationally legible than the matching device-page review rows; this run's repo inspection found one last helper-path leak still visible in the entity registry, where `sensor.zero_net_export_fleet_console_next_step` was still surfaced to operators as `Fleet console next step` even though the current native-only UI source of truth has already moved the user-facing language to Managed Devices workspace wording; this run's documented live Home Assistant API corpus replay against current repo candidate discovery confirmed the unmanaged promotion pool was still too noisy, with 163 surfaced candidates dominated by generic `light.*` fixtures plus non-load variable helpers such as buy/sell price, tax-percent, and surround-level controls instead of a tighter appliance-focused shortlist; a follow-on corpus replay in this run confirmed one more repo-side shortlist leak, where obvious lighting circuits exposed as `switch.*` entities such as `switch.batten_roof_lights` were still surviving promotion discovery even after generic `light.*` entities had already been filtered out; this run's refreshed live-state corpus replay against repo candidate discovery then found one last raw-hardware leak, where `switch.shellyproem50_a0dd6ca0970c_switch_0` was still surfacing as a promotable Managed Devices candidate even though its only visible label was the Shelly Pro EM meter identifier rather than a human-meaningful load name; and this run's live API corpus replay confirmed `_none` media-wrapper switches such as `switch.living_room_none`, `switch.lounge_room_none`, and `switch.master_bedroom_none` were still surviving promotion discovery as fake `Room Power` candidates even though they are service-style speaker wrappers rather than first-class discretionary loads; and this run's fresh live API corpus replay against the current repo candidate also confirmed one remaining ranking-quality gap, where vague room/circuit labels such as `Garage Power` and `3rd Bedroom Power` plus anonymous hardware labels like `AC Outlet 2` were still surfacing too high in the unmanaged order even after the larger shortlist-noise reductions had landed. This run's documented live API corpus replay then found one more appliance-fit leak in the same shortlist, where ventilation-style loads such as `switch.exhaust_fan` and `switch.hood_power` were still surviving unmanaged promotion discovery even though the surrounding shortlist was already trying to stay centered on clearer discretionary export loads. This watchdog run then found one more ranking leak in the same unmanaged order: `AC Outlet 2` was still being falsely boosted by the broad `ac ` positive-load keyword, so anonymous outlet hardware could rank above clearer appliance loads like `Dishwasher Power` even though the shortlist is meant to prefer real appliance names. A follow-on repo inspection in this run also found the native command-center guide text itself still contradicted the current UI source of truth by telling operators that managed-device workflow belongs on the device page, even though Configure -> Managed Devices is the primary fleet workspace and the device page is only the deeper review path. This run's repo inspection then found one more managed-vs-unmanaged continuity gap: the fleet enablement, edit, and remove subflows still dropped the unmanaged backlog context, so once an operator left the main Managed Devices screen the top-managed and bottom-unmanaged workspace split stopped being visible. Repo inspection in this run then found the same continuity gap still existed deeper in the promotion flow, where candidate review, preset choice, and the final save form dropped the managed-top and unmanaged-bottom workspace context after the shortlist/full-list entry points. This watchdog run rechecked repo HEAD after the latest product-work commits and confirmed that the remaining repo-side continuity gap had moved again: `1aceae9` kept managed-workspace context visible through fleet enablement, edit, and remove subflows, `71b5f9c` carried the same managed/unmanaged context through candidate review, preset choice, and add/save, `fc5edaa` tightened the shortlist and full candidate-pick screens so they explicitly restate the managed-top and unmanaged-bottom split plus candidate-mix counts, and the paired docs commits `9ad3dcd` and `a1c3831` realigned the source-of-truth wording and release gate around that same Configure-first managed-workspace model. This run also found one remaining wording leak on the device-page Diagnostics path: after the buttons were renamed to `Review diagnostics` and `Review diagnostics snapshot`, the generated notification body still opened with `Zero Net Export native support center` and `Support snapshot`, which kept the deeper native Diagnostics surface out of sync with the current bucket naming. A follow-on repo inspection in this run found the companion diagnostics snapshot payload itself still opened with `Zero Net Export support snapshot`, so the snapshot action title and body were not yet aligned on the Diagnostics bucket wording. This watchdog run then found one more stale device-page path leak: the shared `DETAILED_MANAGEMENT_PATH` string still told operators to use the device page for `native support actions` even though the bucket and actions are now explicitly Diagnostics. This run's repo inspection then found that the post-save Managed Devices success landing still dropped the blocker-first command-center handoff and unmanaged-backlog context, so promotion or edit success could send operators straight back into fleet work even while Sensors or Diagnostics still owned the true next native step. This watchdog run also found a repo-doc follow-through leak: `README.md` and `docs/VALIDATION_CHECKLIST.md` were still advertising the retired device-page `Show support center` / `Show native diagnostics snapshot` wording even though the shipped native buttons had already been renamed to `Review diagnostics` and `Review diagnostics snapshot`. A follow-on repo inspection in this run found the optional Lovelace debug scaffold still leaked the same retired diagnostics wording and stale button entity ids, pointing operators at `button.zero_net_export_show_support_center` and `button.zero_net_export_show_native_diagnostics_snapshot` even though the shipped native entities are now `button.zero_net_export_show_native_support_center` and `button.zero_net_export_show_native_diagnostics`. This run's repo inspection then found one more IA drift pocket in Configure itself: the main Managed Devices screen still framed the other buckets as a negative `Not here` list, and the Diagnostics Configure screen still used a heavier `Current blocker` / `Mapped-source diagnostics` / `Native paths` structure that made the four-bucket ownership feel less explicit than the newer command-center wording. A follow-on repo inspection in this run then found one last per-device review leak still visible on the native device page, where the `Review {device}` notification body still printed a raw `Entity: switch.*` line and repeated the same raw entity id in its next-action guidance even after the rest of Managed Devices had moved to concise operator-facing previews. This watchdog run then found the same raw-id leak still surviving one level higher in the native `Review managed devices workspace` and `Review managed devices` row summaries, where each managed-device line still ended with `entity=switch.*` / `entity=number.*` even though the rest of the Managed Devices copy had already been cleaned up to stay operator-facing. This run also found the same operator-facing raw-id leak still present in the main Configure -> Managed Devices fleet summary rows, which were appending the raw managed `entity_id` after the runtime summary instead of staying on friendly managed-device copy. A follow-on repo inspection in this run then found the same native-only polish gap still leaking through mapped-source attention summaries, role summaries, and diagnostics/support copy, where source blockers such as `sensor.pv_power` and `sensor.grid_import` were still shown as raw entity ids instead of concise operator-facing source labels. This run's repo inspection also found the native diagnostics snapshot was still appending raw `entity=...` details to mapped-source health rows and managed-device review rows even after the wider operator-facing copy cleanup had landed elsewhere. This run's repo inspection then found one more stage-8 continuity gap on the native device-page review path itself: the managed-device workspace and per-device review buttons still collapsed unmanaged backlog state to a bare candidate count plus a top candidate preview, instead of repeating the richer candidate-mix snapshot that makes the managed-top and unmanaged-bottom split easier to scan at a glance. This run's repo inspection found that the main native fleet surfaces were still stopping at unmanaged count plus top candidate, so `managed_fleet_overview` and the command-center Fleet activity block were not yet carrying the fixed-versus-variable promotion mix that makes the backlog easier to scan at a glance. Repo HEAD now closes that repo-side gap in `53fa040`, so the remaining product gap is live redeploy and visual proof, not another fleet-summary omission in code. This run then found one more opening-console gap still visible in repo state: the command-center Fleet activity block was still leading with a generic device-status phrase instead of a managed/unmanaged-first fleet snapshot, which made the top operator board feel less aligned with the stronger native Managed Devices summaries. This run's repo inspection then found one remaining shorthand gap inside those richer fleet summaries themselves: the opening Fleet activity block and `managed_fleet_overview` still stopped at top-candidate naming without carrying the same compact fit or first-warning hint already used deeper in candidate review, so the operator still had to drill down to judge whether the leading unmanaged target looked credible. A follow-on repo inspection in this run found the same continuity gap still present inside the Configure-managed subflows and save feedback, where the shared unmanaged snapshot only named the top candidate and candidate mix counts without repeating the quick fit/warning hint already visible in the richer fleet sensors. This run's fresh live API corpus replay against repo HEAD then confirmed one more review-quality gap in the same unmanaged path: even after the earlier ordering fixes, anonymous hardware labels like `AC Outlet 2` and vague circuit labels like `Garage Power` were still being presented on native fleet surfaces as if they were `strong match` promotion targets instead of being explicitly downgraded to review-first candidates. A follow-on repo pass in this run then found one more backlog-legibility gap: even after the top candidate review hint landed, the main `managed_fleet_overview`, the opening command-center Fleet activity block, and the shared candidate overview text still did not report how many surfaced unmanaged candidates remained review-first, which made the backlog look more promotion-ready than the actual fit mix. Repo HEAD now closes that repo-side gap too, and live API replay against the current Home Assistant corpus shows the updated summary shape would read `11 candidates | 9 fixed candidates | 2 variable candidates | 5 need review | top Lounge Room - Heated Floor | likely useful`. This run closes one more opening-console handoff gap in repo state: when runtime currently has no managed fleet, the command-center `next_action` and `device_next_step` guidance now treat that as an empty-fleet situation even if stale config inventory still exists behind the scenes, and they point operators at the top unmanaged candidate with a compact operator-facing preview instead of falling back to a generic `continue managed-device setup` line. This run also closes a remaining four-bucket IA leak on the Diagnostics Configure screen: the bucket handoff block now lists the exact Sensors, Controls, live mode, Managed Devices, and Diagnostics native paths instead of using a vague `Sensors repair path` / `Managed Devices stays in Configure` summary that weakened section ownership. A follow-on repo inspection in this run then found the next promotion-flow mismatch: even after the summary fixes, the empty-fleet command-center handoff and `sensor.zero_net_export_fleet_console_next_step` were still skipping straight to the highest-ranked unmanaged candidate when the surfaced backlog still had a different review-first device that needed vetting first. Targeted repo validation in this run now also passes with `python3 -m unittest -q tests.test_candidate_utils tests.test_sensor_entity_categories tests.test_command_center_summary` and `python3 -m unittest discover -q`, including new coverage that locks the first review-first candidate's warning hint into the shared unmanaged overview, native managed-fleet overview sensor, and opening command-center fleet summary. This run also passes `python3 -m unittest -q tests.test_button_entity_categories tests.test_config_flow_device_runtime_overlay tests.test_command_center_summary`, including new coverage for compact planned-action and blocker why-lines on the native device-page managed review rows.
- **suspected cause:** implementation is still too scaffold/text-heavy and not yet sufficiently productized in live HA, translation artifacts can drift when native flow copy changes land without the matching `translations/en.json` update, some Configure fleet summaries were still built from parsed config inventory without overlaying live `device_details` readiness/status, the device-page deep-review path was still implied rather than surfaced as its own obvious action, install-provenance blockers were not yet treated as first-class command-center alerts even though exact-build trust is part of the operator workflow, the shared unmanaged-candidate heuristics were still permissive enough to treat internal Zero Net Export controls plus media-feature toggles as credible promotion targets, obvious variable configuration and telemetry numbers were still not being excluded from promotion discovery, raw infrastructure-style meter switch names were still being treated as credible controllable loads when they lacked a clear appliance-style label, `_none` entity-id media wrappers were still being treated as promotable fixed loads because the existing shortlist penalty demoted them without fully excluding them, some fallback wording paths had not been converted from generic Configure language to the exact native path text used elsewhere in the operator flow, the fleet-overview sensor itself still was not repeating mapped-source blocker urgency locally even when source repair clearly outranked promotion work, the command-center Fleet activity block was still keying blocked-state visibility off `blocked_planned_action_count` instead of the actual unusable managed-device set, the Configure Managed Devices sort order was still ranking enabled/usable devices ahead of blocked ones instead of surfacing the first local exception first, the native command-center guide copy had also drifted into telling operators that managed-device workflow belongs on the device page even though the current design makes Configure -> Managed Devices the primary fleet workspace, the deeper Diagnostics guide text had lagged behind the renamed `Review diagnostics` device-page actions and was still using the older support-center headings, and the config-flow runtime overlay was still not carrying current power, variable target power, or last-action detail into the managed-device labels even though those runtime signals already existed in coordinator state, and the shared shortlist ranking still was not applying enough penalty to vague room/circuit `* Power` labels or anonymous numbered outlet/plug labels when clearer appliance-style candidates were present, while ventilation-style names such as exhaust fans and range-hood power were not yet being treated as non-load shortlist noise, and the broad `ac ` appliance keyword was still falsely matching anonymous `AC Outlet` hardware labels as if they were air-conditioner loads; this run then found the shortlist and full candidate-list forms were still missing the blocker-first and managed-versus-unmanaged context that the design expects to stay visible throughout the promotion workflow; this run then found the Managed Devices forms and save feedback still phrased the deeper device-page review path too much like a peer workflow instead of clearly secondary to Configure -> Managed Devices; and this run found one last smaller copy leak where the main Configure workspace candidate snapshot, fixed/variable candidate summaries, shortlist labels, and shortlist review copy were still inheriting `build_candidate_preview(...)`'s raw entity-id default instead of the concise operator preview wording already used elsewhere in Managed Devices. This watchdog run then found the same raw entity-id fallback still surviving in `_format_device_review_line(...)`, so the managed-device workspace and review row summaries were still appending `entity=...` even after the broader candidate-preview cleanup had landed. This run also found `_device_status_label(...)` still appending the raw managed `entity_id` into Configure -> Managed Devices fleet summary rows. This run also found the mapped-source attention helpers were still defaulting to raw `entity_id` output because freshness diagnostics only carried entity ids, so operator-facing source blocker copy had no friendly-label fallback. The remaining stage-8 device-page review gap was also partly structural: those native notifications were not reusing the richer candidate-overview snapshot helper already used on the fleet surface, so the managed/unmanaged split lost useful fixed-vs-variable mix context once the operator switched into deeper review. That main-fleet surface gap was repo-side and is now addressed on HEAD by `53fa040`; the remaining gap is that live Home Assistant is still behind the current repo candidate, so the richer fixed-versus-variable backlog split is not yet validated in the installed product. This run also confirmed one last shared-fit inconsistency in the promotion heuristics: helper-backed `input_number` controls could still be upgraded back to `high` confidence by positive appliance-style names even though the helper warning itself makes them review-first candidates rather than native-control strong matches. Repo-side follow-up in this run now also makes shared candidate labels honor that review-first posture consistently across previews and fleet snapshots instead of mixing helper warnings with `strong match` or `plausible match` wording. This run then found a smaller remaining backlog-legibility gap, where shared unmanaged summaries still only counted review-first candidates without naming the first one when the top-ranked candidate looked usable, which could make the promotion target and the operator's first review target diverge in the opening console and Managed Devices workspace. This watchdog run then found one more Workstream F cleanup gap on the native device-page Diagnostics path: the `Review diagnostics` guide was still appending the full diagnostics snapshot inline, turning a support handoff into another wall-of-text notification instead of a compact guide plus a separate deeper snapshot path.
- **repo fix:** `9c988a8`, `2603de9`, `f804286`, `827530c`, `5c4ff5c`, `0acb62e`, `96fe3d2`, `28c1834`, `45c61ec`, `55c68bd`, `e9e0189`, `a0046ee`, `7cd6c35`, `b6ddcd2`, `c41929c`, `23f2e9c`, `8ad2e88`, `9e25578`, `469825e`, `f45618c`, `a67acdc`, `261e7b7`, `dae575a`, `c94c19b`, `cc24e33`, `fcf20e6`, `1477b80`, `5eaa529`, `6013f0a`, `50dc07c`, `e89324f`, `fca6632`, `245eb09`, `7d92a22`, `69134a7`, `5d49a2c`, `5925f6f`, `2514ef7`, `02a3e8d`, `08b7994`, `b812e36`, `f4b6f09`, `5186f80`, `f0ef24e`, `8e3759a`, `72a487f`, `4c52c11`, `0fc3444`, `1aceae9`, `71b5f9c`, `fc5edaa`, `26aca12`, `f1b8745`, `d50a4b6`, `0a5ca32`, `c3fdcd2`, `c616150`, `1f64ae6`, `436104b`, `4abbbe5`, `8a62c98`, `d543833`, `202844d`, `859af18`, `9fb7f63`, `53fa040`, `d888c4c`, `4f113ac`, `85e6ef9`, `3f91c5d`, `e7fdc0f`, `d6caab4`, `fe5b0df`, `6e273f3`, and `44a5bc3`. This run adds `d971284`, which keeps the diagnostics guide compact and pushes the full evidence dump back behind the separate `Review diagnostics snapshot` action. Later follow-up repo fixes `ad84902`, `4cae9ea`, `7d893ec`, and `55f627b` keep the first review-first candidate and the live managed-fleet context visible all the way through shortlist, full-list, candidate-review, preset, save, and post-save success feedback, while `44a5bc3` keeps that first review-first warning hint visible on the top fleet surfaces too. This run adds follow-up repo fix `ea7e56b`, which keeps compact planned-action and blocker reasons visible directly in managed-device review rows on the native device-page path.
- **what changed:** repo-side UI cleanup promotes the managed-fleet workspace sensors out of `EntityCategory.DIAGNOSTIC`, the fleet-console button is no longer diagnostic-only, the shipped English translation now includes the managed/unmanaged snapshots and explicit promotion-path wording added to the source strings, `tests/test_translation_sync.py` now fails if `translations/en.json` drifts from `strings.json`, the Configure Managed Devices screen, fleet console, and managed-device workspace sensors now share one ranked unmanaged-candidate order so the top candidate and shortlist no longer default to alphabetical entity order, the Managed Devices Configure, fleet-review, edit, and remove screens now overlay live runtime `device_details` so usable counts, enabled-state ordering, and per-device labels reflect actual runtime readiness/status instead of always reporting config-only `usable: 0`, the native fleet workspace sorts managed-device selectors with enabled-and-usable devices first while keeping blocked/disabled devices lower in the list and adding usable counts to the top fleet summary line, the device page now exposes a first-class `Show managed-device review` action plus diagnostics metadata that advertises that deeper review entry point, managed-device save flows now land on a native success notification with the next Managed Devices or deeper review path, the managed-device review now includes an unmanaged-candidate snapshot plus top promotion targets, the structured command-center control board now stays stable when runtime fields are absent while shipping the matching English command-center copy from `strings.json`, unmanaged-candidate fit and warning signals are aligned behind one shared assessment helper, Configure candidate snapshots plus the full unmanaged pick list now show likely usefulness and the top warning inline, the fleet-console device-page action now matches that native fleet workspace, `b6ddcd2` keeps `Show command center guide` in the main device surface instead of diagnostics-only categorization, `23f2e9c` makes the Configure Managed Devices fleet rows more operational by surfacing per-device guard state, planned action, and last action status directly in the managed-device labels while the fleet summary now calls out blocked devices and active planned actions at a glance, the candidate vetting screen now uses a balanced native review that explicitly calls out control suitability, safety/confidence, and operational value before promotion, the command center now has a concise top-level alert summary plus shared device-path command-center/support text so source blockers, managed-device repair needs, and runtime-readiness issues show up globally before the local section detail, install-provenance refresh/version-mismatch blockers now land in that same top-level alert lane so the command center recommends Diagnostics and the exact-build repair step instead of burying install trust only in secondary support text, this run further hardens candidate discovery so internal `zero_net_export_*` control entities are never offered as unmanaged promotion targets while media-feature toggles like speech enhancement, subwoofer, bass, treble, balance, audio-delay, and `_none` service wrappers are pushed below more appliance-like outlets, heaters, chargers, plugs, and relays in the native promotion shortlist, `a67acdc` now adds per-device managed review buttons so the native device page offers a direct deep-review action for each configured load instead of forcing operators back through the shared fleet review alone, this run also makes Configure -> Sensors prefill empty required separate-grid mappings when live scoring finds a clear best candidate so source repair starts from likely solar/grid entities instead of all-empty required selectors, this run also replaces the remaining operator-facing fallback `Open Configure` / `Configure flow` wording in command-center, Configure, and Repairs fallbacks with the exact native Home Assistant Configure and Diagnostics paths, this run also tightens the add-integration bootstrap copy so the first native handoff now names `Settings -> Devices & Services -> Integrations -> Zero Net Export -> Configure` instead of vague Configure-flow wording, this run further strengthens the primary device-page managed summary row so each load now advertises its kind, variable target power when present, and non-nominal guard/last-action signals directly in the main summary instead of pushing operators into diagnostics to understand the current managed-device posture, this run separates the unmanaged candidate overview from the shortlist so operators now get a distinct candidate-mix snapshot with fixed versus variable counts and the top promotion target instead of two duplicate name-summary sensors, this run now drops clearly non-load variable settings and telemetry numbers from candidate discovery before ranking so Managed Devices no longer suggests room-temperature limits, battery-capacity controls, dynamic-price fee/VAT fields, auto-relock timers, or audio gain sliders as promotion targets, this run keeps the managed-fleet overview itself from collapsing into a blind `No managed devices yet` dead end by surfacing unmanaged count plus the top promotion target when the fleet is empty, this run makes the same native fleet surface more actionable after devices already exist by keeping the top unmanaged promotion target visible in `managed_fleet_overview` and naming the first blocked device or active managed plan directly in `fleet_console_next_step` instead of generic next-step wording, this run advances the same fleet surface again by making `managed_fleet_overview` itself surface `repair sources first` whenever blocking mapped-source issues are active while its attributes now expose `top_candidate_name`, `source_blocked`, `source_blocker_summary`, and the native Sensors repair path for local fleet-context troubleshooting, this run now gates `device_next_step` behind install-provenance and source-repair blockers so command-center attributes plus managed-device workspace handoffs stop pointing operators into Managed Devices while Sensors or Diagnostics still own the real next action, `cc24e33` now keeps that same fleet overview decision-first even after devices already exist by surfacing source-repair urgency plus the unmanaged promotion backlog before lower-priority composition rollups like fixed/variable mix or nominal watts, this run now applies that same global blocker gating inside the Configure -> Managed Devices, edit, and remove forms so their `Recommended fleet action` text no longer contradicts the command center by pushing promotion while Sensors, Controls, or Diagnostics still own the true next step, and `7d92a22` now surfaces that blocker-first ownership explicitly near the top of the Managed Devices Configure, enablement-review, edit, and remove forms so operators see the higher-priority Sensors/Controls/Diagnostics handoff before touching fleet actions. This run also extends the command-center Fleet activity block in `custom_components/zero_net_export/native_support.py` so it carries unmanaged candidate backlog, top promotion target, and blocked planned-action count directly in the opening operator console, `1477b80` then makes both the native fleet-overview sensor and the opening Fleet activity block name the first blocked or actively planned managed device so operators can see which load needs attention without drilling into deeper review first, this run hardens that same Fleet activity block again so an unusable managed device still shows up as blocked even when no blocked plan count is active yet, and this run now flips the Configure Managed Devices sort order in `custom_components/zero_net_export/config_flow.py` so blocked managed devices surface before healthy planned or ready rows in the fleet summary plus the enablement, edit, and remove selectors. `2514ef7` then carries live `current_power_w`, variable `current_target_power_w`, and `last_action_status` through the config-flow runtime overlay so the Configure -> Managed Devices labels and fleet summary stay aligned with the richer device-page managed review rows instead of dropping those runtime signals. `02a3e8d` then trims unmanaged discovery noise again by excluding generic `light.*` fixtures from automatic promotion discovery unless they already look like real load names, and by excluding non-load variable helpers such as buy/sell price, tax-percent, and surround-level controls, so Managed Devices rankings and summary counts stay closer to appliance-like controllable loads. `08b7994` advances that filter again by excluding obvious service/media switch toggles when they do not also look like real discretionary loads, and by dropping audio balance/bass/treble number controls from promotion discovery, so current live-state corpus replay now falls from 71 surfaced unmanaged candidates to 18 appliance-like candidates. `b812e36` then excludes switch-exposed lighting circuits such as roof-light and lamp controls too, so the same live-state corpus replay drops again from 18 to 17 candidates while keeping the shortlist centered on outlets, chargers, heated floors, towel rails, and other more plausible controllable loads. `f4b6f09` then excludes raw Shelly Pro EM meter-switch identifiers from unmanaged discovery as well, so the same live-state corpus replay now drops from 17 to 16 candidates and stops surfacing `switch.shellyproem50_a0dd6ca0970c_switch_0` as if it were a managed-load promotion target. This run then hardens the same discovery path again by fully excluding `_none` media-wrapper switches from unmanaged promotion discovery, so speaker power wrappers stop surviving the shortlist as fake `Room Power` loads; `5186f80` is the repo commit carrying that last exclusion. `f0ef24e` now adds a mild ranking penalty for vague room/circuit `* Power` labels that do not also look like explicit appliance loads, and this run further boosts explicit appliance-style friendly names such as coffee machines, heated floors, towel rails, and purifiers so they surface ahead of generic room-power wrappers without hiding those generic candidates entirely. `72a487f` fixes the remaining command-center-guide ownership drift in `custom_components/zero_net_export/native_support.py`, so the native guide now points operators back to Configure -> Managed Devices as the primary fleet workspace and keeps the device page framed as the deeper per-device review path instead of incorrectly reversing those roles. `4c52c11` then tightens the first shortlist/full-list promotion steps, so both candidate-pick forms now repeat the blocker-first fleet summary, the managed snapshot, the unmanaged snapshot, and the exact Configure plus deeper review paths before the operator chooses a candidate. `0fc3444` then tightens Configure bucket-ownership copy again so the command center, Sensors, Managed Devices, Controls, and Diagnostics screens each restate their owned bucket explicitly. `1aceae9` extends that same managed-vs-unmanaged continuity into the existing-fleet subflows, `71b5f9c` carries it through candidate review, preset selection, and the final add/save form, and `fc5edaa` strengthens the shortlist and full candidate-pick screens so both entry paths restate the managed-top and unmanaged-bottom split, candidate-mix counts, and top unmanaged target before selection. This run advances the same four-bucket cleanup again by renaming the Zero Net Export device-page actions from helper-style support labels to `Review diagnostics` and `Review diagnostics snapshot`, while tightening the Diagnostics bucket copy so it reads as blocker-first triage and install-validation guidance instead of a mixed narrative dump. This run then aligns the diagnostics snapshot payload itself with that same bucket naming, so the snapshot body now opens with `Zero Net Export diagnostics snapshot` instead of the older support-snapshot heading. This watchdog run also removes one last stale device-page path leak by changing `DETAILED_MANAGEMENT_PATH` from `native support actions` to `native diagnostics actions`, so the shared deeper-review handoff stops contradicting the renamed Diagnostics bucket and actions. This run also tightens the remaining deeper-review handoff copy across Configure -> Managed Devices, promotion/save forms, and managed-device success notifications so the device page now reads clearly as the secondary per-device review path only after the main fleet step is clear. This run then removes the last stale `support actions` wording still leaking through native operator-ready and Diagnostics follow-through copy, so Configure, the command-center summary, and the operator-readiness path now consistently point to device-page Diagnostics actions instead of the retired support bucket wording. This run also upgrades the managed-device save notification itself so post-save success now repeats the managed snapshot, the unmanaged snapshot, and any higher-priority Sensors or Diagnostics blocker before telling the operator what to do next, keeping the promotion success landing aligned with the blocker-first native command-center model instead of dropping back to a blind fleet-only handoff. This watchdog run then realigns `README.md` and `docs/VALIDATION_CHECKLIST.md` to the shipped Diagnostics bucket wording, so the repo docs stop telling operators to look for the retired `Show support center` / `Show native diagnostics snapshot` labels after the device-page actions were renamed. A follow-on watchdog pass in this run also fixes the remaining README/validation wording leak where those docs were still telling operators to use the retired Configure bucket label `Health, support, and troubleshooting` instead of Configure Diagnostics plus the device-page `Review diagnostics` / `Review diagnostics snapshot` actions. This run also realigns the optional Lovelace debug scaffold and its setup doc to the same shipped Diagnostics naming and entity ids, so that secondary native-HA-only example no longer points operators at retired support labels or missing button entities. This run also tightens the Configure IA copy again by replacing the Managed Devices screen's negative `Not here` list with an explicit `Bucket ownership` block, and by reshaping the Diagnostics Configure screen into `Diagnostics now`, `Mapped-source triage`, `Install validation`, `Live hints`, and `Bucket ownership and paths` so Sensors, Controls, Managed Devices, and Diagnostics read more like distinct native homes instead of helper prose. A follow-on repo inspection in this run found one remaining subflow continuity gap, where the main Managed Devices screen now surfaced blocker-first `Recommended next fleet action` guidance but the shortlist, full-list, vetting, preset, add/save, and enablement forms did not yet repeat that same next-step handoff near the top of each form. This run then advances the same Managed Devices polish again by replacing the remaining raw `name (entity_id, kind)` top-candidate summaries in the Configure subflows and per-device review notification with the same concise candidate preview shape used elsewhere, so the native fleet workflow now shows kind, likely usefulness, and the key warning without leaking raw entity ids into the main operator copy. This run also found the post-save managed-device success notification and `Recommended next fleet action` copy still leaking raw entity ids such as `switch.pool_pump` and `switch.ac_outlet_2` into the main operator wording even though the rest of the promotion flow had already moved to concise candidate previews. This run also removes the same raw entity-id leakage from the device-page Managed Devices workspace and review notifications, so their promotion handoff and top-candidate summaries now stay aligned with the concise candidate-preview wording already used across the Configure flow. A follow-on repo audit in this watchdog run found that the per-row unmanaged shortlist lines inside those same two device-page notifications were still inheriting `build_candidate_preview(...)`'s raw entity-id default even after the broader cleanup had landed, and this run removes that last device-page candidate-preview leak too. This run then removes the remaining raw entity-id leakage from the main Configure Managed Devices candidate snapshot, fixed/variable candidate summaries, shortlist labels, and shortlist review copy too, so the primary native fleet workspace now stays on concise candidate previews instead of falling back to `switch.*` or `number.*` ids during the first promotion steps. This run also removes one last raw entity-id leak from the native Managed Devices surface by making `sensor.zero_net_export_top_unmanaged_candidate` publish the concise candidate preview without the raw `switch.*` or `number.*` id in its main state. This run then removes the matching raw-id leak from the per-device `Review {device}` notification too, so the detailed managed-device review now keeps its main copy on the friendly device name and `this device's sensors` wording instead of surfacing a separate `Entity: switch.*` line in operator-facing text. This run then tightens unmanaged candidate discovery again by excluding dishwasher child-lock and eco-dry switches plus delayed-start timer controls from promotion discovery, so the live replayed shortlist keeps the real `Dishwasher Power` load while dropping those non-load appliance feature settings from the native Managed Devices workflow. This run also removes the same raw managed `entity_id` leak from the main Configure -> Managed Devices fleet summary rows, so the primary native fleet workspace now stays on the device name plus kind, priority, nominal power, and live runtime posture instead of ending each managed row with `switch.*` or `number.*`. This run also carries friendly names into source freshness diagnostics, humanizes remaining source-id fallbacks, and rewrites mapped-source attention summaries plus Repairs/support wording to say `Affected mapped sources` while keeping raw `sensor.*` ids out of the main native blocker copy. This run also makes operator overrides visible in the main Managed Devices row summaries across Configure, the device-page managed summary sensor, and the workspace/review notification rows, so priority or enablement overrides stop hiding behind deeper per-device detail. A follow-on repo inspection in this run then found those same newly surfaced override details were still rendered as raw `priority_override=...` / `enabled_override=...` debug-style tokens in Configure -> Managed Devices and the device-page review rows instead of matching the operator-facing wording used elsewhere on the native fleet surfaces. The latest repo fixes then close that wording gap too: `859af18` keeps operator overrides visible in the main Managed Devices summaries, and `9fb7f63` rewrites those override fragments into the same concise operator-facing `priority override` / `enabled override` copy used elsewhere instead of raw debug tokens. This run then fixes one more operator-facing fleet-summary wording ambiguity by labeling unmanaged backlog counts as `fixed candidates` / `variable candidates` and managed composition counts as `fixed managed` / `variable managed`, so the main native fleet overview no longer reuses the same `1 fixed` shorthand for two different meanings in one state string. `cfa0c54` then tightens the native shortlist sensors themselves so `candidate_shortlist` carries each surfaced candidate's kind plus usefulness hint, while `candidate_shortlist_fit` now includes compact warning-aware fit guidance instead of a bare confidence dump. This run hardens the shared unmanaged-review path again by capping helper-backed variable controls such as heated-floor `input_number` helpers at review-first confidence even when their names look appliance-like, so helper targets stop surfacing as `strong match` promotions across candidate preview, review, and fleet-summary surfaces. This run also tightens the native shortlist rows again so `candidate_shortlist` now carries each surfaced candidate's compact key-warning hint inline, keeping the shortlist closer to the `likely usefulness plus key warning` unmanaged-row shape called for by the `0.1.87` UI design instead of splitting that warning context into a separate sensor alone. `2fa19bc` then clarifies shared candidate wording one more step so medium-confidence candidates with active warnings render as explicit review-first options instead of reading like fully credible promotion targets across fleet summaries and managed-device handoffs. This run then removes one more promotion-path duplication leak by dropping helper-backed unmanaged candidates when a stronger native switch/light/number candidate already carries the same load label, so Managed Devices no longer offers both `Lounge Room - Heated Floor` and the shadow `input_number.lounge_room_heated_floor` helper as separate top promotion choices for the same load. This run also neutralizes the last shared candidate usefulness summaries that still described switch and number targets as `strong` candidates, keeping the unmanaged review copy aligned with the `0.1.87` design's review-first posture for ambiguous promotion hints. This run also realigns the final managed-device add/edit save form with that same wording by switching its `Candidate fit` label to `Candidate usefulness` and by surfacing the candidate's usefulness text instead of raw confidence values in the save-step placeholders. This run then closes one more Configure continuity gap by making the shared unmanaged snapshot used across shortlist, vetting, add/save, and fleet-edit follow-through carry `needs review` backlog count plus the top candidate warning whenever the leading unmanaged target is still review-first, so those native subflows keep the same promotion-risk signal already visible in the richer fleet summaries. `d888c4c` carries that unmanaged review context through the config flow, and `4f113ac` fixes the remaining shared-snapshot counting bug so low-confidence `review carefully` candidates now count toward `needs review` even when the top unmanaged candidate itself looks likely useful. This run also strengthens the opening command-center Fleet activity block so it now carries the same managed mix and controllable nominal-power snapshot already visible in Configure subflows and device-page managed review, keeping the top operator board aligned with the richer native Managed Devices workspace instead of dropping back to a thinner `managed/enabled/usable` summary. This run also keeps the richer unmanaged backlog preview visible after operators leave the main Managed Devices screen by carrying the same candidate snapshot through fleet enablement, edit, remove, shortlist, full-list, vetting, preset, and add/save forms instead of collapsing those native subflows back to counts plus a single top-candidate line. This run also tightens `custom_components/zero_net_export/native_support.py` so the opening command-center `Fleet activity` block now leads with the managed/unmanaged split, candidate-review burden, and top promotion target before falling back to enabled/usable and managed-mix detail, keeping the top operator console aligned with the same fleet snapshot shape already used deeper in Managed Devices. This run then realigns `sensor.zero_net_export_managed_fleet_overview` to the same ordering when managed devices already exist, so the local Managed Devices surface now leads with unmanaged backlog and any source-repair blocker before lower-priority enabled/usable composition detail instead of telling a different story than the opening command center. This run also makes the shared unmanaged snapshots more review-legible by carrying the first review-first candidate's own usefulness hint through `managed_fleet_overview`, the opening Fleet activity block, and the Configure promotion snapshots whenever that review target is different from the top promotion candidate. This run also removes the last `strong fit` / `strong operational value` wording from shared variable-candidate assessment copy in `custom_components/zero_net_export/candidate_utils.py`, keeping the deeper review text aligned with the same review-first posture already used in candidate usefulness summaries. This run also closes one smaller four-bucket wording leak by renaming the primary `managed_fleet_overview` sensor label to `Managed devices overview`, so the native entity surface no longer keeps the older fleet-only label after `fleet_console_next_step` had already moved to the Managed Devices wording. This run then keeps the opening command-center device-status line aligned with that same review-first fleet story by surfacing the first review-first unmanaged candidate alongside the top promotion target whenever those differ, instead of dropping back to only the highest-ranked candidate preview. This run also keeps the same review-first warning context visible inside the shared Configure unmanaged snapshot, so shortlist, vetting, preset, save, and post-save feedback now carry the first review-first candidate's own warning hint when it differs from the top promotion target instead of dropping back to usefulness-only wording.
- **validation status:** repo-side UI changes advanced again in this run. `python3 -m unittest -q tests.test_bucket_ownership_copy tests.test_translation_sync tests.test_command_center_setup_focus tests.test_source_repair_guidance` passed on the updated repo candidate, locking the Diagnostics copy cleanup plus translation sync on top of the earlier UI coverage. `python3 -m unittest -q tests.test_candidate_utils tests.test_sensor_entity_categories tests.test_config_flow_device_runtime_overlay tests.test_button_entity_categories tests.test_command_center_setup_focus tests.test_bucket_ownership_copy tests.test_source_repair_guidance` passed on the updated repo candidate. This run's documented live Home Assistant API corpus replay against current repo candidate discovery now confirms helper-backed variable controls such as `input_number.lounge_room_heated_floor`, `input_number.master_bathroom_heated_floor`, and `input_number.powder_room_heated_floor` stay review-first, the duplicate heated-floor helper is dropped when the stronger `switch.ac_outlet_1` candidate already names the same load, and the replayed unmanaged candidate pool falls from 12 to 11 candidates led by clearer appliance-style loads rather than duplicate helper shadows. The follow-on save-form wording fix in this watchdog run is repo-validated with `python3 -m unittest -q tests.test_config_flow_device_runtime_overlay tests.test_translation_sync`. The config-flow continuity follow-up in `d888c4c` is repo-validated with `python3 -m unittest -q tests.test_config_flow_device_runtime_overlay tests.test_translation_sync`, and this run's `4f113ac` regression fix is repo-validated with `python3 -m unittest -q tests.test_config_flow_device_runtime_overlay tests.test_candidate_utils tests.test_translation_sync`, including new coverage that now counts low-confidence `review carefully` candidates in the shared `needs review` backlog summary instead of understating unmanaged review work inside Configure subflows. This run's command-center fleet-summary follow-up is repo-validated with `python3 -m unittest -q tests.test_command_center_summary tests.test_config_flow_device_runtime_overlay tests.test_button_entity_categories`, including new coverage that locks the opening Fleet activity block to the same `fixed managed` and `W nominal` snapshot detail already used elsewhere on the native managed-device path. This run's managed-workspace continuity follow-up is repo-validated with `python3 -m unittest -q tests.test_config_flow_device_runtime_overlay tests.test_bucket_ownership_copy tests.test_translation_sync`, including new coverage that locks the richer `Candidate snapshot` backlog preview into the Managed Devices subflows so the managed-top and unmanaged-bottom split stays actionable after leaving the main workspace. Live Home Assistant validation is still pending the next exact-build redeploy and review, so this managed-devices shortlist cleanup is repo-validated and corpus-checked but not yet judged in the installed UI. This run's command-center handoff follow-up is repo-validated with `python3 -m unittest -q tests.test_command_center_summary tests.test_command_center_setup_focus tests.test_source_repair_guidance tests.test_config_flow_device_runtime_overlay tests.test_button_entity_categories tests.test_sensor_entity_categories tests.test_translation_sync`, including new coverage that keeps the opening command center on the empty-fleet promotion path when runtime `device_count=0` and caps the top-candidate wording to a compact preview that still fits native summary limits. This run's Diagnostics-path follow-up is repo-validated with `python3 -m unittest -q tests.test_bucket_ownership_copy tests.test_config_flow_device_runtime_overlay tests.test_translation_sync tests.test_command_center_summary tests.test_command_center_setup_focus tests.test_source_repair_guidance tests.test_button_entity_categories`, including new support-form placeholder coverage so the Diagnostics screen keeps the exact native bucket paths wired through the form instead of only in copy. This run's review-first snapshot follow-up is repo-validated with `python3 -m unittest -q tests.test_candidate_utils tests.test_sensor_entity_categories tests.test_config_flow_device_runtime_overlay tests.test_command_center_summary tests.test_button_entity_categories tests.test_translation_sync`, which now locks the first review-first candidate's usefulness hint into the shared Managed Devices and promotion snapshots when the top promotion target itself still looks usable. This run also repo-validates the richer managed-top continuity through the promotion flow with `python3 -m unittest -q tests.test_config_flow_device_runtime_overlay tests.test_translation_sync tests.test_command_center_modal_copy tests.test_source_repair_guidance`, including new placeholder coverage that keeps a live `Current managed fleet` block visible across shortlist, vetting, preset, and save screens instead of dropping promotion into counts-only context. This watchdog run also fixed the stale test-double drift in `tests/test_sensor_issue_count_state_class.py` after `sensor.py` began importing `first_review_candidate`, and `python3 -m unittest -q` now passes again on repo HEAD instead of failing during import-time validation. This run's follow-on device-page review fix is repo-validated with `python3 -m unittest -q tests.test_button_entity_categories tests.test_config_flow_device_runtime_overlay` plus `python3 -m unittest discover -q`, including new coverage that keeps the first review-first candidate's own usefulness and warning guidance visible inside both native device-page Managed Devices notifications and their exported attributes when that review target differs from the top-ranked promotion candidate. The latest repo follow-up is also covered by `python3 -m unittest -q tests.test_button_entity_categories tests.test_config_flow_device_runtime_overlay tests.test_sensor_entity_categories` plus `python3 -m unittest -q`, which now lock the review-first handoff text through Configure, the next-step sensor, and the device-page review surfaces while keeping idle `hold` noise off the top-level managed summaries. This run adds focused repo validation for the post-save success landing follow-through with `python3 -m unittest -q tests.test_config_flow_device_runtime_overlay tests.test_button_entity_categories tests.test_command_center_summary`, including new coverage that keeps promote, remove, and enablement feedback aligned on the first review-first unmanaged candidate instead of a generic next-promotion instruction. This run's Managed Devices label cleanup is repo-validated with `python3 -m unittest -q tests.test_sensor_entity_categories`, including new coverage that locks `managed_fleet_overview` onto the current `Managed devices overview` bucket wording. This run's command-center device-status continuity follow-up is repo-validated with `python3 -m unittest -q tests.test_command_center_summary tests.test_command_center_setup_focus tests.test_source_repair_guidance tests.test_translation_sync` plus `python3 -m unittest discover -q`, including new coverage that now keeps the opening device-status line aligned on the first review-first unmanaged candidate when the top-ranked promotion target differs. This run's Configure unmanaged-snapshot follow-up is repo-validated with `python3 -m unittest -q tests.test_config_flow_device_runtime_overlay tests.test_translation_sync` plus `python3 -m unittest discover -q`, including updated coverage that now keeps the first review-first unmanaged candidate's warning hint visible in the shared subflow snapshot whenever that review target differs from the top-ranked promotion candidate.
- **next action:** keep this bug in `fixed_pending_validation`, continue the next unfinished `0.1.87` UI stages while the exact-build mismatch remains unchanged, and treat the live fingerprint check as an edge-triggered release/deploy blocker rather than the default repeated next-step headline; only return to the formal `0.1.86` release-reconciliation flow when materially new release-boundary evidence exists. Once the exact repo build is installed, rerun fingerprint validation until it reports `overall_match=true`, then re-check the managed/unmanaged workspace, command-center/device-path visibility, promotion-flow continuity across the Managed Devices subflows, and four-bucket UI outcome against the exact installed build.

## ZNE-005 — `build_release_info()` missing required `current_version` argument
- **status:** `validated`
- **severity:** `critical`
- **area:** `runtime`
- **where seen:** live Home Assistant integration page during setup/retry
- **current observed behavior:** historical bug. Earlier live Home Assistant runs showed `Failed setup, will retry: build_release_info() missing 1 required positional argument: 'current_version'`.
- **expected behavior:** Zero Net Export setup should complete without this runtime error
- **evidence:** user screenshot of the Home Assistant integration page showing the exact setup error; repo/HA SSH inspection on 2026-04-15 confirmed the live `0.1.81` install still had `coordinator.py` calling `build_release_info(include_changelog=False)` at line 223 without the required version argument. In this run, documented HA SSH/API access confirmed the live install now has one active `zero_net_export` config entry and 145 live Zero Net Export entities, `scripts/validate_install_fingerprint.py` shows `coordinator.py` matches the repo candidate exactly, and fresh `ha core logs` review no longer shows the earlier `build_release_info()` missing-argument retry.
- **suspected cause:** coordinator release-update metadata called `build_release_info()` without the required `current_version`, causing setup to fail while building validation details
- **repo fix:** `48a9d45` — pass `INTEGRATION_VERSION` from `coordinator.py` into `build_release_info()` and cover the call contract with a coordinator unit test; this run adds `tests/test_release_update_details.py` so the release-update path has its own focused regression coverage for the missing-argument contract
- **validation status:** repo-side fix remains covered by `python3 -m unittest tests.test_source_freshness_probes tests.test_release_info_install_guidance tests.test_release_update_details` plus `python3 -m py_compile custom_components/zero_net_export/coordinator.py`. Live validation is now good enough for this bug specifically: the fixed `coordinator.py` is present in the live install, the integration has an active config entry again, and the earlier missing-argument setup retry is no longer present in current log review.
- **next action:** no further action for this specific bug beyond keeping future coordinator/release-info changes under regression coverage

## ZNE-029 — Fleet workspace candidate sensors redo full candidate discovery on every sensor read
- **status:** `fixed_pending_validation`
- **severity:** `medium`
- **area:** `sensors`
- **where seen:** live Home Assistant logs on the current mixed `0.1.86` install
- **current observed behavior:** Home Assistant logged `Updating state for sensor.zero_net_export_unmanaged_candidate_overview ... took 0.593 seconds`, which means the main Managed Devices candidate surface is still heavy enough to trigger runtime warnings in the live install.
- **expected behavior:** the fleet workspace candidate sensors should reuse one candidate-discovery result per coordinator refresh instead of rescanning the entire HA state registry separately for each related sensor value.
- **evidence:** this run used the documented SSH path in `TOOLS.md` and `ha core logs -n 400 | grep -i "entity.py\|blocking call\|zero_net_export"`, which returned `2026-04-16 17:10:11.612 WARNING ... sensor.zero_net_export_unmanaged_candidate_overview ... took 0.593 seconds`; this run's repo inspection then confirmed `custom_components/zero_net_export/sensor.py` was calling `discover_candidate_devices(...)` independently for `managed_fleet_overview`, `unmanaged_candidate_count`, `unmanaged_candidate_overview`, `top_unmanaged_candidate`, `top_candidate_fit`, `top_candidate_warnings`, `candidate_shortlist`, `candidate_shortlist_fit`, and `fleet_console_next_step`, all against the same coordinator state. This watchdog run found one more repo-side rediscovery leak in the same area: those fleet-workspace sensors were now caching candidate discovery for their main state values, but their `extra_state_attributes` path was still calling `_candidate_devices_for_hass(...)` directly and re-scanning Home Assistant state again instead of reusing the same per-refresh cache.
- **suspected cause:** fleet workspace sensors were each re-running full candidate discovery from `hass.states.async_all()` during the same coordinator update instead of sharing one cached candidate list, and the first repo-side caching pass only covered `native_value` while leaving `extra_state_attributes` on the old direct rediscovery path.
- **repo fix:** `c380a77` cached fleet candidate discovery per coordinator snapshot in `custom_components/zero_net_export/sensor.py`, and `3cb05a0` extends that same fix so the fleet-workspace sensors' `extra_state_attributes` now reuse `_candidate_devices_for_state(...)` instead of triggering a second full discovery pass. `tests/test_sensor_entity_categories.py` now also locks that regression by asserting the managed-fleet overview state plus attributes share one candidate-discovery call per coordinator snapshot.
- **validation status:** repo-side fix verified again in this run with `python3 -m unittest tests.test_sensor_entity_categories -q` and `python3 -m unittest discover -s tests -q`. Live validation is still pending because the optimized sensor code is not yet installed on the live Home Assistant build.
- **next action:** deploy one exact current repo build, rerun fingerprint validation until `overall_match=true`, then confirm the unmanaged-candidate sensors stop emitting slow-update warnings in Home Assistant logs

## ZNE-026 — Integration overview/device page still shows stale version, and the first repo-side fix regressed into blocking I/O
- **status:** `fixed_pending_validation`
- **severity:** `high`
- **area:** `release`
- **where seen:** live Home Assistant UI after `0.1.84`, and again during `0.1.85` live validation
- **current observed behavior:** version fidelity on the HA integration/device surfaces was unstable. Earlier screenshots showed `Version 0.1.83` / `Firmware: 0.1.83` after a `0.1.84` deploy, and the first repo-side fix for this then triggered Home Assistant blocking-I/O warnings from `entity.py` while reading `manifest.json` on the event loop during setup.
- **expected behavior:** the Home Assistant integration and device surfaces should show the currently installed package version without introducing new blocking-call warnings during startup
- **evidence:** user screenshots on 2026-04-16 showed the integration header and device info card rendering `0.1.83` after the `0.1.84` release/deploy. In the last live validation, post-restart HA logs showed `Detected blocking call to read_text ... at custom_components/zero_net_export/entity.py, line 16` and `Detected blocking call to open ...` from `_device_surface_version()`, which had been introduced to fix the stale-version display. Repo inspection in the fix run confirmed `custom_components/zero_net_export/entity.py` was still doing that synchronous manifest read for every entity device-info build. A later repo-wide fingerprint rerun widened the currently shipped drift set beyond this single file, but the documented HA SSH path still shows the same live `entity.py` warning at `2026-04-16 10:03`, and the fingerprint output still includes the older live `entity.py` hash (`2dffb9a4ef0c` remote vs `9b087d27adff` repo) among the remaining mismatches.
- **suspected cause:** the original stale version was partly a real live mixed-build problem, but the repo-side fix duplicated manifest reading inside entity initialization instead of reusing the already-derived packaged integration version, which violates HA async expectations and is not safe to ship as the final solution.
- **repo fix:** this run removes the synchronous `_device_surface_version()` manifest read from `custom_components/zero_net_export/entity.py` and feeds the native device surface directly from `INTEGRATION_VERSION`, which already tracks the packaged component version. `tests/test_device_surface_version.py` now verifies the device surface uses the integration version constant without depending on manifest I/O during entity setup.
- **validation status:** repo-side fix remains verified with `python3 -m unittest tests.test_device_surface_version -q` plus `python3 -m unittest discover -s tests -q`, which passed again in this run. Live Home Assistant validation is still pending because the currently installed fingerprint still includes the older `entity.py` and the old blocking-I/O warning remains visible until the exact current repo candidate is redeployed.
- **next action:** redeploy the exact current repo candidate, rerun fingerprint validation until `overall_match=true`, then confirm the integration/device surfaces still show the installed package version while Home Assistant startup logs no longer emit the `entity.py` blocking-I/O warning

## ZNE-027 — Command center still lets diagnostics/release plumbing dominate the primary operator surface
- **status:** `fixed_pending_validation`
- **severity:** `high`
- **area:** `diagnostics`
- **where seen:** live Home Assistant command-center modal after `0.1.84` deploy
- **current observed behavior:** the live `0.1.84` command-center modal still shows dense release-fingerprint/install-validation/debug text and long repair-path prose high in the modal, so diagnostics/support detail is visually carrying too much of the primary operator experience.
- **expected behavior:** per `docs/UI_DESIGN.md`, Configure should remain the primary operator workspace with the decision summary, grouped control board, and clear jump-off entries, while Diagnostics stays visible but secondary rather than dominating the landing surface.
- **evidence:** user screenshot on 2026-04-16 shows the modal headed `Zero Net Export command center` with install-fingerprint details and long repair-path prose before the main setup/operator sections, which matched the repo strings that embedded an `Installed package details:` block directly in the primary command-center description. Later repo work removed that diagnostics-heavy lead, but this run's repo inspection found the shared device-page `Show command center guide` builder in `custom_components/zero_net_export/native_support.py` had still drifted behind the shipped Configure wording: it kept the older `Headline` / `Top alert` prose, still showed the stale `Not here` tail section, and omitted the newer `Now` plus `Bucket ownership` structure already present in `strings.json`. That meant the Zero Net Export device-page command-center handoff was still less productized than the Configure landing even in repo state.
- **suspected cause:** the options-flow copy and the shared device-page guide text were being tightened through different paths, so the Python-built command-center guide in `native_support.py` drifted behind the translated Configure copy and kept older setup/ownership wording longer than intended.
- **repo fix:** `d50a4b6` — update `custom_components/zero_net_export/native_support.py` so the shared device-page `Show command center guide` now matches the shipped setup-first operator-board hierarchy: `Now`, the grouped control board, a fuller setup check including source-map and repair-path context, and the same `Bucket ownership` section used by the Configure copy, while dropping the older `Not here` tail section. Focused regression coverage in `tests/test_command_center_setup_focus.py` and `tests/test_source_repair_guidance.py` now locks that guide text to the same setup-first structure already asserted for `strings.json` in `tests/test_command_center_modal_copy.py`.
- **validation status:** repo-side command-center tightening advanced again in this run. `python3 -m unittest tests.test_command_center_setup_focus tests.test_source_repair_guidance tests.test_command_center_modal_copy -q` passed after the device-page guide text was realigned to the shipped `Now` / `Structured control board` / `Setup check` / `Basic setup paths` / `Bucket ownership` hierarchy, and `python3 -m unittest discover -s tests -q` also passed on the current repo candidate. This watchdog run refreshed the bug state to include the landed `d50a4b6` fix so the repo-side source of truth no longer trails current HEAD while live Home Assistant validation remains pending on the next exact-build redeploy.
- **next action:** redeploy the current repo candidate, then confirm the command-center modal now reads as a setup-first operator surface with diagnostics/install-validation detail demoted behind the Diagnostics path instead of leading the modal

## ZNE-028 — Device page still lacks the managed-device structure required by the UI design
- **status:** `fixed_pending_validation`
- **severity:** `high`
- **area:** `managed_devices`
- **where seen:** live Home Assistant device page after `0.1.84` deploy
- **current observed behavior:** the device page is still mostly generic cards plus buttons/sensors, with too little managed-device structure visible in the deeper native review path. The current bug is no longer that the device page fails to become the primary fleet workspace; it is that the deeper review path still does not feel rich or legible enough in support of Configure -> Managed Devices.
- **expected behavior:** per `docs/UI_DESIGN.md` and `docs/UI_IMPLEMENTATION_MAP.md`, Configure -> Managed Devices should remain the primary fleet workspace, while the native device page should act as a clearly secondary but still first-class deeper review path with stronger managed-device structure, better per-device operational detail, and less generic helper-card feel.
- **evidence:** user screenshot on 2026-04-16 shows the device page still dominated by generic Controls/Sensors/Activity cards and button entities like `Show fleet console` / `Show managed-device review`, while the user explicitly stated that managed devices should be on the device info page and that the page has not really progressed enough toward the design. Earlier repo inspection confirmed the per-device surface was still exposing too many deep runtime sensors as primary entities, which diluted the managed-device workspace signal on the native device page. In a later repo pass, those secondary per-device status, guard, planned-action, and planned-delta entities had already been pushed into diagnostics, but this run's repo inspection still found the surviving primary `managed summary` row too sparse because it omitted the load kind and, for variable devices, hid target/guard pressure behind diagnostics or the deeper review button. This run also confirmed the remaining device-page action labels and diagnostics metadata were still leaning on helper-ish `fleet console` / `managed review` wording instead of the stronger Managed Devices workspace and review-first wording the native UI design calls for. Repo inspection in this run then found a follow-on device-page contradiction: the `Review managed devices workspace` notification still printed a promotion handoff even while the shared command-center recommendation was telling operators to repair Sensors/source blockers first, and the same contradiction still leaked through the companion `Review managed devices` notification, which weakened the global-plus-local blocker model right on the native device page. A later repo pass in this run also showed those same device-page review surfaces still reduced blocked and active-plan posture to anonymous counts in the top snapshot line, forcing operators to read deeper rows before they could tell which managed load currently needed attention. This run then found another device-page review regression in repo state: managed-device rows with an empty or missing `planned_action` were still being counted as active plans inside the workspace/review snapshot strings and button attributes, which could falsely surface `plan ...` and `planned action(s)` hints even when no plan existed. Repo inspection in this run then found the ordered device-page workspace and review lists still surfaced healthy planned loads before blocked managed devices, so the local managed-device review could bury the first exception even though the snapshot already named it. A follow-on repo pass in this run found one more device-page legibility gap: the managed-device workspace and review rows were still omitting load kind, priority, and current power, and variable-device rows still hid their current target power, so the deeper native review path was not yet showing the operational row detail the UI design expects. This run's repo inspection found one smaller but still visible detail-review leak in the same path: fixed-load `Review {device}` output was still printing variable-only `Requested target power`, `Variable range`, and `Step size` lines as `n/a`, while operator override lines still fell back to raw values instead of concise operator-facing wording. This watchdog run found the next Workstream E gap one level up in the same device-page review path: `Review managed devices` was still rendering the same compact row shape as the top workspace, so the supposed deeper audit view was not yet visibly richer than `Review managed devices workspace` even when last-action timing and nominal-load context were available in coordinator state.
- **suspected cause:** managed-device UX on the device page remains implemented mainly as button-triggered review flows and supporting entities rather than as a clearly shaped secondary review surface aligned with the current design. The per-device entity mix was also too flat, leaving secondary runtime status/action detail to compete with the main managed-device summary instead of staying secondary, and the surviving primary summary row itself was still too skeletal to carry the deeper-review legibility burden the device page now owns. The detailed review payload was also still inheriting some generic profile fields without checking device kind, which left fixed loads showing variable-only `n/a` lines and raw operator-override values in the middle of an otherwise operator-facing review.
- **repo fix:** earlier work updated `custom_components/zero_net_export/sensor.py` so every configured load now exposes a primary `managed summary` sensor on the Zero Net Export device page, while deeper per-device runtime history sensors such as last-requested power, last-applied power, durations, timestamps, and last-action detail move into diagnostic categorization. This same bug fix path later renamed the per-device review buttons in `custom_components/zero_net_export/button.py` to explicit review actions, and a later pass tightened the device-page surface again by moving per-device status, guard status, planned action, and planned power delta sensors into `EntityCategory.DIAGNOSTIC`, leaving the primary device page focused on each load's managed summary plus live power/target entities. `56a1278` then advanced that cleanup again by moving redundant runtime-health, source-stale, and per-device usable binary sensors into diagnostic categorization in `custom_components/zero_net_export/binary_sensor.py`, so the primary device page keeps the stronger managed-device summary/action rows while the boolean troubleshooting detail stays secondary. `eb42c9b` further strengthened the native device-page posture by replacing the remaining helper-ish `fleet console` / `managed review` labels in `custom_components/zero_net_export/button.py` and `custom_components/zero_net_export/diagnostics.py` with review-first Managed Devices workspace wording, `5eaa529` then made that wording workspace-first in the live-facing action labels and notification titles so the main device-page actions now read as `Review managed devices workspace`, `Review managed devices`, and `Review {device}` instead of internal helper jargon, `6013f0a` hardened the same device-page workspace notification so it stops printing a candidate-promotion handoff when the shared command-center recommendation still says Sensors/source repair comes first, `50dc07c` extends that blocker-gated handoff to the companion `Review managed devices` notification plus attributes so both top-level managed-device review surfaces now stay aligned with the command-center blocker model, and `e89324f` now makes both top-level managed-device notifications surface a blocker-first section near the top whenever Sensors/Controls/Diagnostics still own the real next step instead of leaving the source-repair context buried only in trailing handoff lines. This run advances those same device-page review surfaces again by normalizing empty `planned_action` values in `custom_components/zero_net_export/button.py` so the workspace/review snapshot only names genuine active plans, while the exported button attributes now stop advertising fake `plan ...` targets or non-zero planned-action counts for devices that are merely idle. The corresponding button attributes now also expose `recommended_section`, `recommended_path`, `recommended_reason`, `blocker_first`, `managed_snapshot`, `first_blocked_device`, and `first_planned_device` so the local device-page context stays aligned with the shared command-center recommendation while staying glanceable. `c93b709` then updates the same `button.py` ordering so blocked managed devices surface before healthy planned loads in the workspace and review lists, keeping the first local exception visible instead of burying it below nominal rows. This run extends those same managed-device review rows in `custom_components/zero_net_export/button.py` so the device-page workspace and review notifications now show each load's kind, priority, current power, and variable target power inline instead of leaving that context implicit or hidden behind deeper per-device sensors. This run then keeps the per-device `Review {device}` action aligned with the main Managed Devices workspace by carrying blocker-first guidance, managed/unmanaged snapshot context, and the top unmanaged promotion target into the detailed device review instead of dropping operators into an isolated per-device note. `73dc3b7` then tightens that same per-device detail review again by hiding variable-only `n/a` rows for fixed loads and rewriting operator overrides into concise `forcing on/off` and `forcing <priority>` wording so the deeper device-page review reads like an operator surface instead of a debug dump. This run extends that same deeper review payload again in `custom_components/zero_net_export/button.py` by adding live active-runtime, daily-runtime, last-action timestamp, and last-action age lines so the device-page audit path shows richer execution history instead of only static profile fields plus raw result text. This run then trims another smaller managed-row noise leak by hiding idle `action hold` text in the primary Configure managed-device rows and the primary device-page `managed summary` sensor, so only genuine active plans compete for attention on the top-level fleet surfaces while deeper review can still carry hold detail when needed. `75277cb` carries that top-surface noise cleanup in repo state. This run advances the same device-page audit path again by adding active-runtime snapshots directly into the workspace/review row summaries and by surfacing `Last applied at` in the per-device `Review {device}` detail payload, so the secondary native review path reads more like an audit surface instead of forcing operators to infer recent execution history from counts alone. This run then keeps that deeper per-device review aligned with the stronger review-first promotion model too: `custom_components/zero_net_export/button.py` now carries the first review-first unmanaged candidate plus its usefulness and warning summary into `Review {device}` whenever that review target differs from the top-ranked promotion candidate, so the device-page audit path no longer drops the real next vetting target while operators inspect an individual managed load. Follow-on repo work then kept the same review-first continuity intact across the wider device-page handoff path: `522dd18` aligns the Managed Devices workspace and review handoffs on the first review-first unmanaged candidate instead of always using the top-ranked promotion target, `139b2ee` carries that review-first candidate's own usefulness hint into the same device-page surfaces, `e8cf521` keeps the post-save Managed Devices success handoff review-first too, and `610cb5e` closes the remaining per-device review drift so the device-page audit path itself stays aligned with that same first review-first unmanaged candidate. This run adds `c5f5b6c`, which makes the deeper `Review managed devices` rows visibly richer than the top workspace by carrying nominal-load context plus last-action timing directly in the review-row summaries while keeping the workspace rows more compact.
- **validation status:** repo-side fix advanced again in this run and is now covered by `python3 -m unittest tests.test_button_entity_categories -q`, which passed again after adding managed-snapshot regression coverage for both top-level device-page review actions plus their exported `managed_snapshot` / `first_blocked_device` / `first_planned_device` attributes, including the new empty-plan regression case that keeps idle devices out of the active-plan snapshot, the newer ordering regression that keeps blocked devices ahead of healthy planned loads in the rendered workspace/review lists, and this run's new row-detail regression that keeps kind, priority, current power, and variable target power visible in the managed-device review lines. This run extends that same coverage one step deeper by asserting the per-device review hides fixed-load-only `Variable range`, `Step size`, and empty requested-target rows while rendering operator overrides in the new concise wording. This run adds another audit-detail regression check so the per-device review also keeps active runtime, daily runtime, last-action timestamp, and last-action age visible in the detailed review payload. Follow-on repo passes in `14144d9` and `3b4fa6b` harden that same deeper-review audit detail, and `python3 -m unittest tests.test_button_entity_categories -q` still passes on the current repo candidate. This run extends that same button coverage again with a dedicated per-device review-first regression case, so the detailed device-page review now keeps the top unmanaged candidate and the first review-first unmanaged candidate distinct whenever those are different devices. The same regression coverage now also includes the updated `tests/test_config_flow_device_runtime_overlay.py` ordering assertions so the Configure Managed Devices fleet summary and selectors keep blocked devices ahead of healthy planned or ready rows. This run extends that top-level managed-row coverage again with `python3 -m unittest -q tests.test_config_flow_device_runtime_overlay tests.test_sensor_entity_categories`, which now locks the idle-plan cleanup into both Configure -> Managed Devices and the primary device-page managed summary so `hold` no longer shows up as a fake active plan on the top fleet surfaces. Repo HEAD still validates cleanly in this run with `python3 -m unittest -q`, so the bug tracker now matches the current repo-side cleanup state instead of stopping before the latest top-surface fix. This run adds focused coverage for the richer deeper-review rows with `python3 -m unittest -q tests.test_button_entity_categories tests.test_config_flow_device_runtime_overlay tests.test_command_center_summary tests.test_translation_sync`, which now locks nominal-load context and last-action timing into `Review managed devices` without changing the more compact workspace-row shape. This run also rechecked the documented HA SSH fingerprint path against repo commit `610cb5e`, and it is still the same unchanged ten-file live mismatch (`__init__.py`, `button.py`, `candidate_utils.py`, `config_flow.py`, `coordinator.py`, `diagnostics.py`, `native_support.py`, `sensor.py`, `strings.json`, `translations/en.json`) with `overall_match=false`. That keeps the newer review-first device-page fixes repo-valid but still undeployed, so repeating live validation as the main next step would still be release-gate churn rather than new product evidence.
- **next action:** redeploy the current repo candidate, then confirm the Zero Net Export device page now acts as the deeper review path that supports Configure -> Managed Devices by centering each configured load on the managed summary row plus live power/target entities while the secondary status/guard/plan/detail booleans sit under diagnostics instead of crowding the primary device surface

## ZNE-040 — Runtime attention notification is overloaded and poorly formatted
- **status:** `fixed_pending_validation`
- **severity:** `medium`
- **area:** `ui`
- **where seen:** live Home Assistant runtime attention notification/modal on 2026-04-16
- **current observed behavior:** the `Runtime attention needed` surface is a dense wall of text with weak visual hierarchy. The headings are not visually separated enough, spacing is poor, and the content is too long and repetitive, making the error harder to act on quickly.
- **expected behavior:** the runtime attention surface should use tighter content, clearer grouping, and stronger heading emphasis so the operator can scan the problem, cause, and next step quickly
- **evidence:** user screenshot on 2026-04-16 shows the runtime attention modal with long repeated validation details, weak spacing, and overloaded instructions. User explicitly requested bolded headings, better spacing, and tighter content.
- **suspected cause:** the current runtime/support notification text is generated as a long plain-text dump of support fields rather than being edited into a compact operator-facing alert format
- **repo fix:** this run tightens the Repairs/runtime-attention notification copy in `custom_components/zero_net_export/strings.json` and `custom_components/zero_net_export/translations/en.json`, replacing the long paragraph-style dump with short `Now`, `Mapped-source blockers`, `Do next`, and `Open` sections so the primary signal and repair path scan cleanly in Home Assistant's native modal. `a42b785` then splits the selector fallback out of `Do next` into its own `Fallback, only if Home Assistant rejects a valid choice` section so the repair path reads in the intended priority order.
- **validation status:** repo-side copy fix implemented and advanced again in this run. `tests/test_repairs_copy.py` now asserts the separate `Fallback` section instead of the older inline fallback bullet, and `python3 -m unittest tests.test_repairs_copy tests.test_translation_sync -q` plus `python3 -m unittest discover -s tests -q` passed. Live Home Assistant validation is still pending on the next exact-build redeploy.
- **next action:** redeploy the current repo candidate, then confirm the `Runtime attention needed` modal now reads as a compact sectioned alert instead of a wall of text

## ZNE-030 — Setup-finished/setup-warning notification is also overloaded and poorly formatted
- **status:** `fixed_pending_validation`
- **severity:** `medium`
- **area:** `ui`
- **where seen:** live Home Assistant `Finish native Zero Net Export setup` notification/modal on 2026-04-16
- **current observed behavior:** the setup notification is still a dense block of text with weak visual hierarchy, long repeated prose, and too much instruction text packed into a single alert surface
- **expected behavior:** the setup notification should be short, structured, and easy to scan, with stronger heading separation and a tighter summary/next-step layout
- **evidence:** user screenshot on 2026-04-16 shows the `Finish native Zero Net Export setup` warning with long repeated setup/selector-workaround prose. User explicitly called it poorly formatted in the same way as the runtime-attention notification.
- **suspected cause:** the setup guidance text is still generated as a long support-style narrative instead of a compact notification-specific layout
- **repo fix:** this run also tightens the `Finish native Zero Net Export setup` Repairs copy in `custom_components/zero_net_export/strings.json` and `custom_components/zero_net_export/translations/en.json`, reshaping it into short `Status`, `Do next`, and `Open` blocks so the native setup warning points operators at Configure without the earlier prose dump. `a42b785` then gives the selector fallback its own `Fallback, only if Home Assistant rejects a valid choice` section so the main next step no longer competes with the fallback workaround.
- **validation status:** repo-side copy fix implemented and advanced again in this run. `tests/test_repairs_copy.py` now locks the separate `Fallback` section for the setup warning too, and `python3 -m unittest tests.test_repairs_copy tests.test_translation_sync -q` plus `python3 -m unittest discover -s tests -q` passed. Live Home Assistant validation is still pending on the next exact-build redeploy.
- **next action:** redeploy the current repo candidate, then confirm the `Finish native Zero Net Export setup` warning renders as a concise sectioned checklist instead of a dense paragraph

## ZNE-031 — Source issue-count sensors are generating HA statistics/state-class cleanup notifications
- **status:** `fixed_pending_validation`
- **severity:** `high`
- **area:** `sensors`
- **where seen:** live Home Assistant notifications on 2026-04-16
- **current observed behavior:** Home Assistant is showing more than 10 cleanup/statistics warnings such as `The entity no longer has a state class` for Zero Net Export issue-count sensors like `sensor.zero_net_export_solar_power_issue_count`
- **expected behavior:** these sensors should not create a flood of HA statistics-cleanup warnings. The issue-count sensors should keep a stable numeric state-class posture so Home Assistant does not keep creating and then invalidating statistics metadata for them.
- **evidence:** user screenshot on 2026-04-16 shows an HA dialog for `sensor.zero_net_export_solar_power_issue_count` stating long-term statistics were previously generated but the entity no longer has a supported state class. User also reported there are over 10 similar notifications in Home Assistant.
- **suspected cause:** the diagnostic issue-count entities are still present with the same entity ids, but the current `ZeroNetExportSourceIssueCountSensor` definition did not set an explicit state class, so Home Assistant treats the historical statistics metadata as a cleanup problem and keeps surfacing warning notifications.
- **repo fix:** this run restores a stable explicit numeric posture by setting `ZeroNetExportSourceIssueCountSensor._attr_state_class = SensorStateClass.MEASUREMENT` in `custom_components/zero_net_export/sensor.py`, with focused regression coverage in `tests/test_sensor_issue_count_state_class.py`.
- **validation status:** repo-side fix implemented in this run and verified with `python3 -m unittest tests.test_sensor_issue_count_state_class -q` plus `python3 -m unittest discover -s tests -q`. Live Home Assistant validation is still pending on the next exact-build redeploy and post-restart notification review.
- **next action:** redeploy the current repo candidate, restart Home Assistant, then confirm the statistics/state-class cleanup notifications for the `*_issue_count` sensors stop reappearing

## ZNE-042 — Source-freshness fallback regressed and now crashes repo-side probe coverage
- **status:** `fixed_pending_validation`
- **severity:** `medium`
- **area:** `runtime`
- **where seen:** repo HEAD test suite on 2026-04-17 after the latest source-label cleanup landed
- **current observed behavior:** `python3 -m unittest discover -s tests -q` was failing in `tests/test_source_freshness_probes.py` because `_source_freshness(...)` now assumes every state-like probe object has `.attributes`, but the probe coverage still uses lightweight `SimpleNamespace(last_updated=...)` fixtures. That regressed repo confidence for the freshness path and would also break any runtime fallback path that passes a state-like object without Home Assistant's full attribute bag.
- **expected behavior:** source-freshness diagnostics should preserve the new friendly-name fallback without crashing when the state-like object only provides `name` and `last_updated`.
- **evidence:** this watchdog run reran `python3 -m unittest discover -s tests -q` and got `AttributeError: 'types.SimpleNamespace' object has no attribute 'attributes'` from `custom_components/zero_net_export/coordinator.py` line 831 while exercising `test_stale_energy_sensor_is_visible_but_does_not_block_runtime` and `test_power_sensor_without_companion_probe_stays_stale`.
- **suspected cause:** the latest raw-source-id cleanup added `state.attributes.get("friendly_name")` directly inside `_source_freshness(...)` without guarding for the repo's state-like probe fixtures or other minimal state objects.
- **repo fix:** this run hardens `custom_components/zero_net_export/coordinator.py` to read `attributes = getattr(state, "attributes", None) or {}` before looking for `friendly_name`, so the new label fallback stays compatible with lightweight probe objects as well as real Home Assistant states.
- **validation status:** repo-side fix verified in this run with `python3 -m unittest tests.test_source_freshness_probes -q` and `python3 -m unittest discover -s tests -q`. Live Home Assistant validation is still pending under the next exact-build deploy/restart cycle.
- **next action:** keep this covered by the freshness-probe regression tests, then fold it into the next exact-build reconciliation instead of treating unchanged live fingerprint drift as a reason to skip repo-side validation coverage

## ZNE-037 — HACS still shows v0.1.84 even after v0.1.86 release/deploy
- **status:** `open`
- **severity:** `high`
- **area:** `release/distribution`
- **where seen:** live HACS repository page on 2026-04-16 after `v0.1.86` release/deploy
- **current observed behavior:** HACS still shows the Zero Net Export repository/version as `v0.1.84` even though `v0.1.86` has been tagged, released on GitHub, and manually deployed to Home Assistant
- **expected behavior:** after the `v0.1.86` release is published and HACS metadata is refreshed, the HACS repository view should surface `v0.1.86`, not `v0.1.84`
- **evidence:** user screenshot on 2026-04-16 shows the HACS repository page header still displaying `v0.1.84`. In the same release run, GitHub release publication for `v0.1.86` succeeded and the remote HA manifest copy showed `version: 0.1.86`, so the stale HACS view is a real distribution/metadata lag or refresh failure, not just a missing git tag. This run also rechecked the GitHub release state directly through the GitHub API and confirmed `v0.1.86` is already published as the newest non-draft, non-prerelease release (`published_at=2026-04-16T03:52:08Z`), which narrows the remaining bug to HACS refresh or Home Assistant-side release visibility rather than GitHub publication.
- **suspected cause:** GitHub release visibility is now verified, so the remaining stale version is most likely HACS metadata that has not refreshed to the latest published release, or a Home Assistant-side HACS cache/refresh path that still has not picked up `v0.1.86`
- **validation status:** still confirmed live and still not fixed, but the bug is now narrowed: GitHub latest-release state is good, HACS visibility is the remaining failing layer
- **next action:** keep this release-management bug open as supporting evidence, but do not let it displace ongoing `0.1.87` UI implementation work; when the next materially new release-reconciliation step happens, explicitly run the HACS `Update information` refresh for Zero Net Export, verify the repository row surfaces `v0.1.86`, and keep treating stale HACS version visibility as a release-management bug until the repository page matches the already-published GitHub release

## ZNE-038 — Fleet workspace next step ignores blocking source repair and sends operators to Managed Devices first
- **status:** `fixed_pending_validation`
- **severity:** `high`
- **area:** `managed_devices`
- **where seen:** live Home Assistant API inspection on 2026-04-16 while the current install still had required source-role blockers
- **current observed behavior:** `sensor.zero_net_export_fleet_console_next_step` still told the operator to open `Configure -> Managed Devices` and tag the first candidate into the fleet even while `sensor.zero_net_export_command_center_status` was simultaneously reporting missing required source roles for solar/grid mappings. This run also confirmed the same mixed guidance is still visible in the live command-center payload itself, where `recommended_section` correctly points to `Sensors` but `device_next_step` still pushes `Managed Devices` first.
- **expected behavior:** when required mapped sources are still missing or otherwise blocking runtime, the fleet workspace next-step sensor should point operators back to `Configure -> Sensors` first, then only send them into Managed Devices after source repair is out of the way.
- **evidence:** documented Home Assistant API inspection originally returned `sensor.zero_net_export_command_center_status = Missing required source roles: Solar power, Solar energy, Grid import power, Grid export power, Grid import energy, Grid export energy` while `sensor.zero_net_export_fleet_console_next_step = Open Settings -> Devices & Services -> Integrations -> Zero Net Export -> Configure -> Managed Devices and tag the first candidate into the fleet`. This run rechecked the same live API path and the mismatch is still present on the current installed build: `sensor.zero_net_export_command_center_status` still reports the same required source-role blockers, its `recommended_section` is now `Sensors` but its `device_next_step` still incorrectly sends the operator to `Configure -> Managed Devices`, `sensor.zero_net_export_managed_fleet_overview = 0 managed | 163 unmanaged | top AC Outlet 2`, and `sensor.zero_net_export_fleet_console_next_step` still incorrectly sends the operator to `Configure -> Managed Devices` first.
- **suspected cause:** the fleet workspace next-step sensor was ranking fleet actions from managed-device state alone and was not gating those recommendations on the same blocking mapped-source attention signals already used by the command center and source-blocker sensors.
- **repo fix:** this run's fleet-next-step source-gating fix updates `custom_components/zero_net_export/sensor.py` so `fleet_console_next_step` checks blocking mapped-source attention first and sends operators to `Configure -> Sensors` before any fleet-promotion guidance; `tests/test_sensor_entity_categories.py` now covers that ordering.
- **validation status:** repo-side fix remains verified with `python3 -m unittest tests.test_sensor_entity_categories -q` plus `python3 -m unittest discover -s tests -q`, which passed again in this run. Live Home Assistant validation is still not green: this run's API recheck confirms the old wrong next-step wording is still live in both the fleet next-step sensor and the command-center `device_next_step`, which matches the remaining undeployed `config_flow.py`, `native_support.py`, and `sensor.py` drift tracked under ZNE-022 rather than disproving the repo fix.
- **next action:** redeploy the current repo candidate, then confirm `sensor.zero_net_export_fleet_console_next_step` points to `Configure -> Sensors` while required mapped-source blockers remain and only returns to Managed Devices guidance after those blockers are cleared

## ZNE-014 — Post-install log review confirms the live install state is still not fully healthy
- **status:** `fixed_pending_validation`
- **severity:** `high`
- **area:** `runtime`
- **where seen:** live Home Assistant mixed-build state on 2026-04-16 after user install and manual log review request
- **current observed behavior:** the earlier broken-config-flow and missing-entity install state is no longer the current live symptom. The documented HA SSH path still works, but the live system remains materially behind the repo candidate and is not in a trustworthy release state: the latest exact-build fingerprint now shows **ten** undeployed files rather than the older eight-file set. Live Home Assistant is still on `0.1.86`, while `__init__.py`, `button.py`, `candidate_utils.py`, `config_flow.py`, `coordinator.py`, `diagnostics.py`, `native_support.py`, `sensor.py`, `strings.json`, and `translations/en.json` are all behind the current repo state.
- **expected behavior:** after install, Zero Net Export should load cleanly, allow Add Integration, avoid integration-specific live errors in Home Assistant logs, and match the exact repo candidate under review, including the current managed-device review handoff fixes plus the `config_flow.py`, `native_support.py`, and `sensor.py` fleet-workspace fixes.
- **evidence:** live HA review on 2026-04-15 originally confirmed remote manifest `0.1.83` plus `homeassistant.config_entries` errors and the missing-config-entry/orphaned-entity condition tracked elsewhere in this file. In the last live validation run, documented HA SSH access on `root@192.168.86.200:2222` succeeded and `python3 scripts/validate_install_fingerprint.py /config/custom_components --ssh-host root@192.168.86.200 --ssh-port 2222` against repo commit `f5f1715` reported `overall_match=false` with `config_flow.py`, `native_support.py`, and `sensor.py` still behind. In this watchdog run, the same documented HA SSH path still succeeds and the fingerprint result is still not aligned with the current repo candidate: `overall_match=false`, `manifest_version_matches=true`, with live `__init__.py` (`sha256_12=eec18fa012fe`, 7266 bytes) versus repo `__init__.py` (`sha256_12=7820b4dac42e`, 7266 bytes), live `button.py` (`sha256_12=96f46044d071`, 29095 bytes) versus repo `button.py` (`sha256_12=39d262e3e2c2`, 39180 bytes), live `candidate_utils.py` (`sha256_12=bbffc1f2d809`, 17386 bytes) versus repo `candidate_utils.py` (`sha256_12=504227023b62`, 20898 bytes), live `config_flow.py` (`sha256_12=a894cbcd8fd9`, 126989 bytes) versus repo `config_flow.py` (`sha256_12=474a27c0b626`, 141336 bytes), live `coordinator.py` (`sha256_12=ea4438b2b837`, 72632 bytes) versus repo `coordinator.py` (`sha256_12=4d190afddab9`, 72823 bytes), live `diagnostics.py` (`sha256_12=3325ccb1cd73`, 10324 bytes) versus repo `diagnostics.py` (`sha256_12=f16c7a91922a`, 10313 bytes), live `native_support.py` (`sha256_12=872e48901843`, 69325 bytes) versus repo `native_support.py` (`sha256_12=e5319e187c6c`, 75285 bytes), live `sensor.py` (`sha256_12=6c87c053febb`, 37812 bytes) versus repo `sensor.py` (`sha256_12=a5fd8d1d5a00`, 42785 bytes), live `strings.json` (`sha256_12=0eb43eca5ca5`, 21073 bytes) versus repo `strings.json` (`sha256_12=1c0b7f9e9aa5`, 27410 bytes), and live `translations/en.json` (`sha256_12=0eb43eca5ca5`, 21073 bytes) versus repo `translations/en.json` (`sha256_12=1c0b7f9e9aa5`, 27410 bytes). A same-run SSH log spot-check via `ha core logs -n 200 | grep -i 'zero_net_export\|entity.py\|blocking call' | tail -60` returned no project-specific output, so the live blocker is still exact-build drift rather than a fresh Zero Net Export traceback. The same run still shows `manifest.json` matching live at `0.1.86`, so the release line is aligned while the shipped component files are not.
- **suspected cause:** the original install-health failure overlapped ZNE-002 and ZNE-011; the remaining live-health concern now overlaps ZNE-022 because Home Assistant is still running older `button.py`, `candidate_utils.py`, `diagnostics.py`, `config_flow.py`, `native_support.py`, `sensor.py`, `strings.json`, and `translations/en.json` files than the current repo candidate even after the repo release metadata was realigned to the live `0.1.86` line.
- **validation status:** materially improved versus the earlier broken install, and the previous manifest/version mismatch is still cleared in this run. The latest HA log spot-check did not surface a new Zero Net Export traceback, but this umbrella bug should stay open until the remaining `__init__.py`, `button.py`, `candidate_utils.py`, `config_flow.py`, `coordinator.py`, `diagnostics.py`, `native_support.py`, `sensor.py`, `strings.json`, and `translations/en.json` drift is reconciled under the formal release flow and a post-restart log review confirms the live install is healthy and fingerprint-aligned. Repeating the same fingerprint check again without a new deploy/restart or explicit release authorization would now be loop churn, not new validation.
- **next action:** treat the remaining undeployed `__init__.py`, `button.py`, `candidate_utils.py`, `config_flow.py`, `coordinator.py`, `diagnostics.py`, `native_support.py`, `sensor.py`, `strings.json`, and `translations/en.json` drift under ZNE-022 as the real release blocker, and ask James directly for explicit deploy/restart approval before another formal release-validation pass. If release authorization is still not available, keep `0.1.87` repo-side UI work moving instead of re-running unchanged fingerprint bookkeeping.

## ZNE-011 — Config flow cannot load because backup folder names pollute Home Assistant module discovery
- **status:** `fixed_pending_validation`
- **severity:** `critical`
- **area:** `config_flow`
- **where seen:** live Home Assistant `0.1.83` install while adding the integration
- **current observed behavior:** the Add Integration flow fails with `Config flow could not be loaded: {"message":"Invalid handler specified"}` and the HA log shows `Error occurred loading flow for integration zero_net_export: No module named 'custom_components.zero_net_export.backup_openclaw_20260415_220427'`
- **expected behavior:** selecting Zero Net Export in Add Integration should load `custom_components.zero_net_export.config_flow` normally and open the setup flow
- **evidence:** user screenshots from 2026-04-15 show the exact popup and log detail. Live HA inspection confirmed multiple backup directories still exist under `/config/custom_components/`, including `zero_net_export.backup_openclaw_20260415_220427` and `zero_net_export.backup_openclaw_20260415_220522`, matching the module path mentioned in the traceback.
- **suspected cause:** the deploy backup strategy leaves backup directories under `/config/custom_components/` with names that still begin with `zero_net_export`, polluting Home Assistant custom-component discovery and causing config-flow import resolution to point at a backup pseudo-module path instead of the real integration package
- **repo fix:** this run updates `scripts/deploy_exact_repo_build.py` so default backups are now written outside Home Assistant discovery under `<config>/.openclaw_backups/custom_components/zero_net_export.backup-<timestamp>` instead of as sibling directories inside `/config/custom_components/`; `tests/test_install_helper_scripts.py` now covers the new backup path and verifies the copied backup stays outside the discovery root. This run also adds `scripts/clean_legacy_discovery_artifacts.py` plus extra `tests/test_install_helper_scripts.py` coverage so older `zero_net_export.backup_*` directories and matching stale `zero_net_export.*.pyc` artifacts can be detected and removed safely from an existing Home Assistant `custom_components` tree.
- **validation status:** repo-side fix and cleanup helper are implemented and verified with `python3 -m unittest tests.test_install_helper_scripts` and `python3 -m unittest discover -s tests`. Live state changed materially in this run: documented HA SSH inspection now shows `/config/custom_components` no longer contains any `zero_net_export.backup*` sibling directories, so the earlier discovery-pollution evidence is gone. The user later got past Add Integration and reached the integration card, which is another sign that the original backup-module import failure is likely cleared. However, Configure is still failing with a different 500 error, so this bug remains `fixed_pending_validation` rather than closed until the whole config-flow path is revalidated cleanly.
- **next action:** treat the original backup-discovery root cause as likely mitigated, and focus immediate debugging on the new Configure 500 error and setup traceback. Once that new failure is fixed, re-verify Add Integration and Configure end to end before closing this bug

## ZNE-015 — Configure gear action fails with 500 because runtime setup still crashes on missing `build_source_attention_brief`
- **status:** `validated`
- **severity:** `critical`
- **area:** `config_flow`
- **where seen:** live Home Assistant `0.1.83` after the user successfully added the integration and then clicked the gear/configure icon
- **current observed behavior:** the integration card shows `Failed to set up`, and clicking the gear/configure icon raises `Config flow could not be loaded: 500 Internal Server Error Server got itself in trouble`
- **expected behavior:** once the integration is added, clicking Configure should open the native Zero Net Export options/config flow instead of surfacing a server-side 500
- **evidence:** user screenshot from 2026-04-15 shows the integration card in Home Assistant with `Failed to set up` and the exact 500 popup after clicking the gear icon. Live HA logs from the same run show the real traceback: `ImportError: cannot import name 'build_source_attention_brief' from 'custom_components.zero_net_export.native_support'` while loading `custom_components.zero_net_export.sensor`, followed by `homeassistant.config_entries` setup failure for the Zero Net Export entry and an aiohttp server error path through `config_flow.py`.
- **suspected cause:** repo/runtime drift left `sensor.py` importing `build_source_attention_brief` even though that helper no longer existed in `native_support.py`, so entry setup fails and the later Configure request surfaces as a generic 500
- **repo fix:** this run restores `build_source_attention_brief(...)` in `custom_components/zero_net_export/native_support.py` and adds focused regression coverage in `tests/test_source_attention_brief.py`; commit `37777cd` — `fix: restore source attention brief helper`
- **validation status:** repo-side fix implemented and earlier verified with `python3 -m unittest tests.test_source_attention_brief tests.test_install_helper_scripts tests.test_command_center_summary tests.test_release_update_details tests.test_source_freshness_probes tests.test_release_info_install_guidance -q` and `python3 -m py_compile custom_components/zero_net_export/native_support.py custom_components/zero_net_export/sensor.py`. Live validation changed materially in this run: documented HA SSH access now shows an active `zero_net_export` config entry and live Zero Net Export sensors are updating often enough to emit new candidate-shortlist state-length errors, which would not happen if `sensor.py` were still failing to import `build_source_attention_brief`. The Configure path still fails, but with a different traceback (`KeyError: 'source_repair_step'`) rather than the old import error.
- **next action:** treat the original missing-helper crash as validated fixed, and continue the current Configure 500 work under ZNE-018 instead of keeping this older root cause open

## ZNE-018 — Configure init crashes with `KeyError: 'source_repair_step'`
- **status:** `fixed_pending_validation`
- **severity:** `high`
- **area:** `config_flow`
- **where seen:** live Home Assistant `0.1.83` after re-adding the integration and opening Configure
- **current observed behavior:** clicking Configure still raises a server-side 500 because `async_step_init` expects `command_center["source_repair_step"]`, but `build_native_command_center_summary(...)` does not always populate that key
- **expected behavior:** Configure should open even when the command-center summary omits optional source-repair detail; the options flow should fall back to a generic source-repair step instead of crashing
- **evidence:** documented HA SSH access on `root@192.168.86.200:2222` succeeded in this run, and `ha core logs -n 240` shows `File "/config/custom_components/zero_net_export/config_flow.py", line 1431, in async_step_init` followed by `"source_repair_step": command_center["source_repair_step"],` and `KeyError: 'source_repair_step'`
- **suspected cause:** the options-flow handoff assumed the command-center summary always carried `source_repair_step`, but some runtime states only emit `next_action_summary` and related fields
- **repo fix:** `209665e` — this run updates `custom_components/zero_net_export/native_support.py` so the command-center summary always emits `source_repair_step`, and also hardens `custom_components/zero_net_export/config_flow.py` with a fallback `build_source_repair_step()` default if the key is ever absent again
- **validation status:** repo-side fix implemented and covered by `python3 -m unittest tests.test_command_center_summary -q`, `python3 -m unittest discover -s tests -q`, and `python3 -m py_compile custom_components/zero_net_export/config_flow.py custom_components/zero_net_export/native_support.py`. In this run, the exact `209665e` repo build was also copied to `/config/custom_components/zero_net_export`, `scripts/validate_install_fingerprint.py /config/custom_components --ssh-host root@192.168.86.200 --ssh-port 2222` returned `overall_match=true`, and Home Assistant core was restarted cleanly. However, Configure itself still needs a post-deploy click test before this bug can be closed.
- **next action:** re-open Configure in live Home Assistant and confirm the 500 is gone on the deployed `209665e` build

## ZNE-019 — Candidate overview and shortlist sensors overflow Home Assistant state length
- **status:** `validated`
- **severity:** `medium`
- **area:** `sensors`
- **where seen:** live Home Assistant `0.1.83` with a larger unmanaged-candidate backlog
- **current observed behavior:** `sensor.zero_net_export_unmanaged_candidate_overview` and `sensor.zero_net_export_candidate_shortlist` emit long usefulness/warning strings that exceed Home Assistant's 255-character sensor-state limit, so HA logs repeated errors and falls those states back to `unknown`
- **expected behavior:** Managed Devices candidate summary sensors should stay compact enough to remain valid HA state strings while still giving operators a useful at-a-glance snapshot
- **evidence:** documented HA SSH access on `root@192.168.86.200:2222` succeeded in this run, and `ha core logs -n 120` shows repeated errors such as `State AC Outlet 2 (fixed) | strong match | key warning: No immediate warnings; ... for sensor.zero_net_export_unmanaged_candidate_overview is longer than 255, falling back to unknown` and `State 3rd Bedroom Crossfade (switch.bedroom_crossfade, fixed) | ... for sensor.zero_net_export_candidate_shortlist is longer than 255, falling back to unknown`
- **suspected cause:** the candidate summary sensors were reusing verbose review-preview strings that are suitable for notifications or attributes, but too long for HA state payload limits once multiple candidates are concatenated
- **repo fix:** `209665e` — this run adds `build_candidate_name_summary(...)` in `custom_components/zero_net_export/candidate_utils.py` and switches the candidate overview/shortlist sensor states to compact name-list summaries, keeping the richer fit/warning detail in attributes and deeper review paths instead of the state string itself
- **validation status:** repo-side fix implemented and covered by `python3 -m unittest tests.test_candidate_utils -q`, `python3 -m unittest discover -s tests -q`, and `python3 -m py_compile custom_components/zero_net_export/candidate_utils.py custom_components/zero_net_export/sensor.py`. Live validation succeeded in this run: the exact `209665e` repo build was copied to `/config/custom_components/zero_net_export`, `scripts/validate_install_fingerprint.py /config/custom_components --ssh-host root@192.168.86.200 --ssh-port 2222` returned `overall_match=true`, Home Assistant core was restarted, and a follow-up `ha core logs` pass after the next coordinator cycles no longer showed the earlier `longer than 255, falling back to unknown` errors for `sensor.zero_net_export_unmanaged_candidate_overview` or `sensor.zero_net_export_candidate_shortlist`.
- **next action:** keep candidate detail richness in attributes/notifications/deeper review paths so future summary expansions do not regress back into overlong HA state strings

## ZNE-020 — Install provenance fingerprinting performs blocking file I/O in the event loop
- **status:** `fixed_pending_validation`
- **severity:** `medium`
- **area:** `runtime`
- **where seen:** live Home Assistant on the older installed `0.1.83` component build, plus earlier exact-build validation of commit `e1dcef8`
- **current observed behavior:** the repo fix exists, but the currently installed Home Assistant copy is still old enough to emit blocking `read_bytes` and `open` warnings from `release_info.py` while building install-provenance metadata from `manifest.json`
- **expected behavior:** install-provenance and release-info helpers should avoid synchronous filesystem reads on the event loop during coordinator/update paths
- **evidence:** documented HA SSH access on `root@192.168.86.200:2222` succeeded again in this run. `ha core logs -n 400 | grep -i zero_net_export` still shows `Detected blocking call to read_bytes ... at custom_components/zero_net_export/release_info.py, line 62: sha256_12 = hashlib.sha256(path.read_bytes()).hexdigest()[:12]` plus the matching blocking `open` warning at `2026-04-16 00:09:48`. The earlier exact-build validation against commit `e1dcef8` had cleared those warnings after restart, so this run's renewed warning is consistent with the current live fingerprint drift rather than a newly disproven repo fix.
- **suspected cause:** `build_install_provenance()` ultimately called `_cached_install_provenance()`, which still fell back to synchronous manifest/hash reads from `release_info.py` when the earlier warmup path had not produced a stable shared snapshot for later sync consumers on the event loop; the current live symptom persists because Home Assistant is still running an older `release_info.py` than repo HEAD
- **repo fix:** `e1dcef8` — replace the install-provenance warmup with an explicit async-primed snapshot in `custom_components/zero_net_export/release_info.py`, make synchronous callers fall back to a non-blocking pending snapshot instead of doing file I/O on the event loop, and refresh that snapshot during both setup-entry and migrate-entry startup in `custom_components/zero_net_export/__init__.py`; `tests/test_release_info_install_guidance.py` now covers the pending-snapshot fallback plus the new executor priming path
- **validation status:** repo-side fix remains verified with `python3 -m unittest tests.test_release_info_install_guidance -q`, `python3 -m unittest discover -s tests -q`, and `python3 -m py_compile custom_components/zero_net_export/release_info.py custom_components/zero_net_export/__init__.py`. Earlier live validation on the exact fixed build had also succeeded. However, this bug cannot stay `validated` as the current live state because the installed component fingerprint has drifted again and the old blocking-call warning is presently visible in Home Assistant logs.
- **next action:** treat exact-build redeploy as the immediate prerequisite under ZNE-022, then rerun post-restart log review and close this only after the current installed build no longer emits the blocking `release_info.py` warnings

## ZNE-022 — Live HA install no longer matches the current repo candidate after fingerprint coverage widened
- **status:** `fixed_pending_validation`
- **severity:** `medium`
- **area:** `release`
- **where seen:** repo-versus-live fingerprint validation on 2026-04-18 against current repo HEAD, rechecked again in this watchdog run
- **current observed behavior:** this watchdog run rechecked the documented HA SSH path and `python3 /root/.openclaw/workspace/projects/zero-net-export/scripts/validate_install_fingerprint.py --ssh-host root@192.168.86.200 --ssh-port 2222 /config` still reports `overall_match=false`. The live Home Assistant component payload is still aligned on the `0.1.86` release line, and the active drift is still the same unchanged ten-file mismatch: `__init__.py`, `button.py`, `candidate_utils.py`, `config_flow.py`, `coordinator.py`, `diagnostics.py`, `native_support.py`, `sensor.py`, `strings.json`, and `translations/en.json`. The real drift in this run is unchanged release-blocker churn: recent repo history spent two more docs-only commits (`966e654` then `8c83034`) re-correcting the same unchanged mismatch count instead of advancing remaining `0.1.87` implementation work or executing an approved deploy.
- **expected behavior:** before trusting further live validation, Home Assistant should match one exact current component candidate across the full shipped component surface, including the latest managed-fleet snapshot wording follow-through in `button.py` and `config_flow.py` plus the earlier unmanaged-review backlog wording and candidate usefulness refinements in `candidate_utils.py`, `native_support.py`, and `sensor.py`, while still staying on the active `0.1.86` release line. Release bookkeeping should anchor on the current `expected_component_commit` / `preferred_validation_commit` helper output at deploy time, but unchanged fingerprint mismatch should not trigger another bug-tracker-only commit by itself.
- **evidence:** documented HA SSH access on `root@192.168.86.200:2222` succeeded again in this watchdog run, and a fresh `python3 /root/.openclaw/workspace/projects/zero-net-export/scripts/validate_install_fingerprint.py --ssh-host root@192.168.86.200 --ssh-port 2222 /config` against current repo state still reports `overall_match=false` with `manifest_version_matches=true`, `expected_component_commit=28361a2`, and the same ten tracked-file mismatches seen in the last accurate release-blocker entry: `__init__.py`, `button.py`, `candidate_utils.py`, `config_flow.py`, `coordinator.py`, `diagnostics.py`, `native_support.py`, `sensor.py`, `strings.json`, and `translations/en.json`. Recent repo-only churn before this correction was `966e654` (`docs: correct live fingerprint drift count`) followed by `8c83034` (`docs: correct fingerprint blocker drift count again`), both touching only source-of-truth docs while the underlying release boundary and approval state stayed unchanged.
- **suspected cause:** Home Assistant is still running an older deployed component build, and watchdog/supervisor bookkeeping had drifted into rotating the documented deploy anchor inside `docs/BUGS.md` whenever a newer component commit landed locally, even without new live evidence. That invited low-value fingerprint/doc churn to replace remaining `0.1.87` product work.
- **repo fix:** `afbbd71`, `cf46eac`, `b9766aa`, `f45aef9`, `c94c19b`, `f11ac60`, `5eaa529`, `6013f0a`, `50dc07c`, `e89324f`, `fca6632`, `245eb09`, `7d92a22`, `69134a7`, `c93b709`, `5d49a2c`, `5925f6f`, `2514ef7`, `02a3e8d`, `08b7994`, `b812e36`, `f4b6f09`, `895842d`, `c8c23d9`, `a70ee31`, `9e46756`, `0e5683b`, `aeb0775`, `fee2d5d`, `778e258`, `c380a77`, `5186f80`, `f0ef24e`, `8e3759a`, `72a487f`, `4c52c11`, `0fc3444`, `1aceae9`, `71b5f9c`, `fc5edaa`, `26aca12`, `f0c35d3`, `452b928`, `a49f400`, `6391b0e`, `da41665`, `5ae32ca`, `8a62c98`, `49d8bb0`, `10f54d8`, `e3bdaa3`, `d543833`, `3cb05a0`, `1808714`, `c04a7a6`, `1965d67`, `7e0ee7a`, `859af18`, `9fb7f63`, `73dc3b7`, `b7152bc`, `46e3e34`, `815a271`, `b485047`, `09057df`, `11ea6f2`, `7fd4ae2`, `579a9cb`, `2fa19bc`, `8f79d84`, `2293aef`, `671572a`, `0a10da0`, `bbcd00c`, `692985e`, `d888c4c`, `4f113ac`, `0d250b8`, `85e6ef9`, `42d9c2d`, `688070b`, `e5f7bbb`, `92c5301`, `0283928`, `d971284`, and `28361a2`, plus the earlier fingerprint-helper split work in `b73dfa7`. Exact-build fingerprint coverage is in place; this run only corrects the stale blocker state so unchanged mismatch checks stop masquerading as fresh release movement.
- **validation status:** this watchdog run re-confirmed through the documented HA SSH fingerprint path that live Home Assistant is still behind the current repo candidate, now anchored on `expected_component_commit=28361a2`. The mismatch set is still unchanged, so the material project update in this run is source-of-truth correction plus churn detection, not release progress.
- **next action:** do not spend another watchdog run re-correcting this same ten-file mismatch unless new live evidence appears. If formal deploy/restart becomes the real next step, ask James directly for approval and use the current helper output at that time. Otherwise keep working the next unfinished `0.1.87` implementation stage instead of repeating unchanged fingerprint bookkeeping.

## ZNE-045 — Fingerprint payload still let `expected_commit` drift with repo-head bookkeeping
- **status:** `fixed_pending_validation`
- **severity:** `medium`
- **area:** `release`
- **where seen:** repo-side fingerprint-helper audit on 2026-04-18, with live HA SSH validation recheck in the same run
- **current observed behavior:** `docs/VALIDATION_CHECKLIST.md`, `README.md`, and the earlier ZNE-043 closure all said fingerprint guidance had been corrected so deploy decisions could stay anchored on the component-changing build, but the helper payload itself still emitted `expected_commit` as full repo HEAD whenever `scripts/compare_install_fingerprint.py` built its own expected payload. In practice that meant doc-only or test-only commits could still move one of the primary comparison fields and reintroduce the same false release-candidate churn the supervisor explicitly warns against.
- **expected behavior:** the emitted comparison payload should keep `expected_commit`, `expected_component_commit`, and `preferred_validation_commit` aligned on the latest component-changing commit, while exposing full repo HEAD separately as `repo_head_commit`, so helper output, docs, and bug-tracker guidance all describe the same deploy anchor.
- **evidence:** repo inspection in this run confirmed `scripts/print_expected_install_fingerprint.py` and the no-`--expected-json` path in `scripts/compare_install_fingerprint.py` were still setting `expected_commit` from `git rev-parse --short HEAD` even though the surrounding guidance already told operators to treat component commit as the real shipped-build anchor. The documented HA SSH path in `TOOLS.md` also worked again in this run: `python3 scripts/validate_install_fingerprint.py /config/custom_components --ssh-host root@192.168.86.200 --ssh-port 2222` still reports the same unchanged 10-file live mismatch set, and the refreshed expected payload now renders `expected_commit=ad84902`, `expected_component_commit=ad84902`, `preferred_validation_commit=ad84902`, and `repo_head_commit=be623a4`, which matches the intended component-anchor semantics instead of rotating with doc-only repo HEAD.
- **suspected cause:** the earlier fingerprint-guidance cleanup updated deploy wording and added `preferred_validation_commit`, but left the legacy `expected_commit` field bound to repo HEAD in the helper payload and compare fallback path.
- **repo fix:** this run's fingerprint-anchor follow-up commit — update `scripts/print_expected_install_fingerprint.py` and the repo-head fallback in `scripts/compare_install_fingerprint.py` so `expected_commit` is the component-changing validation anchor, add `repo_head_commit` as the separate full-repo marker, refresh `tests/test_install_helper_scripts.py`, and align `README.md` plus `docs/VALIDATION_CHECKLIST.md` with the corrected payload contract.
- **validation status:** repo-side fix implemented in this run. Verification passed with the documented HA SSH validation path above plus `python3 -m unittest tests.test_install_helper_scripts -q`. Full live deploy/restart validation is not applicable yet because ZNE-022's unchanged mixed-build mismatch is still the release gate.
- **next action:** keep the watchdog focused on remaining `0.1.87` implementation work or an explicitly approved deploy, not another fingerprint-anchor wording refresh.

## Recently validated or closed bugs

## ZNE-036 — Repo working version drifted forward to `0.1.86` without new release-line evidence
- **closed on:** 2026-04-16
- **severity:** `medium`
- **area:** `release`
- **historical behavior:** repo HEAD had been bumped to `0.1.86` in shipped metadata and install-helper regression expectations while other source-of-truth docs still lagged on `0.1.85` wording. That intermediate state is now historical only.
- **repo fix:** this run's release-metadata correction commit initially restored the repo working version back to `0.1.85` while the release boundary was still being reconciled.
- **closure evidence:** later same-day source-of-truth updates superseded the temporary `0.1.85` boundary. `docs/SUPERVISOR.md`, `project_status.md`, `custom_components/zero_net_export/manifest.json`, `CHANGELOG.md`, and live HA fingerprint evidence now all treat `0.1.86` as the active correction line, so this bug should no longer be read as current steering.

## ZNE-035 — Project status still hid the real `0.1.85` release-approval boundary
- **closed on:** 2026-04-16
- **severity:** `medium`
- **area:** `process`
- **historical behavior:** repo inspection in this run found `project_status.md` still said `user_action: none` and still listed the already-corrected `0.1.83` metadata drift as the main blocker, even though `docs/SUPERVISOR.md`, `RELEASE_MANAGEMENT.md`, and active bug state already made the true next boundary explicit: James must directly approve the formal `0.1.85` release flow before redeploy/restart/live validation continues.
- **repo fix:** this run's status-boundary correction commit — update `project_status.md` so the next action explicitly asks James for release approval now, the blocker names the remaining `entity.py` drift plus the approval boundary, and `user_action` no longer incorrectly says `none`.
- **closure evidence:** repo-side source-of-truth audit plus direct status-file correction in the same run originally closed this bug. That exact `0.1.85` approval ask is historical only. Current steering has since shifted away from making unchanged `0.1.86` release reconciliation the default headline while unfinished `0.1.87` UI work remains, so `project_status.md` no longer serves as an always-on approval prompt. The durable lesson from this closed bug is narrower: if formal deploy/restart/release execution becomes the real next step again, James must be asked directly instead of being left to infer approval from status wording.

## ZNE-034 — Repo metadata and bug-tracker steering drifted back to `0.1.83` even though the active live release line is `0.1.85`
- **closed on:** 2026-04-16
- **severity:** `medium`
- **area:** `release`
- **historical behavior:** repo metadata and operator-facing status files had drifted back to `0.1.83`, which contradicted the then-current live Home Assistant manifest and steering. That intermediate `0.1.85` correction state is now historical only.
- **repo fix:** this run restored the repo working version and release-target text to `0.1.85` in `custom_components/zero_net_export/manifest.json`, `CHANGELOG.md`, `README.md`, `project_status.md`, and `tests/test_install_helper_scripts.py`.
- **closure evidence:** later same-day source-of-truth updates superseded the temporary `0.1.85` release line. `docs/SUPERVISOR.md`, `project_status.md`, `custom_components/zero_net_export/manifest.json`, `CHANGELOG.md`, and live HA fingerprint evidence now align on `0.1.86`, so this bug is closed historical context rather than active release guidance.

## ZNE-032 — Release summary incorrectly reports a rollback as a normal update
- **closed on:** 2026-04-16
- **severity:** `medium`
- **area:** `release`
- **historical behavior:** earlier live Home Assistant API inspection showed `sensor.zero_net_export_release_summary` reporting `Updated from 0.1.84 to 0.1.83`, which read like a normal upgrade even though the version had moved backwards during the temporary repo-side `0.1.83` release-line drift.
- **repo fix:** `63265ab` with regression coverage in `7617337` — update `custom_components/zero_net_export/coordinator.py` so release summaries compare previous versus current version direction and only use normal upgrade wording for forward version moves.
- **closure evidence:** this run rechecked the documented HA API path from `TOOLS.md` and the live `sensor.zero_net_export_release_summary` now reports `current_version: 0.1.85`, `previous_installed_version: 0.1.83`, `version_change_direction: 1`, and `summary: Updated from 0.1.83 to 0.1.85. ...`, so the rollback-specific miswording is no longer the live bug state.

## ZNE-004 — Live install version stamp mismatched the intended `0.1.82` release
- **closed on:** 2026-04-15
- **severity:** `high`
- **area:** `release`
- **historical behavior:** during `0.1.82` release validation, the live Home Assistant install still showed `Version 0.1.81`, so the installed component fingerprint did not match the intended shipped build.
- **repo fix:** earlier exact-build release/deploy work plus this run's live fingerprint re-check.
- **closure evidence:** in this run, documented HA SSH access on `root@192.168.86.200:2222` succeeded and `python3 scripts/validate_install_fingerprint.py /config/custom_components --ssh-host root@192.168.86.200 --ssh-port 2222` reported live manifest `0.1.83`, all tracked files matching the repo candidate from commit `5fe1c93`, and `overall_match=true`. Release-fingerprint drift is no longer the blocker; the remaining live blocker is the missing Zero Net Export config entry tracked in ZNE-002.

## ZNE-013 — Repo-local `tmp-ha-config/` scratch tree tripped exact-build clean-state guards
- **closed on:** 2026-04-15
- **severity:** `low`
- **area:** `process`
- **historical behavior:** repo inspection in this run found an untracked `tmp-ha-config/` scratch Home Assistant component tree at the repo root. That local validation debris made `git status --short` report the repo dirty and would trip `scripts/deploy_exact_repo_build.py --require-clean`, creating release-gate noise unrelated to the actual `0.1.83` candidate contents.
- **repo fix:** this run's repo-cleanliness guard fix — ignore `tmp-ha-config/` in `.gitignore` and record the release-hygiene correction in `CHANGELOG.md`
- **closure evidence:** after the ignore rule landed, repo status no longer reports `tmp-ha-config/` as an untracked change, so exact-build flows can use `--require-clean` without local scratch-tree drift

## ZNE-012 — Native-path docs still used vague `integration/device surfaces` wording after the source-of-truth path tightened
- **closed on:** 2026-04-15
- **severity:** `low`
- **area:** `docs`
- **historical behavior:** repo wording in `README.md`, `docs/DASHBOARD_SETUP.md`, and `docs/PRODUCT_SPEC_V1.md` still used the fuzzy phrase `integration/device surfaces` even though `docs/UI_DESIGN.md` and `docs/SUPERVISOR.md` now define the supported native path more precisely as Configure plus `Settings -> Devices & Services -> Integrations -> Zero Net Export -> Devices -> open the Zero Net Export device`. That wording drift weakened operator discoverability and contradicted the changelog claim that the remaining path wording had already been corrected.
- **repo fix:** this run's native-path wording alignment commit — replace the vague shorthand with the exact native Configure and device-path wording across the remaining repo docs, and record the correction in `CHANGELOG.md`.
- **closure evidence:** repo-side source-of-truth audit plus direct doc correction in the same run; `README.md`, `docs/DASHBOARD_SETUP.md`, and `docs/PRODUCT_SPEC_V1.md` now match the explicit native Home Assistant path language used by `docs/UI_DESIGN.md` and `docs/SUPERVISOR.md`. This run also corrected the remaining README/bootstrap-validation shorthand so the install guide, primary operator-path section, advanced-path note, and `docs/VALIDATION_CHECKLIST.md` now point to the exact `Settings -> Devices & Services -> Integrations -> Zero Net Export -> Configure` path instead of generic `Configure` wording.

## ZNE-017 — Source-of-truth UI redesign existed only in a dirty working tree
- **closed on:** 2026-04-15
- **severity:** `medium`
- **area:** `docs`
- **historical behavior:** `docs/UI_DESIGN.md` had been locally rewritten to a denser native operator-console model and now referenced `docs/UI_RESEARCH.md`, `docs/UI_IMPLEMENTATION_SPEC.md`, `docs/UI_DESIGN_REVIEW.md`, and `docs/UI_DESIGN-old.md`, but those supporting files were still untracked. That left the repo's own source-of-truth chain half-landed: the active design doc pointed at documents that were not actually shipped in Git, and the redesign rationale/spec trail could disappear on the next clean checkout.
- **repo fix:** this run's UI source-of-truth doc commit — add the referenced redesign support docs to Git and keep the refreshed `docs/UI_DESIGN.md` as the committed design source of truth.
- **closure evidence:** repo-side audit plus direct doc correction in the same run; `git status` no longer reports the source-of-truth UI docs as modified/untracked once the commit lands, and the design document's referenced research/spec/review trail is now present in the repo

## ZNE-016 — UI design checklist and 1A execution slice drifted out of the committed source of truth
- **closed on:** 2026-04-15
- **severity:** `medium`
- **area:** `docs`
- **historical behavior:** repo inspection in this run found `docs/UI_DESIGN.md` dirty again. The working tree had already added the new build checklist, implementation-gap analysis, and the explicit `1A` headline-decision-summary execution slice, but HEAD still ended at the older relationship section. That meant the committed source-of-truth design doc was materially behind the current steering and no longer matched the fuller design shape being used for watchdog/supervisor guidance.
- **repo fix:** this run's UI source-of-truth sync commit — commit the pending `docs/UI_DESIGN.md` checklist/gap-analysis/`1A` design sections so the repo-shipped design source matches the current steering.
- **closure evidence:** repo-side audit plus direct doc correction in the same run; after the sync commit, `git diff -- docs/UI_DESIGN.md` is empty and the committed design doc now carries the build checklist, implementation-gap analysis, `1A` priority slice, and `docs/UI_IMPLEMENTATION_SPEC.md` reference.

## ZNE-010 — Repo release metadata drifted behind the documented `0.1.83` UI target
- **closed on:** 2026-04-15
- **severity:** `medium`
- **area:** `release`
- **historical behavior:** `docs/SUPERVISOR.md`, `docs/UI_DESIGN.md`, and `docs/UI_IMPLEMENTATION_MAP.md` all defined `0.1.83` as the active UI release target, but the repo working version still advertised `0.1.82` in `custom_components/zero_net_export/manifest.json`, the top changelog section, and `project_status.md`. That left the current candidate version tracking behind the documented release target and risked another misleading release/deploy handoff.
- **repo fix:** this run's release-metadata alignment commit — bump the repo working version to `0.1.83` in the manifest, make the changelog's active `Unreleased` section explicitly target `0.1.83`, and refresh `project_status.md` so the next action/blocker reflect the real release boundary and the still-installed live `0.1.81` build.
- **closure evidence:** repo-side source-of-truth audit plus direct metadata correction in the same run; `custom_components/zero_net_export/manifest.json`, `CHANGELOG.md`, and `project_status.md` now all point at the active `0.1.83` candidate, while live HA version drift remains tracked separately under `ZNE-004`

## ZNE-009 — Repo-wide unit verification drifted from the current button import contract
- **closed on:** 2026-04-15
- **severity:** `low`
- **area:** `process`
- **historical behavior:** repo-side verification for the managed-devices UI work had been recorded as passing, but a full `python3 -m unittest discover -s tests` run still failed because `tests/test_button_entity_categories.py` stubbed `homeassistant.components.*` modules without creating the `homeassistant` package root now imported by `custom_components/zero_net_export/button.py`
- **repo fix:** this run's test-stub coverage fix commit — restore the test stub's `homeassistant` package hierarchy so the button entity-category test reflects the real import contract and the full repo suite passes again
- **closure evidence:** `python3 -m unittest tests.test_button_entity_categories` passed after the stub fix, followed by `python3 -m unittest discover -s tests` passing repo-wide on 2026-04-15

## ZNE-046 — Button entity-category test loader drifted behind the new managed-snapshot import
- **closed on:** 2026-04-18
- **severity:** `low`
- **area:** `process`
- **historical behavior:** after `33f6138` started importing `DEVICE_KIND_FIXED` and `DEVICE_KIND_VARIABLE` into `custom_components/zero_net_export/button.py`, `tests/test_button_entity_categories.py` still loaded the button module without first providing `custom_components.zero_net_export.device_model`, so the targeted repo verification set started failing with `ImportError: cannot import name 'DEVICE_KIND_FIXED'` before any button assertions ran.
- **repo fix:** this run's test-loader alignment commit — load the real `device_model.py` module inside `tests/test_button_entity_categories.py` before importing `button.py`, so the button test harness matches the current module import contract instead of stubbing an incomplete package graph.
- **closure evidence:** `python3 -m unittest -q tests.test_button_entity_categories tests.test_config_flow_device_runtime_overlay tests.test_candidate_utils tests.test_sensor_entity_categories tests.test_command_center_summary tests.test_translation_sync` passed after the loader fix on 2026-04-18

## ZNE-008 — Source-of-truth UI docs still treated dashboards as a required `0.1.83` deliverable
- **closed on:** 2026-04-15
- **severity:** `medium`
- **area:** `docs`
- **historical behavior:** `docs/UI_DESIGN.md` still said there should be two dashboards built around existing HACS or custom dashboard technologies, and `docs/UI_IMPLEMENTATION_MAP.md` still carried a dashboard-delivery phase as though those dashboards were required for the `0.1.83` UI release. That conflicted with the same docs' native-only operator path, with supervisor steering, and with the project rule that optional dashboards must not displace Configure and Managed Devices work.
- **repo fix:** `863cba8` — clarify dashboards as optional in-native supplemental visibility only, and remove them as a required `0.1.83` implementation phase
- **closure evidence:** repo-side source-of-truth audit plus direct doc correction in the same run; `docs/UI_DESIGN.md` now marks dashboards optional and secondary, and `docs/UI_IMPLEMENTATION_MAP.md` now reframes dashboard work as optional cleanup instead of a required release phase

## ZNE-006 — Deploy helper CLI drift from documented release flow
- **closed on:** 2026-04-15
- **severity:** `medium`
- **area:** `release`
- **historical behavior:** `scripts/deploy_exact_repo_build.py` had rejected flags used by the intended exact-build workflow, including `--expected-commit`, `--require-clean`, and `--require-upstream-sync`
- **repo fix:** `d6c80be` — `scripts/deploy_exact_repo_build.py` now accepts `--expected-commit`, `--require-clean`, and `--require-upstream-sync`, and `tests/test_install_helper_scripts.py` covers matching and failing cases for each guard
- **closure evidence:** this was closed with repo validation because the bug was a local release-helper CLI contract mismatch, not a live Home Assistant runtime defect; `python3 -m unittest tests.test_install_helper_scripts tests.test_release_info_install_guidance` passed on 2026-04-15 after the flag support and test coverage were added

## ZNE-007 — Validation workflow awkward against remote HA environment
- **closed on:** 2026-04-15
- **severity:** `medium`
- **area:** `release`
- **repo fix:** `f3388cf` — `release: add ssh-backed HA fingerprint validation`
- **closure evidence:** repo-side SSH validation now works against the documented HA path without remote Python, and a live run against `root@192.168.86.200:2222` returned the installed component fingerprint from `/homeassistant/custom_components/zero_net_export`

## ZNE-021 — Exact-build fingerprint validation skipped critical startup/provenance files
- **closed on:** 2026-04-16
- **severity:** `medium`
- **area:** `release`
- **historical behavior:** the exact-build validation helpers only tracked `manifest.json`, `config_flow.py`, `native_support.py`, `coordinator.py`, and translation files. That let `scripts/validate_install_fingerprint.py /config/custom_components --ssh-host root@192.168.86.200 --ssh-port 2222` report `overall_match=true` even though important startup/provenance files like `__init__.py` and `release_info.py` were outside the comparison set.
- **repo fix:** this run's validation-coverage fix commit — expand the tracked-file set in `scripts/print_expected_install_fingerprint.py`, `scripts/compare_install_fingerprint.py`, and `custom_components/zero_net_export/release_info.py` to include `__init__.py` and `release_info.py`, with regression coverage in `tests/test_install_helper_scripts.py` and `tests/test_release_info_install_guidance.py`.
- **closure evidence:** in this run, documented HA SSH access on `root@192.168.86.200:2222` succeeded, fresh `ha core logs` review showed the earlier `release_info.py` startup path was a real validation concern, repo-side verification passed with `python3 -m unittest tests.test_install_helper_scripts tests.test_release_info_install_guidance -q`, `python3 -m unittest discover -s tests -q`, and `python3 -m py_compile scripts/print_expected_install_fingerprint.py scripts/compare_install_fingerprint.py scripts/validate_install_fingerprint.py custom_components/zero_net_export/release_info.py`, and a rerun of `python3 scripts/validate_install_fingerprint.py /config/custom_components --ssh-host root@192.168.86.200 --ssh-port 2222` now correctly reports `release_info.py` drift and `overall_match=false` until the latest repo build is redeployed.

## ZNE-023 — Exact-build fingerprint validation skipped active native-UI Python files
- **closed on:** 2026-04-16
- **severity:** `medium`
- **area:** `release`
- **historical behavior:** repo inspection in this run confirmed the exact-build fingerprint helpers still ignored active native-UI Python files like `button.py` and `candidate_utils.py`, even though those files were now ahead of the live Home Assistant install (`button.py` local `36948a15c7a0` vs remote `cba2671c12c1`, `candidate_utils.py` local `b297807df1e4` vs remote `f6899919f5c8`). That meant fingerprint output could under-report real repo-versus-live drift during the `0.1.83` UI release gate.
- **repo fix:** this run's validation-coverage fix commit — expand the tracked-file set in `scripts/print_expected_install_fingerprint.py`, `scripts/compare_install_fingerprint.py`, and `custom_components/zero_net_export/release_info.py` to include `button.py`, `candidate_utils.py`, `diagnostics.py`, and `sensor.py`, with regression coverage in `tests/test_install_helper_scripts.py` and `tests/test_release_info_install_guidance.py`.
- **closure evidence:** `python3 -m unittest tests.test_install_helper_scripts tests.test_release_info_install_guidance -q`, `python3 -m unittest discover -s tests -q`, and `python3 -m py_compile scripts/print_expected_install_fingerprint.py scripts/compare_install_fingerprint.py custom_components/zero_net_export/release_info.py` all passed in this run. Live SSH hash checks also confirmed the previously untracked drift in `button.py` and `candidate_utils.py`, so the widened fingerprint set now covers the missing UI files instead of silently skipping them.

## ZNE-024 — UI implementation map drifted behind the current repo-native UI candidate
- **closed on:** 2026-04-16
- **severity:** `low`
- **area:** `docs`
- **historical behavior:** `docs/UI_IMPLEMENTATION_MAP.md` is the implementation source of truth for `0.1.83`, but it had fallen behind the actual repo candidate after recent native-UI work landed. The status summary and phase sections still described the command center, Managed Devices workspace, candidate review flow, and release gate in older terms that understated delivered repo-side progress and did not clearly surface the current explicit release-approval boundary.
- **repo fix:** this run's implementation-map sync commit — sync `docs/UI_IMPLEMENTATION_MAP.md` so the completed/remaining status and Phases 3, 4, 5, and 7 match the current repo-native UI candidate and state plainly that James must be asked directly for release approval before the next exact-build redeploy.
- **closure evidence:** repo-side source-of-truth audit plus direct doc correction in the same run; `docs/UI_IMPLEMENTATION_MAP.md` now reflects the shipped repo candidate's command-center alert/board work, runtime-aware Managed Devices state, balanced candidate review, device-page deep-review handoff, and the explicit formal release-approval boundary.

## ZNE-025 — Repo release metadata jumped ahead of the source-of-truth UI release gate
- **closed on:** 2026-04-16
- **severity:** `medium`
- **area:** `release`
- **historical behavior:** repo inspection in this run found `custom_components/zero_net_export/manifest.json` and the top `CHANGELOG.md` target bumped to `0.1.84` even though `docs/SUPERVISOR.md`, `docs/UI_DESIGN.md`, and `docs/UI_IMPLEMENTATION_MAP.md` still define `0.1.83` as the active UI release. `project_status.md` also still asked for redeploy approval using an older shipped-component commit, which made the release boundary misleading in two different ways at once.
- **repo fix:** this run's release-metadata sync commit — return the manifest and changelog target to `0.1.83`, add the correction to `CHANGELOG.md`, and remove the stale hard-coded redeploy commit from `project_status.md` so the approval ask points at the exact current shipped-component candidate instead of an outdated hash.
- **closure evidence:** repo-side source-of-truth audit plus direct metadata correction in the same run; `custom_components/zero_net_export/manifest.json` and the changelog target now match the `0.1.83` UI release docs again, and `project_status.md` no longer points release approval at stale commit `f45618c`.

## ZNE-033 — Repo working version drifted forward again to `0.1.85`
- **closed on:** 2026-04-16
- **severity:** `medium`
- **area:** `release`
- **historical behavior:** repo inspection in this run found `custom_components/zero_net_export/manifest.json`, the changelog target, and install-helper regression expectations bumped to `0.1.85` even though `docs/SUPERVISOR.md`, `docs/UI_DESIGN.md`, `docs/UI_IMPLEMENTATION_MAP.md`, `README.md`, and `project_status.md` still define `0.1.83` as the active UI release. That reopened the same release-gate drift in a new form and made exact-build tooling report the wrong candidate version.
- **repo fix:** this run's release-metadata correction commit — return the manifest and changelog target to `0.1.83`, realign `tests/test_install_helper_scripts.py` with the documented release target again, and record the correction in `CHANGELOG.md`.
- **closure evidence:** historical only. That closure was later superseded by `docs/SUPERVISOR.md` and live fingerprint evidence confirming `0.1.85` as the active correction line; current release-metadata reality is tracked under ZNE-034.

## ZNE-039 — README and project status still treated `0.1.86` as the future UI target after the implementation map moved the rollout checklist to `0.1.87`
- **closed on:** 2026-04-16
- **severity:** `low`
- **area:** `docs`
- **historical behavior:** repo inspection in this watchdog run found `README.md` still describing `0.1.86` as the active UI-correction target, and `project_status.md` still framed the next exact-build reconciliation without distinguishing the current live `0.1.86` correction line from the next `0.1.87` UI rollout target. That drift contradicted `docs/UI_IMPLEMENTATION_MAP.md`, which now defines `0.1.87` as the rollout checklist while keeping `0.1.86` as the live correction line.
- **repo fix:** this run's doc-state sync commit — update `README.md` and `project_status.md` so they distinguish the current live `0.1.86` correction line from the next `0.1.87` UI rollout target, and make the release-approval boundary explicitly block any `0.1.87` UI-release claim until the exact `0.1.86` live reconciliation is done.
- **closure evidence:** repo-side source-of-truth audit plus direct doc correction in the same run; `README.md`, `project_status.md`, and `docs/UI_IMPLEMENTATION_MAP.md` now agree that `0.1.86` is the live correction line while `0.1.87` is the next UI rollout target.

## ZNE-041 — UI implementation map briefly regressed the Managed Devices workspace back onto the device page
- **closed on:** 2026-04-17
- **severity:** `low`
- **area:** `docs`
- **historical behavior:** this watchdog run found one remaining source-of-truth contradiction inside `docs/UI_IMPLEMENTATION_MAP.md`. Stage 4 still said to "move the real managed-devices experience onto the device page strongly enough that it does not just look like generic HA cards plus buttons," which directly conflicted with `docs/UI_DESIGN.md`, `docs/SUPERVISOR.md`, and the same implementation map's own `0.1.87` rollout and acceptance text that keep Configure -> Managed Devices as the primary fleet workspace and the device page as the deeper review path.
- **repo fix:** this run's implementation-map correction commit — rewrite the Stage 4 remaining item so it keeps the device page framed as the deeper per-device review path that supports Configure -> Managed Devices, instead of competing with or replacing the main fleet workspace.
- **closure evidence:** repo-side source-of-truth audit plus direct doc correction in the same run; `docs/UI_IMPLEMENTATION_MAP.md` Stage 4 now matches `docs/UI_DESIGN.md`, `docs/SUPERVISOR.md`, and the map's own `0.1.87` rollout/acceptance sections on Configure -> Managed Devices being primary.

## ZNE-043 — Fingerprint guidance still anchored release/deploy decisions to repo HEAD instead of the shipped component commit
- **closed on:** 2026-04-18
- **severity:** `medium`
- **area:** `process`
- **historical behavior:** this watchdog run confirmed the documented HA SSH validation path still reports the same unchanged mixed install, but the repo guidance around fingerprint validation was still telling operators to compare the live package against the generic expected repo commit. That let doc-only or bug-tracker-only repo commits move the apparent release/deploy target even when `custom_components/zero_net_export` itself had not changed, which is the same bookkeeping churn `docs/SUPERVISOR.md` explicitly warns against.
- **repo fix:** this run updates `scripts/print_expected_install_fingerprint.py` to emit `preferred_validation_commit` alongside `expected_component_commit`, adds `--expected-component-commit` to `scripts/deploy_exact_repo_build.py`, and rewrites `README.md` plus `docs/VALIDATION_CHECKLIST.md` so release/deploy checks anchor on the component-changing commit rather than full repo HEAD when doc-only commits are the only delta.
- **closure evidence:** repo-side source-of-truth audit plus direct tooling/doc correction in the same run; validation guidance now distinguishes repo HEAD from the shipped component commit, and deploy tooling can pin the component commit directly so unchanged live fingerprint mismatch is not re-labeled as a new candidate just because docs moved.

## ZNE-044 — Release procedure still used retired `Health/support` bucket wording
- **closed on:** 2026-04-18
- **severity:** `low`
- **area:** `docs`
- **historical behavior:** this watchdog run found one remaining source-of-truth leak in `RELEASE_MANAGEMENT.md`. Step 10 still told operators to verify the installed package fingerprint in `Configure or Health/support`, even though the current native UI source of truth has already renamed that bucket and device-page path to Diagnostics.
- **repo fix:** this run updates `RELEASE_MANAGEMENT.md` so the live verification step now points to `Configure or Diagnostics`, matching `docs/UI_DESIGN.md`, `docs/UI_IMPLEMENTATION_MAP.md`, `README.md`, and `docs/VALIDATION_CHECKLIST.md`.
- **closure evidence:** repo-side source-of-truth audit plus direct doc correction in the same run; the remaining release procedure no longer points operators at retired support-bucket wording during fingerprint validation.

## Closure rule

Do not mark a bug `closed` just because a commit exists.
If the bug affects the user-visible product or live Home Assistant behavior, closure should usually require live validation evidence.
