# Archived UI Design Snapshot for Zero Net Export

> **Archive note:** this file is historical only. It is not a current source of truth and must not be used for active UI steering, release gating, watchdog ranking, implementation planning, or operator instructions.

The current source-of-truth files are:

- `docs/UI_DESIGN.md` for product design and supported native Home Assistant surfaces
- `docs/UI_IMPLEMENTATION_MAP.md` for implementation strategy, delivery status, remaining work, and the active release line
- `docs/BUGS.md` for active bugs, regressions, validation state, and closure state
- `docs/SUPERVISOR.md` for current execution discipline and release-approval behavior

This archived file intentionally no longer repeats the old design snapshot. Keeping the full stale snapshot here was creating source-of-truth drift: it still named older release targets and older section wording even though the active UI line is now the native-only `0.1.91` / release `1.91` integration-main-page Managed Devices and Un Managed device-list scope defined in `docs/UI_IMPLEMENTATION_MAP.md` and `docs/RELEASE_0.1.91_PLAN.md`.

If any older notes, discussions, or commits appear to disagree with the current native Home Assistant UI direction, follow the source-of-truth files above.
