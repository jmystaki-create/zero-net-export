# 0.3.3 Release Validation

Date: 2026-07-07

Status: live validated for Milestone 6 service/action checks

## Summary

Zero Net Export `v0.3.3` was verified as installed through HACS and rendered successfully in the live Home Assistant app. This release supersedes `v0.3.1-fix` and includes the diagnostics export fixes recorded in `CHANGELOG.md`.

## Evidence

- HACS repository page showed Zero Net Export downloaded at `v0.3.3`.
- Zero Net Export sidebar app opened successfully in Home Assistant.
- App header showed `Version 0.3.3 - 1 plan`.
- Backend status showed `Connected to Home Assistant`.
- Overview showed readiness `ready`, safe mode `off`, source mismatch `off`, executor `running`.
- Diagnostics tab opened and rendered.
- Diagnostics showed:
  - Installed: `0.3.3`
  - Update summary: `Updated from 0.3.1 to 0.3.3`
  - Safe mode: `off`
  - Stale data: `off`
  - Command failure: `off`
  - Recent Logs: `No log entries available`

## Service And Action Validation

Performed on 2026-07-07 using the Home Assistant REST API with the stored validation token.

- `/api/services` reported `zero_net_export.export_diagnostics` registered.
- `/api/services` reported `zero_net_export.repair_issue` registered.
- `POST /api/services/zero_net_export/export_diagnostics` returned successfully.
- `POST /api/services/zero_net_export/repair_issue` returned successfully.
- `sensor.zero_net_export_installed_version = 0.3.3`.
- `sensor.zero_net_export_previous_installed_version = 0.3.1`.
- `sensor.zero_net_export_health_status = healthy`.
- `sensor.zero_net_export_status = degraded`.
- `sensor.zero_net_export_diagnostic_summary = Power sources do not reconcile cleanly; home load reads about 1100 W higher than the other flows imply`.

## Export File Evidence

The export service created a new diagnostics file:

- `/config/zne-diagnostics-20260707-192929.json`

The file parsed as JSON and contained:

- top-level keys: `config_entry`, `executor_paused`, `last_action_timestamp`, `log_buffer`, `reconciliation_history`, `sensor_states`
- `sensor_states_count = 6`
- `log_buffer_count = 0`
- `reconciliation_history_count = 0`
- `executor_paused = False`
- `config_entry_state = ConfigEntryState.LOADED`

## Log Review

`ha core logs -n 80` was checked after the service calls. The recent window did not show a new `export_diagnostics` or `repair_issue` exception from the 19:29 service call.

Observed non-blocking or unrelated items:

- Historical pre-`v0.3.3` `NameError: name '_LOGGER' is not defined` entries from earlier `15:41`-`15:43` export attempts.
- Standard custom-integration warnings for several integrations, including Zero Net Export.
- `sensor.managed_devices_unmanaged_backlog` took 2.056 seconds to update at `19:26`; track separately if repeated.
- Current runtime status remains `degraded` due to source reconciliation, not due to diagnostics service failure.

## Result

Milestone 6 is live validated for the released `v0.3.3` service/action scope. Remaining work is product/runtime readiness, starting with the next app milestone.
