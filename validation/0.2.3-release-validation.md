# Zero Net Export 0.2.3 release validation

Date: 2026-07-01
Status: GitHub Release published; HACS install live-validated

## Scope

Feature release `0.2.3` covers the `ZNE-APP-002` app-native Sources workflow:

- Home Assistant app Sources workflow rows for source roles with binding,
  status, reading, age, and issue detail.
- Scoped backend service saves for app source-role bindings.
- Sources workflow milestone planning, feasibility, and repo validation records.

All live install work used the approved GitHub/HACS path. No direct Home
Assistant custom-component file backend deployment was used.

## Repo validation

Passed before release commit/tag:

- `node --check custom_components/zero_net_export/frontend/zero-net-export-app.js`
- `python3 -m py_compile custom_components/zero_net_export/__init__.py`
- Focused tests:
  `python3 -m unittest -q tests.test_managed_devices_panel tests.test_config_flow_device_runtime_overlay tests.test_bucket_ownership_copy tests.test_source_repair_guidance tests.test_setup_notice_copy tests.test_install_helper_scripts`
  - `Ran 183 tests`
  - `OK`
- Full discovery:
  `python3 -m unittest discover -s tests`
  - `Ran 618 tests`
  - `OK`
  - Output includes expected negative-path `ERROR:` lines from release/deploy
    guard tests.
- `git diff --check`

## GitHub / HACS path

- Release prep commit: `d619a00 release: prepare zero net export 0.2.3`
- Pushed `main` to GitHub.
- Created and pushed tag `v0.2.3`.
- Remote `main` and tag `v0.2.3` both report commit
  `d619a000afa36cd4d9ad7318c061e92e9e4c83a6`.
- GitHub Release `v0.2.3` published:
  `https://github.com/jmystaki-create/zero-net-export/releases/tag/v0.2.3`
- GitHub latest release API reports `tag_name=v0.2.3`, `draft=false`,
  `prerelease=false`, and `target_commitish=main`.

HACS install:

- HACS repository refresh succeeded through websocket command
  `hacs/repository/refresh` with repository `1189518522`.
- HACS explicit tag download succeeded through websocket command
  `hacs/repository/download` with repository `1189518522` and version
  `v0.2.3`.
- `update.zero_net_export_update` reported `installed_version=v0.2.3`,
  `latest_version=v0.2.3`, and restart required.

## Install fingerprint and restart

Expected fingerprint:

- `tmp/expected-install-fingerprint-v0.2.3.json`
- `manifest_version=0.2.3`
- `repo_head_commit=d619a00`
- `expected_component_commit=d619a00`

Pre-restart install fingerprint:

- `tmp/install-fingerprint-compare-v0.2.3-before-restart.json`
- `overall_match=true`
- `manifest_version_matches=true`
- all tracked files matched
- no legacy artifacts found

Restart:

- Restart requested through Home Assistant API.
- Restart request returned a timeout while Home Assistant was going down.
- API recovered at poll attempt 5 with `{"message":"API running."}`.

Post-restart install fingerprint:

- `tmp/install-fingerprint-compare-v0.2.3-after-restart.json`
- `overall_match=true`
- `manifest_version_matches=true`
- all tracked files matched
- no legacy artifacts found

Post-restart state/API proof:

- `sensor.zero_net_export_installed_version=0.2.3`
- `update.zero_net_export_update.installed_version=v0.2.3`
- `update.zero_net_export_update.latest_version=v0.2.3`
- `124` Zero Net Export sensor/binary/update state entities were present in
  the Home Assistant state API snapshot.
- `/zero-net-export` returned HTTP `200`.
- `/zero_net_export_static/zero-net-export-app.js` returned HTTP `200` with
  `31507` bytes.
- HACS metadata refresh after restart succeeded and still reported
  installed/latest `v0.2.3`.
- Targeted HA CLI and container log scans found no Zero Net Export
  traceback/error/warning lines in the tailed post-restart logs.

## Remaining validation notes

- Browser screenshot proof for the Sources tab was not captured in this run
  because the available Slave browser node did not expose a working navigation
  path through the simple node invocation shape.
- Reversible live source-role write proof was not performed because no
  disposable/safe source mapping was separately identified for mutation during
  the release run.

## Conclusion

The installed Home Assistant runtime is live-validated on `0.2.3` through
GitHub Release publication, HACS download, restart, fingerprint, app route,
static asset, state, HACS metadata, and targeted log checks.
