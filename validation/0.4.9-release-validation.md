# Zero Net Export 0.4.9 Release Validation

Date: 2026-07-13

## Scope

Release `v0.4.9` ships the Managed Devices layout/order polish requested in `ZNE-FR-015` and `ZNE-FR-016`.

## GitHub Release

- Commit: `e01a6d5927d2ad4cbcea6da37caf4fc99a7d3630`
- Tag: `v0.4.9`
- Release: `https://github.com/jmystaki-create/zero-net-export/releases/tag/v0.4.9`
- GitHub latest-release API returned `tag_name=v0.4.9`, `draft=false`, `prerelease=false`, and the expected target commit.

## Repo Validation

- `node --check custom_components/zero_net_export/frontend/zero-net-export-app.js`
  - Result: PASS
- `python3 -m unittest -v tests.test_managed_devices_panel`
  - Result: PASS, 22 tests
- `python3 -m unittest -v tests.test_install_helper_scripts tests.test_release_info_install_guidance tests.test_managed_devices_panel`
  - Result: PASS, 84 tests
- `python3 -m unittest discover -v`
  - Result: PASS, 636 tests
- `git diff --check`
  - Result: PASS

## HACS / Home Assistant Install

- HACS `Update information` was triggered from the Zero Net Export repository page in the Slave browser.
- `update.zero_net_export_update` then reported installed `v0.4.8`, latest `v0.4.9`, state `on`.
- `update.install` was called for `update.zero_net_export_update` with `version=v0.4.9`.
- After install, `update.zero_net_export_update` reported installed/latest `v0.4.9`.
- Install fingerprint validation over SSH passed:
  - Command: `python3 scripts/validate_install_fingerprint.py /config/custom_components --ssh-host root@192.168.86.200 --ssh-port 2222 --expected-json tmp/expected-install-fingerprint.json --compare-json tmp/install-fingerprint-compare.json`
  - Result: `overall_match=true`
  - Manifest version: `0.4.9`
  - Expected component commit: `e01a6d5`
  - No legacy backup paths, nested component paths, or stale bytecode paths were found.

## Restart / Runtime Validation

- Home Assistant restart was requested through `homeassistant.restart`.
- The request returned HTTP 504 while Home Assistant was dropping for restart; API polling then showed connection refused followed by API recovery.
- Post-restart API state:
  - `sensor.zero_net_export_installed_version=0.4.9`
  - `update.zero_net_export_update` installed/latest `v0.4.9`
  - 143 Zero Net Export entities visible through `/api/states`
- Post-restart log review through `ha core logs --lines 300` found known recorder attribute-size warnings for Zero Net Export entities, but no Zero Net Export traceback or new project-specific release error was found.

## Slave Browser UI Proof

Browser target: `http://192.168.86.200:8123/zero-net-export?validation=v049-managed-devices-layout-order-20260713`

Desktop DOM proof:

- Header: `Version 0.4.9 · 1 plan`
- Fleet Summary uses `.zne-fleet-summary-stats`.
- Fleet Summary stat items rendered as separate count/label text:
  - `1 Total`
  - `1 Enabled`
  - `0 Disabled`
  - `0 Blocked`
  - `0 Stale`
- Each Fleet Summary `<strong>` count had `margin-right: 4px`.
- Heading positions showed `Fleet List (1 devices)` at top `655` and `Unmanaged Candidate Queue (29 candidates)` at top `930`.
- Computed check: `fleetListBeforeUnmanaged=true`.

Narrow viewport proof (`390x844`):

- Fleet Summary stat items retained separate text and `strongMarginRight=4px`.
- Computed check: `fleetListBeforeUnmanaged=true`.

## Decision

`v0.4.9` is released, installed through HACS, restarted, and live validated for `ZNE-FR-015` and `ZNE-FR-016`.

## Remaining Risk

Known recorder attribute-size warnings remain present and are tracked separately under `ZNE-595`.
