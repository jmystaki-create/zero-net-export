# Zero Net Export 0.1.102 corrective release validation

Date: 2026-05-01
Status: released and live-validated.

## Scope validated

Corrective release for the `0.1.101` ZNE-FR-007 visible Tier 1/Tier 2 release miss.

- Adds visible Tier 1 native Home Assistant device-page launcher buttons:
  - Open Sensors setup
  - Open Controls setup
  - Open Managed Devices setup
  - Open Diagnostics setup
- Each Tier 1 launcher points users toward the corresponding Tier 2 native Home Assistant Configure/options flow section.
- Curates the primary Tier 1 Sensors card to the accepted summary set and keeps detailed telemetry in Diagnostic surfaces.
- Preserves existing Diagnostic and Connected Devices surfaces.

## Publish evidence

```text
gh release view v0.1.102 --repo jmystaki-create/zero-net-export --json tagName,name,url,publishedAt,targetCommitish,isDraft,isPrerelease | jq .
{
  "isDraft": false,
  "isPrerelease": false,
  "name": "0.1.102",
  "publishedAt": "2026-05-01T05:59:53Z",
  "tagName": "v0.1.102",
  "targetCommitish": "main",
  "url": "https://github.com/jmystaki-create/zero-net-export/releases/tag/v0.1.102"
}
```

## Install/fingerprint evidence

Home Assistant target: `http://192.168.86.200:8123`.
Install path inspected over SSH port `2222`: `/homeassistant/custom_components/zero_net_export`.

The installed component fingerprint matches the intended repository build:

```text
jq '.comparison | {overall_match, all_tracked_files_match, manifest_version_matches, mismatches, legacy_artifacts}' tmp/install-fingerprint-compare-0.1.102-post-hacs.json
{
  "overall_match": true,
  "all_tracked_files_match": true,
  "manifest_version_matches": true,
  "mismatches": [],
  "legacy_artifacts": {
    "legacy_backup_paths": [],
    "legacy_component_child_paths": [],
    "stale_bytecode_paths": []
  }
}
```

Runtime version evidence:

```text
sensor.zero_net_export_installed_version = 0.1.102
```

The expected new button code is present in the installed release and runtime registry/state. The earlier apparent miss was caused by filtering only `button.zero_net_export*`; Home Assistant names entities from the config-entry device, so the visible buttons are device-named, for example `button.winter_plan_open_sensors_setup`.

Runtime state evidence after restart:

```text
button.winter_plan_open_sensors_setup FOUND
Winter Plan Open Sensors setup | Tier 1 launcher | Tier 2 native Home Assistant flow | Settings -> Devices & Services -> Integrations -> Zero Net Export -> Configure -> Sensors

button.winter_plan_open_controls_setup FOUND
Winter Plan Open Controls setup | Tier 1 launcher | Tier 2 native Home Assistant flow | Settings -> Devices & Services -> Integrations -> Zero Net Export -> Configure -> Controls

button.winter_plan_open_managed_devices_setup FOUND
Winter Plan Open Managed Devices setup | Tier 1 launcher | Tier 2 native Home Assistant flow | Settings -> Devices & Services -> Integrations -> Zero Net Export -> Configure -> Managed Devices

button.winter_plan_open_diagnostics_setup FOUND
Winter Plan Open Diagnostics setup | Tier 1 launcher | Tier 2 native Home Assistant flow | Settings -> Devices & Services -> Integrations -> Zero Net Export -> Configure -> Diagnostics; device-page diagnostics: Settings -> Devices & Services -> Integrations -> Zero Net Export -> Devices -> open the Zero Net Export device -> Review diagnostics / Show setup checklist / Review diagnostics snapshot; Settings -> Repairs
```

## Browser/UI evidence

Headless Chromium loaded the actual Home Assistant device page with frontend auth and captured browser evidence:

- `bug-evidence/zne-0.1.102-device-page.png`
- `bug-evidence/zne-0.1.102-device-page.json`

```text
url: http://192.168.86.200:8123/config/devices/device/707b7945a3b230696da9de0e013af875
title: Devices – Home Assistant
required text present:
  Open Sensors setup: true
  Open Controls setup: true
  Open Managed Devices setup: true
  Open Diagnostics setup: true
  Review diagnostics: true
  Connected devices: true
  Diagnostic: true
```

Accessible browser tree matches included:

```text
heading: Diagnostic
heading: Connected devices
StaticText: Open Controls setup
StaticText: Open Diagnostics setup
StaticText: Open Managed Devices setup
StaticText: Open Sensors setup
StaticText: Review diagnostics
button: Winter Plan Review diagnostics snapshot
button: Winter Plan Review diagnostics
button: Winter Plan Open Diagnostics setup
button: Winter Plan Open Managed Devices setup
button: Winter Plan Open Controls setup
button: Winter Plan Open Sensors setup
```

The browser tree also showed the accepted Tier 1 summary sensor labels, including:

```text
Actions today
Active controlled power
Battery reserve SOC
Battery state of charge
Health status
Status
```

Detailed telemetry remains available under Diagnostic surfaces, as expected.

## Repo validation evidence

Commands run from `/root/.openclaw/workspace/projects/zero-net-export`.

```text
python3 -m py_compile custom_components/zero_net_export/config_flow.py custom_components/zero_net_export/button.py custom_components/zero_net_export/sensor.py custom_components/zero_net_export/binary_sensor.py
# passed
```

```text
python3 -m unittest -q tests.test_button_entity_categories tests.test_sensor_entity_categories tests.test_device_surface_version tests.test_install_helper_scripts
----------------------------------------------------------------------
Ran 133 tests in 0.979s

OK
```

```text
python3 -m unittest -q tests.test_release_info_install_guidance
----------------------------------------------------------------------
Ran 34 tests in 0.047s

OK
```

```text
python3 -m unittest discover -s tests
----------------------------------------------------------------------
Ran 606 tests in 1.638s

OK
```

Additional focused pytest gate run during live validation:

```text
tmp/validation-venv/bin/python -m pytest tests/test_button_entity_categories.py tests/test_sensor_entity_categories.py tests/test_install_helper_scripts.py tests/test_release_info_install_guidance.py tests/test_release_update_details.py -q
169 passed in 1.22s
```

```text
git diff --check
# clean
```

## Home Assistant logs

`ha core logs` was used instead of `/config/home-assistant.log`.

Latest targeted Zero Net Export log scan after restart:

```text
ha core logs -n 120 | grep -i 'custom_components.zero_net_export\|zero_net_export'
2026-05-01 16:10:41.314 WARNING (SyncWorker_0) [homeassistant.loader] We found a custom integration zero_net_export which has not been tested by Home Assistant. This component might cause stability problems, be sure to disable it if you experience issues with Home Assistant
```

No current `custom_components.zero_net_export` setup/platform traceback was found in the latest targeted scan. Earlier HACS API install attempts logged `The version 0.1.102 for this integration can not be used with HACS`, but the final installed fingerprint, running manifest/code version, entity state, restart, and browser evidence confirm the live instance is running the intended `0.1.102` build.

## Result

Release `0.1.102` is published, installed on the Home Assistant target, restarted, fingerprint-matched, entity-state validated, browser/UI validated, and repo-regression validated.

ZNE-FR-007 corrective release status: **released_live_validated**.
