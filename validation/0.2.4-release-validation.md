# Zero Net Export 0.2.4 release validation

Date: 2026-07-01
Status: GitHub Release published; HACS install live-validated

## Scope

Corrective release `0.2.4` covers ZNE-594:

- Cap `sensor.zero_net_export_source_blocker_next_step` and
  `sensor.zero_net_export_command_center_next_step` state values at Home
  Assistant's 255-character state limit.
- Preserve full next-step guidance in entity attributes.

All live install work must use the approved GitHub/HACS path. No direct Home
Assistant custom-component file backend deployment is allowed.

## Repo validation

Passed before release commit/tag:

- `node --check custom_components/zero_net_export/frontend/zero-net-export-app.js`
- `python3 -m py_compile custom_components/zero_net_export/sensor.py custom_components/zero_net_export/__init__.py tests/test_sensor_entity_categories.py tests/test_release_info_install_guidance.py`
- Focused tests:
  `python3 -m unittest -q tests.test_sensor_entity_categories tests.test_command_center_summary tests.test_managed_devices_panel tests.test_install_helper_scripts tests.test_release_info_install_guidance`
  - `Ran 295 tests`
  - `OK`
  - Output includes expected negative-path `ERROR:` lines from release/deploy
    guard tests.
- Full discovery:
  `python3 -m unittest discover -s tests`
  - `Ran 620 tests`
  - `OK`
  - Output includes expected negative-path `ERROR:` lines from release/deploy
    guard tests.
- `git diff --check`

## GitHub / HACS path

- Release prep commit: `63c2568 release: prepare zero net export 0.2.4`
- Pushed `main` to GitHub.
- Created and pushed tag `v0.2.4`.
- Remote `main` and tag `v0.2.4` both report commit
  `63c25682a8a54dc90898797a16fae07300446275`.
- GitHub Release `v0.2.4` published:
  `https://github.com/jmystaki-create/zero-net-export/releases/tag/v0.2.4`
- GitHub release reports `tagName=v0.2.4`, `draft=false`,
  `prerelease=false`, and `targetCommitish=main`.

HACS install:

- HACS repository info reported `available_version=v0.2.4` and
  `installed_version=v0.2.3` before download.
- HACS repository refresh succeeded through websocket command
  `hacs/repository/refresh` with repository `1189518522`.
- HACS explicit tag download succeeded through websocket command
  `hacs/repository/download` with repository `1189518522` and version
  `v0.2.4`.
- `update.zero_net_export_update` reported `installed_version=v0.2.4` and
  `latest_version=v0.2.4` after download.

## Install fingerprint and restart

Pre-HACS install fingerprint:

- `tmp/install-fingerprint-compare-v0.2.4-before-hacs.json`
- `overall_match=false`
- live manifest was still `0.2.3`
- mismatches were limited to `manifest.json` and `sensor.py`, matching the
  intended corrective release scope.

Pre-restart install fingerprint after HACS download:

- `tmp/install-fingerprint-compare-v0.2.4-before-restart.json`
- `overall_match=true`
- `manifest_version_matches=true`
- `manifest_version=0.2.4`
- `repo_head_commit=63c2568`
- `expected_component_commit=63c2568`
- all tracked files matched
- no legacy artifacts found

Restart:

- Restart requested through Home Assistant API at
  `2026-07-01T11:02:14+10:00`.
- Restart request returned a timeout while Home Assistant was going down.
- API recovered at poll attempt 15 with `{"message":"API running."}`.

Post-restart install fingerprint:

- `tmp/install-fingerprint-compare-v0.2.4-after-restart.json`
- `overall_match=true`
- `manifest_version_matches=true`
- `manifest_version=0.2.4`
- `repo_head_commit=63c2568`
- `expected_component_commit=63c2568`
- all tracked files matched
- no legacy artifacts found

Post-restart state/API proof:

- `sensor.zero_net_export_installed_version=0.2.4`
- `update.zero_net_export_update.installed_version=v0.2.4`
- `update.zero_net_export_update.latest_version=v0.2.4`
- `124` Zero Net Export sensor/binary/update state entities were present in
  the Home Assistant state API snapshot.
- `sensor.zero_net_export_source_blocker_next_step` state length was `156`.
- `sensor.zero_net_export_command_center_next_step` state length was `147`;
  its `current_next_step` attribute was also present at length `147`.
- `/zero-net-export` returned HTTP `200`.
- `/zero_net_export_static/zero-net-export-app.js` returned HTTP `200` with
  `31507` bytes.

Post-restart log proof:

- `tmp/ha-core-logs-v0.2.4-after-restart-tail350.txt`
- `tmp/ha-core-logs-v0.2.4-post-restart-zne-scan.txt`
- Post-restart scan found `0` Zero Net Export
  `sensor.zero_net_export_*_next_step is longer than 255` errors.
- The only post-restart Zero Net Export log line in the filtered scan was
  Home Assistant's standard custom-integration warning.

## Remaining validation notes

- Browser screenshot proof for the installed Sources workflow remains pending
  from `0.2.3`; it is not part of the ZNE-594 corrective fix.

## Conclusion

The installed Home Assistant runtime is live-validated on `0.2.4` through
GitHub Release publication, HACS download, restart, fingerprint, app route,
static asset, state, HACS metadata, and targeted log checks. ZNE-594 is
released and live-validated fixed.
