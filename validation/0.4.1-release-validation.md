# Zero Net Export v0.4.1 Release Validation

Date: 2026-07-08
Status: released and live validated

## Release

- Version: `v0.4.1`
- Commit: `f985ad3`
- Tag: `v0.4.1`
- GitHub release: https://github.com/jmystaki-create/zero-net-export/releases/tag/v0.4.1
- Scope: corrective ZNE-596 fix for the Sources app Battery state of charge row.

## Repo Validation

Passed:

```text
python3 -m unittest discover -s tests -q
Ran 627 tests in 1.762s
OK
```

Passed:

```text
node --check custom_components/zero_net_export/frontend/zero-net-export-app.js
```

Passed:

```text
python3 -m py_compile $(find custom_components/zero_net_export -name '*.py' -print)
```

Passed:

```text
git diff --check
```

## GitHub / HACS

- GitHub release list showed `v0.4.1` as `Latest`.
- HACS row for Zero Net Export was refreshed with **Update information**.
- `update.zero_net_export_update` changed from installed/latest `v0.4.0` to update available with latest `v0.4.1`.
- Home Assistant `update.install` installed `v0.4.1`.
- Post-install update entity reported installed/latest `v0.4.1`, `in_progress=false`.

## Install Fingerprint

Before restart and again after restart:

```text
python3 scripts/validate_install_fingerprint.py --ssh-host root@192.168.86.200 --ssh-port 2222 /config/custom_components/zero_net_export
overall_match=True
manifest=0.4.1
expected_commit=f985ad3
```

The validator reported all tracked files matched and no legacy discovery artifacts were found.

## Home Assistant Restart

- Home Assistant restart was requested through `homeassistant.restart`.
- API returned to `200` after restart.
- Zero Net Export entities registered after startup.

## Live Validation

Post-restart API proof:

```text
sensor.zero_net_export_installed_version = 0.4.1
update.zero_net_export_update installed_version = v0.4.1
update.zero_net_export_update latest_version = v0.4.1
sensor.zero_net_export_battery_state_of_charge_status = ok
sensor.zero_net_export_battery_state_of_charge_status binding = sensor.x1_p6k_us_s_state_of_charge
sensor.zero_net_export_battery_state_of_charge_status value = 61.0
binary_sensor.zero_net_export_battery_state_of_charge_stale = off
sensor.x1_p6k_us_s_state_of_charge = 61 %
```

Browser proof from the installed app:

```text
Zero Net Export app header: Version 0.4.1 - 1 plan
Sources tab: Battery state of charge
status: ok
Binding: sensor.x1_p6k_us_s_state_of_charge
Reading: 62.0 | Age: unknown s | Issues: 0
```

## Log Review

Home Assistant `/api/error_log` post-restart checks found:

```text
Zero Net Export log matches: none
Recorder attribute warnings: none
```

## Result

ZNE-596 is released and live validated in `v0.4.1`.

Remaining known follow-up:

- ZNE-595 remains open for broader recorder-backed attribute-size cleanup.
