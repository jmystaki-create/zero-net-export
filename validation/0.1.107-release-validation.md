# Zero Net Export 0.1.107 release validation

Date: 2026-05-06
Status: passed

## Release candidate

- Version: `0.1.107`
- Release commit: `dbb9bf2`
- ZNE-590 implementation commit: `c12d91b`
- GitHub release: https://github.com/jmystaki-create/zero-net-export/releases/tag/v0.1.107

## Repo validation

Command:

```sh
python3 -m py_compile custom_components/zero_net_export/*.py && python3 -m unittest discover -s tests && git diff --check
```

Result:

- `py_compile` passed.
- `python3 -m unittest discover -s tests` passed: `Ran 607 tests`, `OK`.
- `git diff --check` passed.

## GitHub / release visibility

- `v0.1.107` tag pushed to GitHub.
- GitHub release published as non-draft/non-prerelease.
- `gh release list` showed `Zero Net Export v0.1.107` as `Latest`.
- Remote `main` and tag `refs/tags/v0.1.107` both resolved to `dbb9bf26859fbeac90e74019a53fdc651024f9f8`.

## Home Assistant install

- Home Assistant update entity: `update.zero_net_export_update`.
- Install invoked through Home Assistant update service with target version `v0.1.107`.
- Update entity progressed to installed/latest `v0.1.107` and reported restart required.

## Install fingerprint

Pre-restart fingerprint:

```sh
python3 scripts/validate_install_fingerprint.py /config/custom_components --ssh-host root@192.168.86.200 --ssh-port 2222 --expected-json tmp/expected-install-fingerprint-v0.1.107.json --compare-json tmp/install-fingerprint-compare-v0.1.107-pre-restart.json
```

Result:

- `overall_match=true`
- `manifest_version=0.1.107`
- all tracked files matched
- no legacy artifacts found

Post-restart fingerprint:

```sh
python3 scripts/validate_install_fingerprint.py /config/custom_components --ssh-host root@192.168.86.200 --ssh-port 2222 --expected-json tmp/expected-install-fingerprint-v0.1.107-post-restart.json --compare-json tmp/install-fingerprint-compare-v0.1.107-post-restart.json
```

Result:

- `overall_match=true`
- `manifest_version=0.1.107`
- all tracked files matched
- no legacy artifacts found

## Home Assistant restart/API recovery

- Restart requested through `homeassistant.restart` service.
- API recovered successfully: `{"message":"API running."}` after 5 polling attempts.

## Post-restart state

- `update.zero_net_export_update`: installed `v0.1.107`, latest `v0.1.107`, state `off`.
- `sensor.zero_net_export_installed_version`: `0.1.107`.

State evidence:

- `tmp/ha_states_post_restart_v0.1.107.json`
- `tmp/ha_update_state_v0.1.107.json`
- `tmp/ha_installed_version_v0.1.107.json`

## Post-restart logs

Checked Home Assistant logs over SSH:

```sh
ssh -p 2222 root@192.168.86.200 'for p in /homeassistant/home-assistant.log /config/home-assistant.log; do echo LOGPATH:$p; if test -f "$p"; then tail -n 700 "$p" | grep -iE "zero_net_export|zero net export|custom_components.zero_net_export|Traceback|ERROR|WARNING" || true; fi; done'
```

Result:

- `/homeassistant/home-assistant.log` existed.
- No Zero Net Export tracebacks, errors, or warnings were found in the checked post-restart tail.
- Evidence: `tmp/ha_zero_net_export_log_tail_v0.1.107.txt`.

## Browser proof

Managed-load page:

- URL: `http://192.168.86.200:8123/config/devices/device/f4a50db1640e3aeb8bc77dcb222700bd`
- Screenshot: `validation/artifacts/zne-590-managed-climate-device-v0.1.107.png`
- Extracted proof: `validation/artifacts/zne-590-managed-climate-device-v0.1.107.json`
- Vision review confirmed the page shows `Heated Floor - Lounge Room`, clean ZNE rows including `Priority`, `Test load`, `Zero Net Export enabled`, `Zero Net Export status`, configuration/current-power style rows, and no visible confusing `Settings — Test ...` rows.
- Accessibility/text extraction confirmed:
  - `Heated Floor - Lounge Room`: present
  - `Zero Net Export enabled`: present
  - `Test load`: present
  - `Priority`: present
  - `Zero Net Export configuration`: present
  - `Zero Net Export status`: present
  - `Current power`: present
  - `Review Zero Net Export configuration`: present
  - `Reset Zero Net Export overrides`: present
  - `Settings — Test current power`: absent
  - `Settings — Test enabled`: absent

Original thermostat page:

- URL: `http://192.168.86.200:8123/config/devices/device/d22826346da4887292ddc8031478e88d`
- Screenshot: `validation/artifacts/zne-590-original-thermostat-device-v0.1.107.png`
- Extracted proof: `validation/artifacts/zne-590-original-thermostat-device-v0.1.107.json`
- Vision review confirmed the original `Lounge Room` Tuya/Tuya Local thermostat page still shows thermostat controls and standard configuration rows, without Zero Net Export managed-load controls.
- Accessibility/text extraction confirmed the thermostat page does not expose:
  - `Zero Net Export enabled`
  - `Test load`
  - `Priority`
  - `Zero Net Export configuration`
  - `Zero Net Export status`
  - `Current power`
  - confusing `Settings — Test ...` rows

## Closure decision

Release validation passed. ZNE-590 is live-validated fixed in `0.1.107`.
