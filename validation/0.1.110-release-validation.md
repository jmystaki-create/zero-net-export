# Zero Net Export 0.1.110 release validation

Date: 2026-05-07
Status: blocked before HACS/Home Assistant install; GitHub Release object not published/available to this agent

## Release candidate

- Version: `0.1.110`
- Release prep commit: `958d82e520694126e47c854acbfa50d61dba16d9`
- Feature implementation commit: `5c0a13a`
- Tag: `v0.1.110`

## Completed

- Manifest bumped to `0.1.110`.
- Changelog moved ZNE-FR-009/ZNE-FR-010 entries into `## [0.1.110] - 2026-05-07`.
- Release notes drafted locally at `tmp/release-v0.1.110.md`.
- Release prep committed with `release: prepare 0.1.110`.
- Tag `v0.1.110` created.
- `main` and tag `v0.1.110` pushed to `origin`.
- Remote verification showed:
  - `refs/heads/main` -> `958d82e520694126e47c854acbfa50d61dba16d9`
  - `refs/tags/v0.1.110` -> `958d82e520694126e47c854acbfa50d61dba16d9`

## Repo validation evidence

Commands run from `/root/.openclaw/workspace/projects/zero-net-export`:

```bash
python3 -m unittest -q tests.test_release_update_details tests.test_release_info_install_guidance tests.test_install_helper_scripts tests.test_config_flow_device_runtime_overlay tests.test_managed_devices_panel
python3 -m unittest discover -s tests
python3 -m py_compile custom_components/zero_net_export/__init__.py custom_components/zero_net_export/config_flow.py
git diff --check
```

Results:

- Focused release/feature tests: `Ran 162 tests`, `OK`.
- Full discovery: `Ran 610 tests`, `OK`.
- `py_compile` completed without errors for changed component files.
- `git diff --check` completed without whitespace errors.

Expected negative test output remains present inside the test stream for release/deploy guard tests; those tests intentionally print ERROR lines while asserting guard failures and the suites still finished `OK`.

## Blocking gate

The release-management procedure requires verifying that the intended version is visible on GitHub as a published release before touching HACS or Home Assistant.

Evidence:

```bash
gh release view v0.1.110 --repo jmystaki-create/zero-net-export
```

returned:

```text
To get started with GitHub CLI, please run:  gh auth login
Alternatively, populate the GH_TOKEN environment variable with a GitHub API authentication token.
```

Public GitHub API check:

```text
https://api.github.com/repos/jmystaki-create/zero-net-export/releases/tags/v0.1.110
```

returned `404 Not Found`, meaning the tag exists but the GitHub Release object is not published or not visible.

## Pending

- Publish GitHub Release `v0.1.110` using `tmp/release-v0.1.110.md` notes, or provide a usable `GH_TOKEN`/authenticated `gh` session so this agent can publish it.
- After GitHub Release publication is verified:
  1. Refresh HACS metadata for Zero Net Export.
  2. Wait for HACS to show `0.1.110`.
  3. Upgrade/install via HACS.
  4. Compare installed package fingerprint against repo build.
  5. Restart Home Assistant.
  6. Review Zero Net Export-specific logs.
  7. Browser-validate native managed-load Delete and native reconfigure/edit form.
