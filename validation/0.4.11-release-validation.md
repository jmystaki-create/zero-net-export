# Zero Net Export 0.4.11 Release Validation

Date: 2026-07-13
Status: released/live-validated

## Scope

Corrective release for `ZNE-598`: Managed Devices `Review & promote` looked like
a no-op because the review workflow was rendered below the unmanaged candidate
table.

## GitHub Release

- Release: `https://github.com/jmystaki-create/zero-net-export/releases/tag/v0.4.11`
- Tag: `v0.4.11`
- Release commit: `e82670c`
- GitHub latest-release API returned `tag_name=v0.4.11`, `draft=false`,
  `prerelease=false`.

## Source Validation

Passed before release publication:

- `node --check custom_components/zero_net_export/frontend/zero-net-export-app.js`
- `python3 -m py_compile custom_components/zero_net_export/__init__.py custom_components/zero_net_export/const.py custom_components/zero_net_export/release_info.py`
- `python3 -m unittest -v tests.test_managed_devices_panel tests.test_install_helper_scripts`
  - Result: PASS, 51 tests
- `python3 -m unittest discover -v`
  - Result: PASS, 637 tests
- `git diff --check`

## HACS / Home Assistant Install

- Before install, `update.zero_net_export_update` reported installed/latest
  `v0.4.10`.
- GitHub release `v0.4.11` was published and verified as latest.
- Home Assistant `update.install` was called for
  `update.zero_net_export_update` with `version=v0.4.11`.
- HACS installed `v0.4.11`.
- Post-install update entity reported:
  - `installed_version=v0.4.11`
  - `latest_version=v0.4.11`
  - `in_progress=false`

## Install Fingerprint

Expected release fingerprint:

- `expected_commit=e82670c`
- `expected_component_commit=e82670c`
- `manifest_version=0.4.11`

Before restart:

- Command:
  `python3 scripts/validate_install_fingerprint.py /config/custom_components --ssh-host root@192.168.86.200 --ssh-port 2222 --expected-json tmp/expected-install-fingerprint-v0.4.11.json --compare-json tmp/install-fingerprint-compare-v0.4.11-before-restart.json`
- Result: `overall_match=true`

After restart:

- Command:
  `python3 scripts/validate_install_fingerprint.py /config/custom_components --ssh-host root@192.168.86.200 --ssh-port 2222 --expected-json tmp/expected-install-fingerprint-v0.4.11.json --compare-json tmp/install-fingerprint-compare-v0.4.11-after-restart.json`
- Result: `overall_match=true`

## Restart / Runtime State

- Home Assistant restart was requested through `homeassistant.restart`.
- The request returned HTTP `504` while Home Assistant dropped for restart.
- API polling recovered successfully.
- Post-restart state:
  - `sensor.zero_net_export_installed_version=0.4.11`
  - `update.zero_net_export_update` installed/latest `v0.4.11`
  - `sensor.zero_net_export_status=blocked`

## Slave Browser Proof

- Browser node: Slave Debian Browser Node.
- URL:
  `http://192.168.86.200:8123/zero-net-export?validation=zne-598-v0411-live`
- Header showed `Version 0.4.11`.
- Managed Devices tab opened.
- Unmanaged Candidate Queue showed `Review & promote` actions.
- Clicking the first candidate, `Lounge Room - Heated Floor`
  (`switch.ac_outlet_1`), visibly opened the workflow above the candidate table.
- Visible workflow heading:
  `Review & promote: Lounge Room - Heated Floor`.
- The selected row button changed from `Review & promote` to `Reviewing`.
- The workflow included editable settings and the confirmation that the original
  Home Assistant device/entity will not be modified.
- Browser console check for the validation tab returned no error messages.

## Log Review

Post-restart `ha core logs` review found the known Zero Net Export recorder
attribute-size warnings. These remain tracked under `ZNE-595`.

No new Zero Net Export traceback or ZNE-598-specific release blocker was found.

## Decision

`v0.4.11` is released, installed through HACS, restarted, fingerprint-matched,
and live validated for `ZNE-598`.
