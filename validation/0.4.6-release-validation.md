# Zero Net Export 0.4.6 Release Validation

Date: 2026-07-09

## Scope

Bug fix for Managed Devices unmanaged candidate visibility.

## Validation

- `node --check custom_components/zero_net_export/frontend/zero-net-export-app.js` passed.
- `python3 -m unittest discover -v` passed, 633 tests.
- `git diff --check` passed.
- Published commit `bec1bcf` and tag `v0.4.6`.
- HACS/Home Assistant installed version reports `v0.4.6`; update entity reports installed/latest `v0.4.6`.
- Install fingerprint comparison matched repo build `bec1bcf` with `overall_match=true`.
- Home Assistant restarted and API returned `{"message":"API running."}`.
- Live `sensor.managed_devices_overview` exposed the full candidate queue:
  - `candidate_devices_len=28` immediately after restart, later rendering refreshed to 29.
  - no internal `managed_devices_*` or `Managed Devices ...` helper entities remained in the candidate queue.
  - candidate row fields included `needs_review`, `usefulness_label`, `warning_summary`, and `fit_confidence`.
- Browser validation confirmed the app shows `Version 0.4.6`.
- Browser validation confirmed the Managed Devices tab renders `Unmanaged Candidate Queue` with review/ready counts and visible candidate rows.
- Narrow viewport validation confirmed candidate rows stack and remain readable.
- Desktop viewport validation confirmed candidate rows render in the multi-column table without visible overlap.
- `/config/home-assistant.log` tail had no matching Zero Net Export or recorder warnings after restart.

## Notes

The live candidate count changed from 28 to 29 during validation as Home Assistant state refreshed. The previous count of 30 included two internal Zero Net Export helper/control entities that are now intentionally excluded.
