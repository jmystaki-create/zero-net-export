# Zero Net Export 0.4.15 Release Validation

Date: 2026-07-15

## Scope

Release `v0.4.15` completes `ZNE-599`, the Managed Devices promotion regression.
It covers the full app-owned promotion path:

- confirmation checkbox state persists across app re-renders,
- `zero_net_export.promote_managed_device` accepts UI numeric payloads,
- the candidate is added to the managed Fleet List,
- the original Home Assistant entity is not mutated,
- the promoted numeric priority renders without frontend errors.

## Repository Validation

- `python3 -m unittest discover -s tests -v` passed: 641 tests OK.
- Focused release tests passed: 87 tests OK.
- `node --check custom_components/zero_net_export/frontend/zero-net-export-app.js` passed.
- `python3 -m compileall -q custom_components/zero_net_export tests scripts` passed.
- `git diff --check` passed.

## Release Artifact

- Source commit: `ee8b30d`
- Tag: `v0.4.15`
- GitHub release: https://github.com/jmystaki-create/zero-net-export/releases/tag/v0.4.15
- GitHub latest release check: `v0.4.15`, published 2026-07-15.

## HACS / Home Assistant Install

- HACS update entity reported installed/latest `v0.4.15`.
- Installed version sensor reported `0.4.15`.
- Pre-restart install fingerprint matched `ee8b30d`.
- Home Assistant restart request returned HTTP `504` while HA restarted; the API recovered and ZNE entities loaded.
- Post-restart install fingerprint matched `ee8b30d`.
- No legacy component child paths, legacy backup paths, or stale bytecode paths were found.

## Live Promotion Proof

Validation candidate: `switch.ac_outlet_1` / Lounge Room Heated Floor.

Observed in the installed app:

- The review form opened from the Unmanaged Candidate Queue.
- `Enabled after promotion` was set false for safety.
- The required confirmation checkbox stayed checked across an immediate render and the 10 second app refresh render.
- The app called `zero_net_export.promote_managed_device` with:
  - `candidate_entity_id: switch.ac_outlet_1`
  - `enabled: false`
  - `priority: 120`
  - `nominal_power_w: 2400`
  - `confirm: true`
- HA state showed `switch.ac_outlet_1` no longer present in candidate devices.
- HA state showed a new managed fleet record:
  - key: `lounge_room_heated_floor`
  - entity_id: `switch.ac_outlet_1`
  - enabled: `false`
  - priority: `120`
  - nominal power: `2400 W`
- The original HA entity id remains `switch.ac_outlet_1`; promotion created only the ZNE managed-load record.

Final browser proof on `v0.4.15`:

- App header showed `Version 0.4.15`.
- Managed Devices opened successfully.
- Fleet List rendered 2 devices.
- `lounge_room_heated_floor` appeared in the managed Fleet List.
- `switch.ac_outlet_1` was absent from the Unmanaged Candidate Queue.
- Fresh browser console for the final validation tab had no errors.

## Logs

Historical frontend errors from `v0.4.14`/stale browser tabs remained in the HA
log buffer:

- `TypeError: (a.priority || "").toLowerCase is not a function`

The post-`v0.4.15` restart/current-window scan after 22:37 only found the
standard Home Assistant custom integration warning for `zero_net_export`; it
found no current `TypeError`, no `expected float` promotion schema error, and no
Zero Net Export traceback.

## Residual Runtime State

The release is valid for `ZNE-599`. Separate runtime source-health state still
needs normal operational attention when it appears:

- Some live source entities were observed unavailable during restart recovery
  windows.
- This is tracked separately from the promotion bug and does not invalidate the
  promotion release proof.

## Result

`ZNE-599` is released and live-validated in `v0.4.15`.
