# Zero Net Export 0.2.2 release validation

Date: 2026-06-30
Status: GitHub Release published; HACS install live-validated

## Scope

Corrective release `0.2.2` covers:

- Home Assistant app managed-device action form fix: capture the managed-device
  key and `REMOVE FROM ZNE` confirmation before rendering busy state.
- ZNE-593 Managed Devices `Fleet summary` formatting fix: long live Home
  Assistant values wrap inside stable app rows instead of overlapping labels.

All live install work used the approved GitHub/HACS path. No direct Home
Assistant custom-component file backend deployment was used.

## Repo validation

Passed before release commit/tag:

- `node --check custom_components/zero_net_export/frontend/zero-net-export-app.js`
- `python3 -m py_compile custom_components/zero_net_export/__init__.py custom_components/zero_net_export/app_api.py custom_components/zero_net_export/const.py`
- `python3 -m unittest -q tests.test_managed_devices_panel tests.test_operator_docs_consistency tests.test_bucket_ownership_copy`
  - `Ran 28 tests`
  - `OK`
- `python3 -m unittest discover -s tests -q`
  - `Ran 615 tests`
  - `OK`
  - Output includes expected negative-path `ERROR:` lines from release/deploy
    guard tests.
- `git diff --check`

## GitHub / HACS path

- Release prep commit: `9c3f886 release: prepare zero net export 0.2.2`
- Pushed `main` to GitHub.
- Created and pushed tag `v0.2.2`.
- Public GitHub tag API reports `v0.2.2` at commit `9c3f886`.
- GitHub Release `v0.2.2` published:
  `https://github.com/jmystaki-create/zero-net-export/releases/tag/v0.2.2`
- GitHub Release metadata reports `draft=false`, `prerelease=false`, and
  `publishedAt=2026-06-30T03:59:50Z`.

HACS install:

- HACS repository refresh succeeded through websocket command
  `hacs/repository/refresh` with repository `1189518522`.
- HACS explicit tag download succeeded through websocket command
  `hacs/repository/download` with repository `1189518522` and version
  `v0.2.2`.
- `update.zero_net_export_update` then reported `installed_version=v0.2.2`
  and restart required.
- After the GitHub Release object was published, HACS repository refresh
  succeeded and `update.zero_net_export_update` reports
  `installed_version=v0.2.2`, `latest_version=v0.2.2`, and release URL
  `https://github.com/jmystaki-create/zero-net-export/releases/v0.2.2`.

## Install fingerprint and restart

Expected fingerprint:

- `tmp/expected-install-fingerprint-v0.2.2.json`
- `manifest_version=0.2.2`
- `repo_head_commit=9c3f886`
- `expected_component_commit=9c3f886`

Pre-restart install fingerprint:

- `tmp/install-fingerprint-compare-v0.2.2-before-restart.json`
- `overall_match=true`
- `manifest_version_matches=true`
- all tracked files matched
- no legacy artifacts found

Restart:

- Restart requested through Home Assistant API.
- Restart request returned a gateway timeout while Home Assistant was going
  down.
- API recovered at poll attempt 5 with `{"message":"API running."}`.

Post-restart install fingerprint:

- `tmp/install-fingerprint-compare-v0.2.2-after-restart.json`
- `overall_match=true`
- `manifest_version_matches=true`
- all tracked files matched
- no legacy artifacts found

Post-restart state/API proof:

- `sensor.zero_net_export_installed_version=0.2.2`
- `update.zero_net_export_update.installed_version=v0.2.2`
- `update.zero_net_export_update.latest_version=v0.2.2` after final HACS
  metadata refresh.
- `/zero-net-export` returned HTTP `200`.
- `/zero_net_export_static/zero-net-export-app.js` returned HTTP `200` with
  `26094` bytes.
- Targeted log scan found no Zero Net Export traceback/error/warning lines in
  the tailed Home Assistant logs.

## Live app validation

Temporary managed-load setup:

- Pre-check: `sensor.zero_net_export_managed_devices_count=0`.
- Pre-check: `light.7th=off`, friendly name `7th`.
- Added a temporary disabled fixed managed load through the supported Home
  Assistant subentry flow:
  - name: `7th validation load`
  - entity: `light.7th`
  - nominal power: `10 W`
  - priority: `999`
  - enabled: `false`
- Add result: flow aborted with `managed_device_saved`.
- Managed count after add: `1`.
- Confirmed managed-device key: `7th_validation_load`.
- Confirmed `light.7th` still existed and remained `off`.

Formatting proof:

- Desktop screenshot:
  `validation/artifacts/zne-0.2.2-managed-devices-desktop.png`
- Narrow screenshot:
  `validation/artifacts/zne-0.2.2-managed-devices-narrow.png`
- Desktop geometry JSON:
  `validation/artifacts/zne-0.2.2-managed-devices-desktop.json`
- Narrow geometry JSON:
  `validation/artifacts/zne-0.2.2-managed-devices-narrow.json`
- Both JSON captures found the Managed Devices `Fleet summary` card with 4
  summary rows.
- Geometry check found `any_overlap=false` for desktop and narrow captures.
- Desktop rows use separate label/value columns; narrow rows stack vertically.

Strong-confirmation removal proof:

- Browser automation opened the installed `0.2.2` app, selected Managed
  Devices, typed key `7th_validation_load`, typed confirmation
  `REMOVE FROM ZNE`, and clicked `Remove from ZNE only`.
- The app did not surface the old `Enter a managed-device key first.` error.
- Post-click state:
  - `sensor.zero_net_export_managed_devices_count=0`
  - no `7th_validation_load` entities remain
  - `light.7th=off`
  - `sensor.zero_net_export_installed_version=0.2.2`
- Removal screenshot/JSON:
  - `validation/artifacts/zne-0.2.2-managed-devices-remove-after.png`
  - `validation/artifacts/zne-0.2.2-managed-devices-remove.json`

## Conclusion

The installed Home Assistant runtime is live-validated on `0.2.2` through
GitHub Release publication, HACS download, restart, fingerprint, app route
checks, screenshots, geometry proof, and managed-device add/remove proof. HACS
metadata now reports installed/latest `v0.2.2`.
