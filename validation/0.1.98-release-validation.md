# Zero Net Export 0.1.98 release validation

Date: 2026-04-30
Version: 0.1.98
Tag: v0.1.98
Commit: 4140c1d930fcc6370ec99c108b424042574a0c42

## Release publication

- GitHub release `v0.1.98` was created for `jmystaki-create/zero-net-export`.
- GitHub latest release resolves to `v0.1.98`.
- Release is public, non-draft, and non-prerelease.

## Pre-release local validation

- `python3 -m py_compile custom_components/zero_net_export/config_flow.py` passed.
- `python3 -m unittest discover -s tests` passed: 599 tests OK.
- `git diff --check` passed.
- Working tree was clean before publishing release commit/tag.

## HACS / Home Assistant deployment

- HACS repository refreshed through Home Assistant websocket command `hacs/repository/refresh`.
- HACS detected `available_version: v0.1.98` with `pending_upgrade: true`.
- HACS repository downloaded through `hacs/repository/download` with `version: v0.1.98`.
- HACS reported `installed_version: v0.1.98`, `available_version: v0.1.98`, `pending_upgrade: false`, `status: pending-restart`.
- Install fingerprint validation passed against `/homeassistant/custom_components/zero_net_export` over SSH:
  - `all_tracked_files_match: true`
  - `manifest_version_matches: true`
  - `legacy_artifacts_match: true`
  - `overall_match: true`

## Restart / live validation

- Home Assistant restart was requested through the Home Assistant API. The API call returned a gateway timeout while restart proceeded; availability polling then observed temporary connection refusal followed by HTTP 200 `{"message":"API running."}`.
- Post-restart update entity showed `update.zero_net_export_update` state `off`, installed `v0.1.98`, latest `v0.1.98`.
- Integration runtime entities loaded, including:
  - `sensor.zero_net_export_installed_version` = `0.1.98`
  - `sensor.zero_net_export_previous_installed_version` = `0.1.97`
  - `sensor.zero_net_export_status` = `blocked`
  - `sensor.zero_net_export_reason` = `Validation blocked: solar_power is required but not configured`
- The blocked runtime state is expected for this validation environment because required source roles are not configured; it is not a release-install failure.
- Browser proof captured for the live Home Assistant integration detail page:
  - `bug-evidence/zne-0.1.98-live-integrations.png`
  - Screenshot shows Zero Net Export version `0.1.98`, custom integration, 2 devices, 294 entities, top-level `Add service` and `Add Managed Devices` actions, and Summer/Winter service rows with per-row action controls.

## Logs

- Post-restart log scan found the normal Home Assistant custom integration warning for `zero_net_export`.
- No `custom_components.zero_net_export` traceback/error was observed in the sampled logs.
- Other unrelated existing warnings/errors were present for integrations such as `tuya_local`, `pydaikin`, `robovac`, and `deebot_client`.

## Result

Release `0.1.98` is published, installed via HACS, restarted, and live-validated in Home Assistant.

## Remaining risks

- Exact native Home Assistant managed-row gear placement remains upstream-frontend-limited; this release uses the supported device-detail cog/deep-link path.
- Runtime control remains blocked until required source roles are configured in the validation Home Assistant instance.
