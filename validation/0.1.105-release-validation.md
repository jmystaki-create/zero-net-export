# Zero Net Export 0.1.105 release validation

Date: 2026-05-06
Status: rejected/superseded by 0.1.106.

## Publish and install evidence

- GitHub release `v0.1.105` was published from release commit `f82845f`.
- Home Assistant HACS installed `v0.1.105`.
- Installed package fingerprint matched the intended repository build before restart:
  - `overall_match=true`
  - `manifest_version=0.1.105`
  - `manifest_version_matches=true`
  - no tracked-file mismatches
  - no legacy artifacts
- Home Assistant restart was requested via API; the request returned `504` while HA went down, then the API recovered.
- Runtime state after restart reported:
  - `sensor.zero_net_export_installed_version=0.1.105`
  - `update.zero_net_export_update installed_version=v0.1.105 latest_version=v0.1.105`

## ZNE-589 live result

Passed.

Browser proof from the installed build showed the Home Assistant sidebar/menu without the retired `ZNE Managed Devices` item and without `/zero-net-export-managed-devices` text.

Evidence:

- `validation/artifacts/zne-589-ha-sidebar-v0.1.105.png`
- `validation/artifacts/zne-589-ha-sidebar-v0.1.105.json`

Text/AX extraction counts from the proof JSON:

- `ZNE Managed Devices`: 0
- `zero-net-export-managed-devices`: 0

## ZNE-588 live result

Failed.

Browser proof from the installed Winter Plan device page showed the retired guided launcher button registry rows still visible after upgrade:

- `Open Sensors setup`
- `Open Controls setup`
- `Open Managed Devices setup`
- `Open Diagnostics setup`

The repo code no longer creates those buttons, but live entity-registry inspection showed stale rows persisted from earlier releases for both Winter and Summer entries. That means ZNE-588 cannot be closed on `0.1.105`.

Evidence:

- `validation/artifacts/zne-588-tier1-device-v0.1.105.png`
- `validation/artifacts/zne-588-tier1-device-v0.1.105.json`

Text/AX extraction counts from the proof JSON:

- `Open Sensors setup`: 2
- `Open Controls setup`: 2
- `Open Managed Devices setup`: 2
- `Open Diagnostics setup`: 2
- `⚙ Settings — Open Managed Devices review`: 2
- `⚙ Settings — Open Managed Devices workspace`: 2
- `Firmware: 0.1.105`: 2

Live entity-registry inspection found stale unique IDs ending in:

- `_open_sensors_guided_flow`
- `_open_controls_guided_flow`
- `_open_managed_devices_guided_flow`
- `_open_diagnostics_guided_flow`

## Follow-up

`0.1.106` adds upgrade/migration cleanup for the stale guided setup launcher button registry rows and supersedes `0.1.105` for final ZNE-588 live validation.
