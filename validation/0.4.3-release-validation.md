# Zero Net Export 0.4.3 Release Validation

Date: 2026-07-08
Status: released and live validated

## Scope

Release `0.4.3` covers `ZNE-FR-013`:

- Restructure the Overview Readiness card.
- Show compact health chips and current focus.
- Explain active readiness issues with `What is wrong` and `How to resolve`.
- Surface source blockers, power reconciliation mismatch, controls readiness,
  and managed-device queue context when present.

Implementation evidence:

- `custom_components/zero_net_export/frontend/zero-net-export-app.js`
- `validation/zne-fr-013-overview-readiness-clarity.md`

## Release Approval

Riley approved release execution on 2026-07-08 with: `release the next stage/fixes`.

## Pre-Release Repo Validation

Ran before the release commit/tag:

```text
node --check custom_components/zero_net_export/frontend/zero-net-export-app.js
OK

python3 -m unittest tests.test_managed_devices_panel tests.test_install_helper_scripts tests.test_release_info_install_guidance -v
Ran 81 tests in 0.965s
OK

python3 -m unittest discover -s tests -v
Ran 629 tests in 1.865s
OK

git diff --check
OK
```

Expected negative output remains present inside release/deploy helper tests for
guarded failure paths; those tests finished `OK`.

## GitHub Release

- Release: `v0.4.3`
- URL: https://github.com/jmystaki-create/zero-net-export/releases/tag/v0.4.3
- Release object: published, not draft, not prerelease.
- Tag target: `main`.
- Release prep commit: `644b165 release: prepare zero net export 0.4.3`.

## HACS Install

- HACS metadata initially still reported installed/latest `v0.4.2` after an
  update-entity refresh.
- Explicit `update.install` with version `v0.4.3` succeeded through the Home
  Assistant update entity.
- Install progress evidence is saved in
  `tmp/ha-update-install-v0.4.3-response.json`.
- Post-install update state settled on installed/latest `v0.4.3` with
  `in_progress=false`.

## Install Fingerprint

- Expected fingerprint generated from the release commit in
  `tmp/expected-install-fingerprint-v0.4.3.json`.
- Before restart:
  - `overall_match=true`
  - `manifest_version=0.4.3`
  - `manifest_version_matches=true`
  - `all_tracked_files_match=true`
  - no legacy backup/component-child/stale-bytecode artifacts
  - evidence:
    `tmp/install-fingerprint-compare-v0.4.3-before-restart.json`,
    `tmp/install-fingerprint-v0.4.3-before-restart.out`
- After restart:
  - `overall_match=true`
  - `manifest_version=0.4.3`
  - `manifest_version_matches=true`
  - `all_tracked_files_match=true`
  - no legacy backup/component-child/stale-bytecode artifacts
  - evidence:
    `tmp/install-fingerprint-compare-v0.4.3-after-restart.json`,
    `tmp/install-fingerprint-v0.4.3-after-restart.out`

## Home Assistant Restart

- `homeassistant/restart` was issued after the HACS install.
- The restart request timed out while Home Assistant shut down, as expected for
  this endpoint.
- API polling recovered successfully and reported
  `sensor.zero_net_export_installed_version = 0.4.3`.
- Restart timestamp/response artifacts:
  `tmp/ha-restart-v0.4.3-start.txt`,
  `tmp/ha-restart-v0.4.3-response.json`.

## Live Browser / API Proof

- Static installed frontend route contains the new Readiness implementation:

```text
_readinessModel
What is wrong
How to resolve
zne-readiness-card
Current focus
```

- Browser target:
  `http://192.168.86.200:8123/zero-net-export?validation=v043-readiness`.
- Desktop browser proof, viewport `1440 x 1100`:
  - app header contains `Version 0.4.3`
  - Readiness exists
  - `Current focus` exists
  - `What is wrong` exists
  - `How to resolve` exists
  - issue cards include controller status, power reconciliation, controls
    readiness, and managed-device queue
- Narrow browser proof, viewport `390 x 1200`:
  - app header contains `Version 0.4.3`
  - Readiness exists
  - `Current focus` exists
  - `What is wrong` exists
  - `How to resolve` exists
  - Readiness content stacks before the Reconciliation Status card
- The OpenClaw browser node returned inline desktop and narrow screenshots, but
  did not persist the requested PNG paths for this release run. The validation
  evidence above comes from the live browser snapshot/evaluation plus the inline
  screenshot output captured in the release session transcript.

## Post-Restart Logs

- Log review artifact: `tmp/ha-core-logs-v0.4.3-zne-scan.txt`.
- Known oversized recorder-backed attribute warnings are still present for many
  Zero Net Export entities. This is the existing `ZNE-595` issue and remains the
  next implementation task.
- No release-specific app/static/frontend exception was identified in the
  inspected targeted log scan. A Home Assistant supervisor timeout occurred
  around the restart window and was not tied to the Zero Net Export app code.

## Completion Decision

Pass.

`v0.4.3` is published, installed through HACS, restarted, fingerprint-matched,
and live-browser validated for `ZNE-FR-013`.

Known remaining risk: `ZNE-595` recorder attribute warnings persist and should
be fixed next by trimming recorder-backed attributes while keeping full detail
in diagnostics/app API surfaces.
