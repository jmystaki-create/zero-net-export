# Zero Net Export 0.2.9 Release Validation

Date: 2026-07-02

## Scope

Release `v0.2.9` is the final Milestone 4 corrective release. It includes:

- the live Home Assistant source-health configuration fix
- `v0.2.6` command-center focus-path state capping
- `v0.2.7` app row wrapping/layout fix
- `v0.2.8` app module cache busting
- `v0.2.9` guarded custom-element registration

## Live Home Assistant Configuration

Applied approved Home Assistant configuration change:

- Added template sensor `sensor.anker_battery_discharge_power_fixed`
- Source: `sensor.anker_battery_discharge_power`
- Conversion: live `kW` value multiplied by `1000`
- Output metadata: `unit_of_measurement=W`, `device_class=power`,
  `state_class=measurement`
- HA config backup:
  `/config/configuration.yaml.zne-m4-backup-20260702-084718`

Supported ZNE service changes:

- `zero_net_export.update_source_roles` first pointed
  `battery_discharge_power_entity` at
  `sensor.anker_battery_discharge_power_fixed`
- Live reconciliation then showed battery charge and discharge were both
  materially positive together, so the optional
  `battery_discharge_power_entity` role was cleared through the same supported
  service path
- Existing disposable managed device `7th_validation_load` was enabled through
  `zero_net_export.update_managed_device`

## Repo Validation

Passed before release publication:

- `node --check custom_components/zero_net_export/frontend/zero-net-export-app.js`
- `python3 -m py_compile custom_components/zero_net_export/const.py custom_components/zero_net_export/sensor.py tests/test_sensor_entity_categories.py tests/test_release_info_install_guidance.py tests/test_install_helper_scripts.py tests/test_managed_devices_panel.py`
- `python3 -m unittest -q tests.test_release_info_install_guidance tests.test_install_helper_scripts tests.test_managed_devices_panel tests.test_sensor_entity_categories`
  - `Ran 138 tests`
  - `OK`
- `python3 -m unittest discover -s tests -q`
  - `Ran 623 tests`
  - `OK`
  - Output includes expected negative-path `ERROR:` lines from release/deploy
    guard tests
- `git diff --check`

Release prep commit:

- `89276ba release: prepare zero net export 0.2.9`

## GitHub Release

- Pushed `main` to GitHub at `89276ba`
- Created and pushed tag `v0.2.9`
- Published GitHub Release `v0.2.9`:
  `https://github.com/jmystaki-create/zero-net-export/releases/tag/v0.2.9`

## HACS / Home Assistant Install

- HACS repository refresh succeeded through websocket command
  `hacs/repository/refresh` with repository `1189518522`
- HACS explicit tag download succeeded through websocket command
  `hacs/repository/download` with repository `1189518522` and version
  `v0.2.9`
- `update.zero_net_export_update` reported installed/latest `v0.2.9`

## Installed Fingerprint

Pre-restart install fingerprint:

- `tmp/install-fingerprint-validation-v0.2.9-before-restart.json`
- `overall_match=true`
- `manifest_version_matches=true`
- `expected_component_commit=89276ba`
- mismatches: `0`
- legacy artifact paths: none

Post-restart install fingerprint:

- `tmp/install-fingerprint-validation-v0.2.9-after-restart.json`
- `overall_match=true`
- `manifest_version_matches=true`
- `expected_component_commit=89276ba`
- mismatches: `0`
- legacy artifact paths: none

## Restart, Routes, And State

- Home Assistant restart command completed successfully
- API recovered with `/api/` returning `{"message":"API running."}`
- `/zero-net-export` returned HTTP `200`
- `/zero_net_export_static/zero-net-export-app.js` returned HTTP `200`
  with `53822` bytes

Final API proof:

- `sensor.zero_net_export_installed_version=0.2.9`
- `sensor.zero_net_export_previous_installed_version=0.2.8`
- `sensor.zero_net_export_status=ready`
- `sensor.zero_net_export_reason=Sources validated; 1 controllable device is ready`
- `sensor.zero_net_export_confidence=high`
- `sensor.zero_net_export_last_reconciliation_error=0.0 W`
- `sensor.zero_net_export_source_blocker_summary=None`
- `sensor.zero_net_export_managed_devices_enabled_count=1`
- `sensor.zero_net_export_managed_devices_usable_count=1`
- `sensor.zero_net_export_command_center_focus_path` state length `97`
- `light.7th=on`

Final state artifact:

- `validation/artifacts/zne-0.2.9-final-states-20260702.json`

## Logs

Final log artifact:

- `validation/artifacts/zne-0.2.9-ha-log-tail-20260702.txt`

The final focused scan found:

- no Zero Net Export `command_center_focus_path` state-limit error
- no `customElements.define` duplicate-registration error after `v0.2.9`
- only Home Assistant's standard custom-integration warning for
  `zero_net_export`

Other unrelated Home Assistant warnings/errors remained for non-ZNE
integrations and the original Anker utility-meter sensors.

## Browser Proof

Installed `v0.2.9` app proof:

- Overview screenshot:
  `validation/artifacts/zne-0.2.9-overview-desktop.png`
- Overview snapshot:
  `validation/artifacts/zne-0.2.9-overview-desktop.json`
- Diagnostics screenshot:
  `validation/artifacts/zne-0.2.9-diagnostics-desktop.png`
- Diagnostics snapshot:
  `validation/artifacts/zne-0.2.9-diagnostics-desktop.json`

The screenshots show:

- `Version 0.2.9 · 1 plan`
- connected Home Assistant app shell
- Overview readiness with `Status: ready`, `Safe mode: off`, and
  `Source mismatch: off`
- wrapped command-center and next-step rows without visual overlap
- Diagnostics selected, installed version `0.2.9`, update summary from
  `0.2.8` to `0.2.9`, and support states `off`

## Result

Milestone 4 is complete on the validation Home Assistant instance. The
source-health blocker is resolved, runtime source reconciliation is acceptable,
one managed device is usable, and the installed app proof is clean on the
current GitHub/HACS release `v0.2.9`.
