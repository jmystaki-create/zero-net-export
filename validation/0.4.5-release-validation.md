# Zero Net Export 0.4.5 release validation

Status: released and live validated

## Scope

Corrective release for the Overview Readiness card. The frontend no longer
counts affirmative Controls readiness text as a blocking `Controls are paused`
readiness item when sources are healthy and managed devices are actionable.

## Repository validation

- `node --check custom_components/zero_net_export/frontend/zero-net-export-app.js`
- `python3 -m unittest discover -v`
  - Result: 630 tests, OK
- `git diff --check`

## GitHub / HACS

- Release commit: `091fefb release: prepare zero net export 0.4.5`
- Tag: `v0.4.5`
- GitHub release: https://github.com/jmystaki-create/zero-net-export/releases/tag/v0.4.5
- HACS update entity after install:
  - `installed_version=v0.4.5`
  - `latest_version=v0.4.5`

## Home Assistant validation

- Restart requested through Home Assistant API. The request returned `HTTP 504`
  while Home Assistant restarted; API recovery succeeded afterward.
- Installed package fingerprint:
  - `manifest_version=0.4.5`
  - `expected_commit=091fefb`
  - `overall_match=true`
  - no legacy discovery artifacts
- Live state after recovery:
  - `sensor.zero_net_export_installed_version=0.4.5`
  - `sensor.zero_net_export_status=ready`
  - `binary_sensor.zero_net_export_source_mismatch=off`
  - `sensor.zero_net_export_source_blocker_summary=None`
  - `sensor.zero_net_export_stale_source_count=0`
  - `sensor.zero_net_export_current_native_next_action=Validation healthy; safe to start device modelling on top of these sources`

## Browser proof

Browser validation opened:

`http://192.168.86.200:8123/zero-net-export?validation=v045-readiness`

Visible proof:

- Header shows `Version 0.4.5`.
- Overview status chips show:
  - `Status: ready`
  - `Safe mode: off`
  - `Source mismatch: off`
- Readiness summary shows:
  - `Ready`
  - `No blocking readiness issue is currently reported.`
- The managed-device queue remains visible as review guidance, but no red
  readiness item count is shown.

## Logs / risk

`ha core logs -n 300` showed recurring known recorder warnings for large Zero Net
Export entity attributes exceeding `16384` bytes. No release-blocking Zero Net
Export traceback was found in the checked log tail.

